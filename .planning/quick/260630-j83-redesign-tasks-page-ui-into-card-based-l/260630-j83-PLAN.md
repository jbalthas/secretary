---
phase: quick-260630-j83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/taskCategory.ts
  - frontend/src/lib/taskCategory.test.ts
  - frontend/src/components/TaskCard.tsx
  - frontend/src/components/TasksHero.tsx
  - frontend/src/components/MomentumRing.tsx
  - frontend/src/pages/Tasks.tsx
  - frontend/src/styles.css
autonomous: false
requirements:
  - QUICK-J83
user_setup: []

must_haves:
  truths:
    - "Pending Tasks view renders tasks as category-colored cards (gradient thumbnail + lucide icon + title + meta + circular check), not flat rows"
    - "A 'Focus now' hero card appears at the top of the PENDING view surfacing the top pending task, and is hidden in Completed view and when no pending tasks exist"
    - "The header shows a momentum ring with completed/total (e.g. '4/9 done') for the current visible task set"
    - "Toggling a card's check optimistically completes it, reverts on error, and updates the momentum ring"
    - "Completed cards are dimmed with a line-through title, a 'Completed' label, and a filled check"
    - "Existing behavior is intact: list/goal filter chips, Pending/Completed tabs, sort select, TaskDrawer open/save/delete, FAB"
    - "Category→(gradient, icon) mapping is deterministic and stable per category"
  artifacts:
    - path: "frontend/src/lib/taskCategory.ts"
      provides: "Deterministic category resolution + (gradient, icon) mapping for a Task"
      exports: ["resolveCategory", "categoryVisual", "CategoryVisual"]
    - path: "frontend/src/components/TaskCard.tsx"
      provides: "Card replacement for TaskRow with optimistic toggle + completed-state polish"
      min_lines: 50
    - path: "frontend/src/components/TasksHero.tsx"
      provides: "Focus-now hero card for top of pending view"
    - path: "frontend/src/components/MomentumRing.tsx"
      provides: "Circular done/total ring for the header"
    - path: "frontend/src/pages/Tasks.tsx"
      provides: "Wires hero + ring + card grid, preserves chips/tabs/sort/drawer/FAB"
    - path: "frontend/src/styles.css"
      provides: "task-card / tasks-hero / momentum-ring CSS following existing naming"
  key_links:
    - from: "frontend/src/pages/Tasks.tsx"
      to: "frontend/src/components/TaskCard.tsx"
      via: "renders sorted tasks as TaskCard with onToggle=patchTask"
      pattern: "TaskCard"
    - from: "frontend/src/components/TaskCard.tsx"
      to: "frontend/src/lib/taskCategory.ts"
      via: "categoryVisual(resolveCategory(task, goals))"
      pattern: "categoryVisual"
    - from: "frontend/src/components/TasksHero.tsx"
      to: "frontend/src/components/TaskDrawer.tsx"
      via: "onStart opens drawer for the hero task (Tasks.tsx setEditingTask + setDrawerOpen)"
      pattern: "onStart"
---

<objective>
Redesign the Tasks page from a flat row list into a fresh, card-based layout that matches the existing dark indigo design system and the richer visual patterns already on the Today/Now page.

Deliver four things on the Tasks page:
1. Category-colored task cards (gradient thumbnail tile + lucide category icon + title + meta row + circular check toggle), replacing TaskRow.
2. A deterministic category→(gradient, icon) mapping helper so colors/icons are stable and extendable.
3. A "Focus now" hero card at the top of the PENDING view only, surfacing the top pending task and opening it in the existing TaskDrawer.
4. A daily momentum ring in the header showing done/total for the current visible task set.

Purpose: The Tasks page is the last flat-list surface; bringing it up to the visual language of the Now redesign (260624-kmx) makes the app feel cohesive and gives the user a clearer focus signal.
Output: New helper + 3 components, an updated Tasks.tsx, and new CSS in styles.css. No new npm dependencies, no backend changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current Tasks surface and data:
@frontend/src/pages/Tasks.tsx
@frontend/src/components/TaskRow.tsx
@frontend/src/types/task.ts
@frontend/src/types/goal.ts

Design system + existing visual patterns to MATCH (do not import from these, mirror their conventions):
@frontend/src/styles.css
@frontend/src/components/NowHero.tsx
@frontend/src/components/MomentumStrip.tsx

<interfaces>
<!-- Contracts the executor needs. Use directly — no codebase exploration required. -->

Task model (frontend/src/types/task.ts):
```typescript
export type Priority = "high" | "medium" | "low";
export interface Task {
  id: number;
  title: string;
  description?: string;
  priority: Priority;
  due_date?: string;
  reminder_at?: string;
  recurrence_cron?: string;
  estimated_minutes?: number | null;
  list_name?: string | null;
  parent_list_name?: string | null;
  completed: boolean;
  goal_id?: number | null;
  created_at: string;
  updated_at: string;
}
```

Goal model (frontend/src/types/goal.ts) — for resolving a task's category from its linked goal:
```typescript
export interface Goal {
  id: number;
  title: string;
  type: "career" | "life" | "health" | "learning" | "financial";
  list_name: string | null;
  parent_list_name: string | null;
  // ...other fields omitted
}
```

Tasks.tsx already computes (keep these — do not change the data pipeline):
- `sorted: Task[]` — the filtered+sorted visible set (respects active list/goal chip, Pending/Completed tab, sort)
- `listFiltered: Task[]` — chip-filtered set BEFORE the pending/completed split (use this for the ring's total)
- `patchTask(id, { completed })` — the optimistic-capable mutation
- `goals: Goal[]` from useGoals()
- editing-drawer state: `setEditingTask(task)` + `setDrawerOpen(true)` opens the drawer for a task

TaskCard must preserve TaskRow's optimistic-toggle + error-revert contract (frontend/src/components/TaskRow.tsx):
```typescript
const [localCompleted, setLocalCompleted] = useState(task.completed);
const [error, setError] = useState<string | null>(null);
async function handleToggle(e) {
  e.stopPropagation();
  const newValue = !localCompleted;
  setLocalCompleted(newValue);
  setError(null);
  try { await onToggle(task.id, newValue); }
  catch { setLocalCompleted(!newValue); setError("Couldn't save — try again"); }
}
```

lucide-react icons available (import per-name): Briefcase, User, HeartPulse, GraduationCap, Wallet, ShoppingCart, Bell, Repeat, Play, CheckCircle2, Circle, Flag, etc.
</interfaces>

Design-system facts to honor (locked decisions):
- CSS variables: --bg #0f172a, --surface #1e293b, --accent #6366f1, --destructive #ef4444, --text #f1f5f9, --text-secondary #94a3b8, --text-disabled #475569, --border #334155.
- Add new CSS to frontend/src/styles.css, kebab-case, page-scoped prefixes. Use `tasks-` (page family) for hero/card/ring classes to mirror the `now-` / `organize-` families.
- Generated graphics ONLY: CSS linear-gradients + lucide icons. NO external image/photo fetches of any kind. NO new icon/image library — lucide-react only.
- Mobile-first; must still look clean at the existing single-column widths.
- Respect `@media (prefers-reduced-motion: reduce)` like the existing organize CSS does.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic category → (gradient, icon) mapping helper</name>
  <files>frontend/src/lib/taskCategory.ts, frontend/src/lib/taskCategory.test.ts</files>
  <behavior>
    Pure functions, no React, no fetch. Two exports plus a type.

    `resolveCategory(task: Task, goals: Goal[]): string`
    - Resolution order (first non-empty wins, lowercased + trimmed):
      1. task.list_name
      2. task.parent_list_name
      3. the linked goal's list_name, then parent_list_name (look up goals by task.goal_id)
      4. fallback: `priority:${task.priority}` (e.g. "priority:high")
    - The returned string is the stable category KEY used for both visual lookup and grouping.

    `categoryVisual(category: string): CategoryVisual` where
    `CategoryVisual = { key: string; label: string; gradient: string; Icon: LucideIcon }`
    - Maintains a default palette keyed by normalized aliases covering common categories. Map at least:
      - work / job / career  -> Briefcase, indigo gradient
      - personal / life / home -> User, emerald gradient
      - health / fitness / wellness -> HeartPulse, red gradient
      - learning / study / education -> GraduationCap, amber gradient
      - finance / financial / money -> Wallet, teal gradient
      - errands / shopping / chores -> ShoppingCart, slate-blue gradient
    - Priority fallbacks (when category starts with "priority:"):
      - priority:high -> Flag, red gradient
      - priority:medium -> Flag, amber gradient
      - priority:low -> Flag, slate gradient
    - Unknown category -> a deterministic default (e.g. hash category string to one of the defined gradients) so unlisted lists still get a STABLE color/icon (same input always yields same output). Use a small string-hash (sum char codes % paletteLength) — no Math.random.
    - `label` is a human-friendly Title Case of the resolved category (strip the "priority:" prefix for priority fallbacks → e.g. "High priority").
    - gradients are CSS strings like "linear-gradient(135deg, #6366f1, #4338ca)" using colors consistent with the design system (accent indigo, the type-* colors already in styles.css: career #6366f1, life #10b981, health #ef4444, learning #f59e0b, financial #14b8a6).

    Tests (taskCategory.test.ts, vitest):
    - resolveCategory prefers list_name over parent_list_name over goal over priority fallback (4 cases).
    - resolveCategory falls back to "priority:high" when task has no list/goal.
    - resolveCategory is case-insensitive ("Work" and "work" resolve equal).
    - categoryVisual("work") and categoryVisual("career") return the same gradient+Icon (alias stability).
    - categoryVisual returns identical output across two calls for an UNKNOWN category (deterministic hash).
    - categoryVisual("priority:high").label === "High priority" (or your chosen exact label — assert the real value).
  </behavior>
  <action>
    Create frontend/src/lib/taskCategory.ts with `resolveCategory`, `categoryVisual`, and the `CategoryVisual` type per the behavior block. Import `Task` from ../types/task and `Goal` from ../types/goal. Import the icon components and the `LucideIcon` type from "lucide-react". Define a single `const PALETTE` array/record so adding a category later is a one-line edit (keep it explicit — no premature abstraction beyond the alias map). NO new dependency; lucide-react is already in package.json (^1.18.0).

    Write taskCategory.test.ts FIRST (RED), run it failing, then implement until green. Keep functions pure and side-effect free.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/lib/taskCategory.test.ts</automated>
  </verify>
  <done>taskCategory.test.ts passes; resolveCategory + categoryVisual exported and deterministic; alias and priority fallbacks covered.</done>
</task>

<task type="auto">
  <name>Task 2: Card grid + Focus-now hero + momentum ring, wired into Tasks.tsx</name>
  <files>frontend/src/components/TaskCard.tsx, frontend/src/components/TasksHero.tsx, frontend/src/components/MomentumRing.tsx, frontend/src/pages/Tasks.tsx, frontend/src/styles.css</files>
  <action>
    Build three components and wire them into Tasks.tsx. Do NOT delete TaskRow.tsx (leave it in place; just stop using it in Tasks.tsx). No backend changes, no new deps.

    **MomentumRing.tsx** — `Props { done: number; total: number }`. Render an SVG circular progress ring (two concentric circles: track in --border, progress arc in --accent via stroke-dasharray) plus centered "{done}/{total}" and a small "done" label, OR place "{done}/{total} done" beside the ring. total===0 renders a neutral full-track ring with "0/0". Pure presentational; aria-label={`${done} of ${total} tasks done`}. No animation requirement, but if you add a stroke transition, gate it behind prefers-reduced-motion in CSS.

    **TaskCard.tsx** — `Props { task: Task; goals: Goal[]; onEdit: (t: Task) => void; onToggle: (id: number, completed: boolean) => Promise<void> }`.
    - Copy TaskRow's optimistic-toggle + error-revert logic EXACTLY (localCompleted state, revert on catch, "Couldn't save — try again" error). This is a hard requirement.
    - Layout: clickable card (onClick={() => onEdit(task)}) containing:
      - a rounded thumbnail tile (~48-56px) with `background: visual.gradient` and the `<visual.Icon size={22} />` centered (white/translucent).
      - a body: title line + a meta row (due date via the existing short-date format from TaskRow.formatDueDate, a priority dot colored by priority, Bell icon if reminder_at, Repeat icon if recurrence_cron).
      - a circular check toggle on the right: a real <input type="checkbox" className="tasks-card-check"> styled round; keep aria-label exactly like TaskRow ("Mark '{title}' complete/incomplete"); call e.stopPropagation() in onChange and onClick so toggling doesn't open the drawer.
    - Compute visual via `categoryVisual(resolveCategory(task, goals))`.
    - Completed state (localCompleted): add a `tasks-card--done` class that dims the card, line-through on the title, collapses the meta row to a single "Completed" label, and shows the check as filled (CSS handles filled appearance; the checkbox stays checked). Keep the error message rendering when set.

    **TasksHero.tsx** — `Props { task: Task; goals: Goal[]; onStart: () => void }` (only rendered when a task exists — parent guards).
    - Full-width hero card with a gradient COVER derived from `categoryVisual(resolveCategory(task, goals)).gradient` (gradient as the card/cover background, the category Icon shown large/translucent in the cover).
    - A "FOCUS" label (uppercase, accent), the task.title, a meta line (est-time "~{estimated_minutes} min" when present, else priority label), and a primary "Start" / open action button calling onStart.
    - Mirror the existing `.now-hero` structure/spacing conventions but as a new `tasks-hero` family (do not reuse now-hero classes).

    **Tasks.tsx wiring** (preserve ALL existing behavior — chips, tabs, sort, drawer, FAB):
    - Keep the existing `taskFilters`, `listFiltered`, `filtered`, `sorted`, `handleSave`, drawer state, and FAB exactly.
    - Momentum ring: place a `<MomentumRing done={…} total={…} />` in the header (next to/under the "Tasks" h1). Define "the current visible set" explicitly as `listFiltered` (the active-chip set, both tabs combined): `total = listFiltered.length`, `done = listFiltered.filter(t => t.completed).length`. Add a one-line comment stating this choice. (Do NOT use deriveMomentum — that helper is scoped to today's due-dated tasks + scheduled blocks, which is the wrong set for this page.)
    - Hero: compute `heroTask = filter === "pending" && sorted.length > 0 ? sorted[0] : null` (top of the already-sorted pending set; deterministic and respects the active chip + sort). Render `{heroTask && <TasksHero task={heroTask} goals={goals} onStart={() => { setEditingTask(heroTask); setDrawerOpen(true); }} />}`. This guarantees the hero is hidden in Completed view and when there are no pending tasks. (Rationale: deriving from the sorted pending set respects the chip filters and sort, which the server-side useNextBestTask endpoint does not — so we do not use useNextBestTask here.)
    - Replace the `sorted.map(task => <TaskRow .../>)` block with a card container `<div className="tasks-card-grid">` mapping to `<TaskCard key={task.id} task={task} goals={goals} onEdit={...same as before...} onToggle={(id, completed) => patchTask(id, { completed })} />`. Keep the empty-state branch unchanged.

    **styles.css** — append a "Tasks page — card redesign" section. Add: `.tasks-header` (h1 + ring row), `.momentum-ring` (+ svg sizing), `.tasks-hero` family (cover/gradient/label/title/meta/action mirroring now-hero spacing), `.tasks-card-grid` (single column mobile-first; optional 2-col at >=640px via media query), `.tasks-card`, `.tasks-card-thumb`, `.tasks-card-body`, `.tasks-card-title`, `.tasks-card-meta`, `.tasks-priority-dot` (+ high/medium/low color modifiers reusing the priority colors), `.tasks-card-check` (round checkbox), `.tasks-card--done` (dim + line-through + filled check + "Completed" label), `.tasks-card-error`. Use the existing CSS variables and the type-* gradient colors. Gate any transition behind `@media (prefers-reduced-motion: reduce)`.

    After editing, run `cd frontend && npx tsc -b` to confirm no type errors. Keep focus-visible working (rely on the global `*:focus-visible` rule; do not strip outlines on the checkbox/buttons).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b && npx vitest run</automated>
  </verify>
  <done>tsc passes with no errors; existing vitest suite stays green; Tasks.tsx renders TaskCard grid + MomentumRing in header + conditional TasksHero; chips/tabs/sort/drawer/FAB untouched in behavior; completed cards show dim+line-through+"Completed"+filled check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Golden-path browser verification</name>
  <what-built>
    Redesigned Tasks page: category-colored task cards, a "Focus now" hero at the top of the Pending view, and a momentum ring (done/total) in the header. Optimistic toggle preserved; completed-state polish on cards.
  </what-built>
  <how-to-verify>
    Use the browser preview (preview_* tools) against the running frontend dev server (`cd frontend && npm run dev`, default http://localhost:5173). Navigate to the Tasks page and confirm the golden path:
    1. The Tasks page renders tasks as cards (gradient thumbnail + category icon + title + meta + circular check), NOT flat rows.
    2. In the PENDING tab, the "Focus now" hero card shows at the top surfacing the top pending task; its Start/open action opens that task in the TaskDrawer.
    3. The header momentum ring shows the correct done/total for the visible set (e.g. "4/9").
    4. Toggling a card's check completes the task optimistically AND the momentum ring's done count updates.
    5. Switch to the Completed tab: the hero is HIDDEN, and completed cards are dimmed with line-through title, a "Completed" label, and a filled check.
    6. Sanity: list/goal filter chips still filter, the sort select still reorders, the FAB still opens the new-task drawer, and a card click opens the edit drawer. Confirm the layout looks clean at a narrow mobile width.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/lib/taskCategory.test.ts` passes (category helper).
- `cd frontend && npx tsc -b` reports no type errors.
- `cd frontend && npx vitest run` keeps the existing suite green.
- Browser golden path (Task 3) confirmed by the user.
- No new entries added to frontend/package.json dependencies.
</verification>

<success_criteria>
- Tasks page shows category-colored cards instead of flat rows, matching the dark indigo design system.
- "Focus now" hero appears only in the Pending view with at least one pending task; hidden in Completed and when empty; its action opens the TaskDrawer.
- Momentum ring shows done/total for the active-chip visible set and updates live on toggle.
- Completed cards are dimmed, line-through, show "Completed", and a filled check.
- Optimistic toggle + error-revert behavior preserved from TaskRow.
- Chips, tabs, sort, drawer (open/save/delete), and FAB all still work.
- Accessibility preserved: checkbox aria-labels and focus-visible intact.
- No new npm dependencies; lucide-react used for all icons; all graphics are CSS gradients.
</success_criteria>

<output>
After completion, create `.planning/quick/260630-j83-redesign-tasks-page-ui-into-card-based-l/260630-j83-SUMMARY.md`.
</output>
