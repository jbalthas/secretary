---
phase: quick-260715-k5j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/timeUtils.ts
  - frontend/src/lib/timeUtils.test.ts
  - frontend/src/components/WeatherFocusHero.tsx
  - frontend/src/styles.css
autonomous: true
requirements: [QUICK-260715-k5j]

must_haves:
  truths:
    - "Greeting reads 'Good morning.' before noon, 'Good afternoon.' from 12:00, 'Good evening.' from 18:00"
    - "The hero shows the current time (updating without a page refresh)"
    - "Greeting and clock stay correct as time passes while the app is left open"
  artifacts:
    - path: "frontend/src/lib/timeUtils.ts"
      provides: "Pure greetingForHour + formatClock helpers"
      contains: "greetingForHour"
    - path: "frontend/src/components/WeatherFocusHero.tsx"
      provides: "Time-based greeting + live clock rendering"
  key_links:
    - from: "frontend/src/components/WeatherFocusHero.tsx"
      to: "frontend/src/lib/timeUtils.ts"
      via: "import { greetingForHour, formatClock }"
      pattern: "greetingForHour"
---

<objective>
The hero header hard-codes "Good morning." so it never changes, and it shows no clock.
Make the greeting reflect the time of day and display the current time, both updating
live while the app is open.

Purpose: Correct, time-aware greeting is a small daily-trust signal for a personal dashboard.
Output: Time-based greeting + live clock in WeatherFocusHero, backed by tested pure helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Existing pure helper module the new functions belong in. -->

From frontend/src/lib/timeUtils.ts:
```typescript
export function isAfterWorkHours(workEnd: string | null): boolean;
```

Test style (frontend/src/lib/timeUtils.test.ts) uses vitest with
`vi.useFakeTimers()` + `vi.setSystemTime(new Date(...))`, `afterEach(() => vi.useRealTimers())`.

WeatherFocusHero.tsx (line 18) currently renders:
```tsx
<p className="weather-focus__greeting">Good morning.</p>
```
The component already uses `useState`/`useEffect` (imported from "react").

CSS classes to reuse/extend: `.weather-focus__top`, `.weather-focus__greeting`,
`.weather-focus__message` (frontend/src/styles.css lines ~1806-1808).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tested time-of-day greeting + clock helpers</name>
  <files>frontend/src/lib/timeUtils.ts, frontend/src/lib/timeUtils.test.ts</files>
  <behavior>
    greetingForHour(hour):
    - hour 0..11  -> "Good morning."
    - hour 12..17 -> "Good afternoon."
    - hour 18..23 -> "Good evening."
    - boundary checks: 11 -> morning, 12 -> afternoon, 17 -> afternoon, 18 -> evening
    formatClock(date):
    - returns a 12-hour time string with AM/PM, e.g. new Date(2026,0,1,9,5) -> "9:05 AM"
      (use date.toLocaleTimeString([], { hour: "numeric", minute: "2-digit" }); assert it
      contains the minute "05" and "AM" rather than pinning an exact locale format)
  </behavior>
  <action>
    Add two pure functions to frontend/src/lib/timeUtils.ts (do not remove isAfterWorkHours):
    - `export function greetingForHour(hour: number): string` implementing the ranges above
      (trailing period included to match the current design).
    - `export function formatClock(date: Date): string` returning
      `date.toLocaleTimeString([], { hour: "numeric", minute: "2-digit" })`.
    Extend frontend/src/lib/timeUtils.test.ts with a `describe("greetingForHour")` block covering
    all three ranges and the 11/12/17/18 boundaries, and a `describe("formatClock")` block asserting
    the minute and meridiem substrings. Match the existing vitest import/style already in the file.
  </action>
  <verify>
    <automated>cd frontend; npx vitest run src/lib/timeUtils.test.ts</automated>
  </verify>
  <done>New tests pass; greetingForHour returns the correct label for each hour range and boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Render live greeting + clock in WeatherFocusHero</name>
  <files>frontend/src/components/WeatherFocusHero.tsx, frontend/src/styles.css</files>
  <action>
    In WeatherFocusHero.tsx:
    - Import `greetingForHour` and `formatClock` from "../lib/timeUtils".
    - Add `const [now, setNow] = useState(() => new Date());` and a `useEffect` that starts
      `setInterval(() => setNow(new Date()), 30000)` and clears it on unmount (empty dep array).
      30s keeps the clock minute-accurate and flips the greeting shortly after each boundary.
    - Replace the hard-coded `Good morning.` text with `{greetingForHour(now.getHours())}`.
    - Add a clock element next to the greeting/message, e.g. inside the `weather-focus__top` left
      block: `<p className="weather-focus__clock" aria-live="polite">{formatClock(now)}</p>`.
    Do not change the weather fetch logic or the "Morning" weather-temp fallback string (out of scope).

    In styles.css: add a `.weather-focus__clock` rule near `.weather-focus__message` (line ~1808),
    styled as a small muted time line (reuse the message color rgba(255,255,255,.78); font-size ~14px,
    letter-spacing normal). Keep it legible against the hero background image.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit; npx vitest run src/lib/timeUtils.test.ts</automated>
  </verify>
  <done>
    Hero greeting is `{greetingForHour(now.getHours())}` (no literal "Good morning." remains),
    a `.weather-focus__clock` element renders `formatClock(now)`, a 30s interval updates `now`
    and is cleared on unmount, and TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- `cd frontend; npx vitest run src/lib/timeUtils.test.ts` passes (greeting + clock helpers).
- `cd frontend; npx tsc --noEmit` reports no errors.
- Grep confirms no remaining literal `Good morning.` in WeatherFocusHero.tsx.
</verification>

<success_criteria>
- Greeting switches: morning (<12), afternoon (12-17), evening (18-23).
- Current time is displayed and updates without a page refresh.
- Interval is cleaned up on unmount (no leak / no double-timer).
</success_criteria>

<output>
After completion, create `.planning/quick/260715-k5j-fix-greeting-to-switch-between-good-morn/260715-k5j-SUMMARY.md`
</output>
