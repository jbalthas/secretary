---
phase: quick-260618-l8e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Organize.tsx
  - frontend/src/styles.css
autonomous: true
requirements: [QUICK-l8e]
must_haves:
  truths:
    - "A fully booked day still opens an editable scheduling workspace"
    - "Incomplete tasks can be added to, removed from, and adjusted within today's plan"
    - "Fixed calendar events and flexible task blocks are visually distinct"
    - "The workspace remains usable on phone and desktop widths"
  artifacts:
    - path: "frontend/src/pages/Organize.tsx"
      provides: "Interactive task queue and editable schedule"
      contains: "scheduleTask"
    - path: "frontend/src/styles.css"
      provides: "Responsive Organize workspace styling"
      contains: "organize-workspace"
  key_links:
    - from: "frontend/src/pages/Organize.tsx"
      to: "frontend/src/hooks/usePlan.ts"
      via: "approve/replan plan persistence"
      pattern: "usePlan"
    - from: "frontend/src/pages/Organize.tsx"
      to: "frontend/src/hooks/useCalendarEvents.ts"
      via: "fixed event timeline rendering"
      pattern: "useCalendarEvents"
---

<objective>
Replace the Organize tab's dead-end fully-booked message with a clean scheduling workspace where tasks can be assigned to explicit time slots, edited, removed, and saved alongside visible fixed calendar events.
</objective>

<tasks>

<task type="auto">
  <name>Build the interactive Organize scheduling workspace</name>
  <files>frontend/src/pages/Organize.tsx, frontend/src/styles.css</files>
  <action>
    Redesign Organize as a responsive task queue plus day schedule. Preserve existing plan APIs,
    expose all incomplete unscheduled tasks, allow adding a task at a sensible default time,
    editing start/duration, removing scheduled tasks, auto-arranging, and approving/replacing the
    plan. Render calendar events as fixed context and keep editing available when fully booked.
  </action>
  <verify>cd frontend &amp;&amp; npm run build</verify>
  <done>The fully-booked state is actionable and the scheduling workflow is clean and responsive.</done>
</task>

<task type="auto">
  <name>Browser-test desktop and mobile interactions</name>
  <files>frontend/src/pages/Organize.tsx, frontend/src/styles.css</files>
  <action>
    Run the app, inspect Organize in the in-app browser, verify add/edit/remove/auto-arrange/save
    controls, capture desktop and mobile screenshots, and fix visual or responsive defects.
  </action>
  <verify>Browser interaction checks plus production build</verify>
  <done>The rendered workspace is polished, usable, and free of clipping or broken controls.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build`
- Browser verification at desktop and mobile widths
- Fully-booked API result still renders task queue and editable schedule
</verification>

<success_criteria>
- No dead-end fully-booked screen
- User can assign any incomplete task to a chosen time
- Fixed calendar commitments are visible and distinguishable
- Existing approve/replan API behavior remains intact
</success_criteria>

