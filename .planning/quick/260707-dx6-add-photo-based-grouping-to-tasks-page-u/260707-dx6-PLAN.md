---
phase: quick-260707-dx6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/__init__.py
  - backend/migrations/versions/0019_add_group_photos.py
  - backend/app/routers/group_photos.py
  - backend/app/main.py
  - backend/tests/test_group_photos.py
  - frontend/src/types/groupPhoto.ts
  - frontend/src/hooks/useGroupPhotos.ts
  - frontend/src/components/GroupTileGrid.tsx
  - frontend/src/pages/Tasks.tsx
  - frontend/src/styles.css
autonomous: true
requirements: [DX6-01]

must_haves:
  truths:
    - "Tasks page opens on a photo-tile grid instead of the chip row + flat list"
    - "Each top-level list and each linked goal gets its own tile"
    - "User can upload/replace a photo per tile via a camera/edit overlay and native file picker"
    - "Uploaded photos persist across sessions (stored server-side)"
    - "Clicking a tile drills into that group's existing filtered task view; a Back control returns to the grid"
    - "Tiles without a photo show the existing icon+gradient placeholder"
    - "Tasks with no list/goal are reachable via an Unsorted catch-all tile"
  artifacts:
    - path: "backend/app/routers/group_photos.py"
      provides: "Upload, list, and image-serving endpoints for group photos"
      exports: ["router"]
    - path: "backend/migrations/versions/0019_add_group_photos.py"
      provides: "group_photos table"
      contains: "group_photos"
    - path: "frontend/src/hooks/useGroupPhotos.ts"
      provides: "Photo metadata fetch, upload, and imageUrl helper"
    - path: "frontend/src/components/GroupTileGrid.tsx"
      provides: "Photo tile grid with upload affordance"
  key_links:
    - from: "frontend/src/components/GroupTileGrid.tsx"
      to: "/api/v1/group-photos"
      via: "useGroupPhotos fetch + multipart upload"
      pattern: "group-photos"
    - from: "frontend/src/pages/Tasks.tsx"
      to: "GroupTileGrid"
      via: "grid/drill view-mode state"
      pattern: "GroupTileGrid"
    - from: "backend/app/main.py"
      to: "group_photos.router"
      via: "include_router"
      pattern: "group_photos"
---

<objective>
Add photo-based grouping to the Tasks page. The user uploads a custom photo per top-level umbrella list / linked goal; the Tasks page opens on a grid of those photo tiles, and clicking a tile drills into that group's existing filtered task view.

Purpose: Turn the dense chip-row + flat list entry point into a visual, photo-driven launcher that matches the card-based Tasks redesign (quick task 260630-j83).
Output: A `group_photos` backend table + endpoints, a `useGroupPhotos` hook, a `GroupTileGrid` component, and a grid/drill view-mode reorganization of `Tasks.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260707-dx6-add-photo-based-grouping-to-tasks-page-u/260707-dx6-CONTEXT.md
@backend/app/routers/tts.py
@backend/app/main.py
@backend/app/models/__init__.py
@backend/migrations/versions/0018_advisory_log_priority_rank_last_advisory_at.py
@frontend/src/pages/Tasks.tsx
@frontend/src/lib/taskFilters.ts
@frontend/src/lib/taskCategory.ts
@frontend/src/hooks/useTaskLists.ts

<interfaces>
<!-- Contracts the executor must key off. No codebase exploration needed. -->

Top-level group keys come straight from buildTaskFilters() in frontend/src/lib/taskFilters.ts:
- parent-list tiles: filter.key === `parent-list:${normalize(name)}`  (kind === "parent-list")
- goal tiles:        filter.key === `goal:${goal.id}`                  (kind === "goal")
Sub-lists (kind === "list") are NOT top-level tiles — they remain child chips inside the drilled-in view.

Placeholder visuals come from categoryVisual(category) in frontend/src/lib/taskCategory.ts:
  { key, label, gradient: string /* CSS linear-gradient */, Icon: LucideIcon }
For a tile, resolve the category from the group label (list name) or, for goals, the goal title/category.

Task shape (frontend/src/types/task.ts) fields used here:
  goal_id: number | null; parent_list_name: string | null; list_name: string | null; completed: boolean
"Unsorted" predicate: !task.goal_id && !task.parent_list_name && !task.list_name

Existing DB/proxy facts:
- Vite dev proxy forwards ONLY `/api` (frontend/vite.config.ts). Serve images UNDER `/api/v1/...`, NOT a new StaticFiles mount, so dev works with no proxy/config change.
- Tests build the schema via Base.metadata.create_all (conftest.py), so the new model auto-creates in tests; the Alembic migration is for the real DB.
- python-multipart is installed (fastapi[standard]) — UploadFile/Form work.
- Alembic HEAD is 0018.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend group_photos table + endpoints</name>
  <files>backend/app/models/__init__.py, backend/migrations/versions/0019_add_group_photos.py, backend/app/routers/group_photos.py, backend/app/main.py, backend/tests/test_group_photos.py</files>
  <behavior>
    - POST /api/v1/group-photos with form field `group_key` and file upload → 200, returns {group_key, updated_at}; creates row
    - POST again with same group_key and a different image → 200, replaces bytes (upsert, no duplicate row)
    - GET /api/v1/group-photos → 200, list of {group_key, updated_at} (no image bytes)
    - GET /api/v1/group-photos/image?key={group_key} → 200 with the stored bytes and correct Content-Type
    - GET /api/v1/group-photos/image?key=missing → 404
  </behavior>
  <action>
    Store image bytes IN the database (no static mount, no disk dir) — keeps the feature self-contained and avoids the dev-proxy limitation (only `/api` is proxied).

    1. In backend/app/models/__init__.py add a `GroupPhoto` model (import `LargeBinary` from sqlalchemy):
       - id PK; group_key String(200) unique, nullable=False, index=True; content_type String(100) nullable=False;
         data LargeBinary nullable=False; updated_at DateTime(timezone=True) default+onupdate now(utc) (mirror the Task.updated_at pattern already in this file).
    2. Create migration 0019_add_group_photos.py: revision="0019", down_revision="0018". `op.create_table("group_photos", ...)` with the columns above (use sa.LargeBinary()) + a unique index `ix_group_photos_group_key`. downgrade drops index then table. Mirror 0018's structure.
    3. Create backend/app/routers/group_photos.py (mirror tts.py router setup — `APIRouter(prefix=f"{settings.api_prefix}/group-photos", tags=["group-photos"])`, `get_session` dependency):
       - `POST ""` (status_code=200): params `group_key: str = Form(...)`, `file: UploadFile = File(...)`. Read bytes via `await file.read()`. Upsert by group_key (SELECT existing; update data/content_type or insert new). commit. Return {group_key, updated_at}. Use file.content_type or default "image/jpeg".
       - `GET ""`: return list of {group_key, updated_at} ordered by group_key — do NOT select the `data` column into the response.
       - `GET "/image"`: param `key: str`. Fetch row; if none → raise HTTPException(404). Return `fastapi.Response(content=row.data, media_type=row.content_type)`.
    4. Register in backend/app/main.py: add `group_photos` to the `from app.routers import ...` line and `app.include_router(group_photos.router)`.
    5. Create backend/tests/test_group_photos.py mirroring test_tasks.py (`client = TestClient(app)`). Cover the five behaviors above. Upload via `client.post("/api/v1/group-photos", data={"group_key": "goal:1"}, files={"file": ("p.png", b"\x89PNG...", "image/png")})`.
  </action>
  <verify>
    <automated>cd backend; ./.venv/Scripts/python.exe -m pytest tests/test_group_photos.py -x -q</automated>
  </verify>
  <done>All five behaviors pass; migration 0019 chains from 0018; router registered in main.py.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend photo hook + GroupTileGrid component</name>
  <files>frontend/src/types/groupPhoto.ts, frontend/src/hooks/useGroupPhotos.ts, frontend/src/components/GroupTileGrid.tsx, frontend/src/styles.css</files>
  <action>
    1. frontend/src/types/groupPhoto.ts: `export interface GroupPhotoMeta { group_key: string; updated_at: string }`.
    2. frontend/src/hooks/useGroupPhotos.ts (mirror useTaskLists.ts):
       - state: `Map<string, string>` of group_key → updated_at, built from GET /api/v1/group-photos.
       - `refresh()`: fetch + populate map.
       - `upload(groupKey: string, file: File)`: build FormData (append "group_key", "file"), `await fetch("/api/v1/group-photos", { method: "POST", body: form })`, then `await refresh()`.
       - `imageUrl(groupKey: string): string` → `/api/v1/group-photos/image?key=${encodeURIComponent(groupKey)}&t=${updated_at}` (append updated_at as cache-buster; only call when the map has the key).
       - `hasPhoto(groupKey)` helper. Return { photos: map, hasPhoto, imageUrl, upload, refresh }.
    3. frontend/src/components/GroupTileGrid.tsx. Props: `{ tasks: Task[]; goals: Goal[]; onSelect: (filterKey: string | null) => void }`. Use useGroupPhotos internally.
       - Build tiles from `buildTaskFilters(tasks, goals)` filtered to `kind === "parent-list" || kind === "goal"`. For each: derive placeholder via `categoryVisual(resolveCategory-equivalent)` — for parent-list use the label, for goal use the goal's category (reuse categoryVisual(filter.label.toLowerCase())). Tile renders `<img src={imageUrl(key)}>` when hasPhoto(key), else a div with `background: visual.gradient` + `<visual.Icon />`. Tile label = filter.label. Clicking the tile body → `onSelect(filter.key)`.
       - Overlay a small camera/edit button (lucide `Camera`) on every list/goal tile that triggers a hidden `<input type="file" accept="image/*">`; onChange → `upload(key, file)`. Stop propagation so uploading doesn't trigger drill-in.
       - Append an "Unsorted" catch-all tile (no upload affordance) only when at least one task matches the unsorted predicate (`!goal_id && !parent_list_name && !list_name`); clicking it → `onSelect("__unsorted__")`. Use a neutral gradient + `Inbox`/`Archive` icon.
       - Container: `<div className="group-tile-grid">`.
    4. styles.css: add `.group-tile-grid` (responsive grid, ~2 cols mobile / 3-4 desktop, matches `.tasks-card-grid` spacing), `.group-tile` (aspect-ratio square-ish, rounded, overflow hidden, cover img), `.group-tile-label` (bottom overlay, readable on photo), `.group-tile-camera` (top-right circular button). Follow existing card-based Tasks visual language.
  </action>
  <verify>
    <automated>cd frontend; npm run build</automated>
  </verify>
  <done>tsc + vite build pass; GroupTileGrid renders photo-or-placeholder tiles with a working upload affordance and an Unsorted tile when applicable.</done>
</task>

<task type="auto">
  <name>Task 3: Wire grid/drill view modes into Tasks.tsx</name>
  <files>frontend/src/pages/Tasks.tsx</files>
  <action>
    Reorganize Tasks.tsx into two view modes without changing the existing drilled-in rendering.
    1. Add state `const [mode, setMode] = useState<"grid" | "drill">("grid")`.
    2. When `mode === "grid"`: render the header (title + MomentumRing) and `<GroupTileGrid tasks={tasks} goals={goals} onSelect={(key) => { setActiveFilterKey(key === "__unsorted__" ? null : key); setUnsorted(key === "__unsorted__"); setMode("drill"); }} />`. Do NOT render the chip panel, hero, filter/sort row, or task list in grid mode. Keep the FAB + TaskDrawer mounted in both modes.
    3. Add `const [unsorted, setUnsorted] = useState(false)`. Extend `listFiltered`: when `unsorted` is true, filter to tasks matching the unsorted predicate (`!t.goal_id && !t.parent_list_name && !t.list_name`) and ignore activeTaskFilter; otherwise keep existing behavior.
    4. When `mode === "drill"`: render exactly today's UI (chip panel, hero, tabs, sort, list) PLUS a Back control (e.g. a button with lucide `ArrowLeft`, label "All groups") that calls `setMode("grid"); setActiveFilterKey(null); setUnsorted(false);`. The existing chip row stays so the user can switch sub-lists/goals within the drilled view.
    5. Import GroupTileGrid. Preserve all existing task CRUD wiring (createTask/patchTask/deleteTask, handleSave, refreshLists).
  </action>
  <verify>
    <automated>cd frontend; npm run build; npm test -- --run</automated>
  </verify>
  <done>Tasks page defaults to the tile grid; selecting a tile drills into the scoped list; Back returns to the grid; Unsorted tile shows only orphan tasks; existing tests still pass.</done>
</task>

</tasks>

<verification>
- Backend: `cd backend; ./.venv/Scripts/python.exe -m pytest tests/test_group_photos.py -q` green; `./.venv/Scripts/alembic.exe upgrade head` applies 0019 with no error.
- Frontend: `cd frontend; npm run build` and `npm test -- --run` green.
- Manual golden path (dev): start backend + `npm run dev`; open Tasks → see tile grid → upload a photo to a tile (persists on reload) → click tile → scoped task list with Back control → Back → grid. Confirm an Unsorted tile appears when an orphan task exists.
</verification>

<success_criteria>
- Tasks page opens on the photo-tile grid (grid is the default landing state).
- Every top-level list and linked goal has a tile; sub-lists are not top-level tiles.
- Camera overlay uploads/replaces a photo per tile via native picker; photo persists server-side across reloads.
- Tiles without a photo show the icon+gradient placeholder.
- Clicking a tile drills into the existing scoped filtered view; Back returns to the grid.
- Orphan (no list/goal) tasks reachable via an Unsorted tile.
- All automated checks pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260707-dx6-add-photo-based-grouping-to-tasks-page-u/260707-dx6-SUMMARY.md`
</output>
