---
phase: 09-goals-ingest-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/goal.ts
  - frontend/src/types/task.ts
  - frontend/src/types/routine.ts
  - frontend/src/hooks/useGoals.ts
  - frontend/src/components/GoalSelect.tsx
  - frontend/src/components/BottomNav.tsx
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: true
requirements: [GOAL-04, GOAL-05]
must_haves:
  truths:
    - "A Goals tab appears as the 3rd bottom-nav item (Today / Tasks / Goals / Settings)"
    - "Navigating to /goals and /ingest resolves to a route without a 404"
    - "useGoals() exposes goals plus createGoal/patchGoal/refresh, fetching from /api/v1/goals/"
    - "GoalSelect renders a native select with 'No goal' plus one option per active goal"
  artifacts:
    - path: "frontend/src/types/goal.ts"
      provides: "Goal, GoalCreate, GoalUpdate, Milestone, MilestoneCreate, IngestEntityDiff, IngestPreviewResult, GoalType, GoalStatus"
      contains: "export interface Goal"
    - path: "frontend/src/hooks/useGoals.ts"
      provides: "useGoals plain-fetch hook"
      contains: "export function useGoals"
    - path: "frontend/src/components/GoalSelect.tsx"
      provides: "shared goal dropdown"
      contains: "export"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "/goals and /ingest routes"
      via: "Route elements"
      pattern: "path=\"/goals\""
    - from: "frontend/src/components/BottomNav.tsx"
      to: "/goals"
      via: "NavLink"
      pattern: "to=\"/goals\""
    - from: "frontend/src/hooks/useGoals.ts"
      to: "/api/v1/goals"
      via: "fetch"
      pattern: "/api/v1/goals"
---

<objective>
Lay the shared frontend foundation that the Goals page (Plan 03) and the Ingest page + drawer linking (Plan 04) both build on: TypeScript types, the `useGoals` hook, the shared `GoalSelect` dropdown, the new BottomNav Goals tab, the `/goals` + `/ingest` routes, and all net-new CSS.

Purpose: Plans 03 and 04 run in parallel in Wave 2 and both consume these contracts. Defining them once here prevents drift and scavenger-hunting.
Output: types/goal.ts, types/task.ts + routine.ts extensions, useGoals hook, GoalSelect component, BottomNav 4th tab, App routes (placeholder page imports), CSS additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-goals-ingest-ui/09-CONTEXT.md
@.planning/phases/09-goals-ingest-ui/09-RESEARCH.md
@.planning/phases/09-goals-ingest-ui/09-UI-SPEC.md

<interfaces>
<!-- Backend contracts this foundation models in TypeScript. Extracted from backend schemas. -->

Backend `GoalRead` (backend/app/schemas/goal.py) — shape the Goal type matches:
```python
class GoalRead(BaseModel):
    id: int; title: str; type: GoalType; description: str | None
    target_date: date | None; status: GoalStatus; external_key: str | None
    created_at: datetime; updated_at: datetime; progress_pct: int
    milestones: list[MilestoneRead] = []
# GoalType = career|life|health|learning|financial ; GoalStatus = active|archived|completed
# MilestoneRead: id, goal_id, title, target_date|None, done
# GoalCreate: title(req), type(req), description?, target_date?, external_key?  (omit external_key for manual goals)
# GoalUpdate: title?, type?, description?, target_date?, status?
# MilestoneCreate: title, target_date?
```

Backend preview response (Plan 01 `IngestPreviewResult`):
```python
EntityDiff: { external_key: str, title: str, action: "create" | "update" }
IngestPreviewResult: { goals: EntityDiff[], tasks: EntityDiff[], routines: EntityDiff[], habits: EntityDiff[] }
```

Existing `frontend/src/hooks/useTasks.ts` — the EXACT pattern useGoals mirrors:
```typescript
const API = "/api/v1/tasks";
export function useTasks() {
  const [tasks, setTasks] = useState<Task[]>([]);
  async function refresh() { const res = await fetch(API + "/"); setTasks(await res.json()); }
  async function createTask(body: TaskCreate) { await fetch(API + "/", {method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify(body)}); await refresh(); }
  async function patchTask(id, body) { await fetch(`${API}/${id}`, {method:"PATCH", ...}); await refresh(); }
  async function deleteTask(id) { await fetch(`${API}/${id}`, {method:"DELETE"}); await refresh(); }
  useEffect(() => { refresh(); }, []);
  return { tasks, refresh, createTask, patchTask, deleteTask };
}
```

Existing `frontend/src/components/BottomNav.tsx` — 3 NavLinks (Today/Tasks/Settings), each with `style={({isActive}) => ({...tabStyle, color: isActive ? "var(--accent)" : "var(--text-secondary)"})}`. Imports `{ Calendar, ListTodo, Settings } from "lucide-react"`.

Existing `frontend/src/App.tsx` Routes block:
```tsx
<Routes>
  <Route path="/" element={<Navigate to="/today" replace />} />
  <Route path="/today" element={<Today />} />
  <Route path="/tasks" element={<Tasks />} />
  <Route path="/settings" element={<Settings />} />
</Routes>
```

Existing `frontend/src/types/task.ts` — `Task` and `TaskCreate` interfaces (no goal_id yet).
Existing `frontend/src/types/routine.ts` — `Routine` and `RoutineInput` interfaces (no goal_id yet).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + useGoals hook</name>
  <files>frontend/src/types/goal.ts, frontend/src/types/task.ts, frontend/src/types/routine.ts, frontend/src/hooks/useGoals.ts</files>
  <read_first>
    - frontend/src/types/task.ts (extend, don't rewrite)
    - frontend/src/types/routine.ts (extend, don't rewrite)
    - frontend/src/hooks/useTasks.ts (copy this shape exactly)
  </read_first>
  <action>
    1. Create `frontend/src/types/goal.ts` with:
    ```typescript
    export type GoalType = "career" | "life" | "health" | "learning" | "financial";
    export type GoalStatus = "active" | "archived" | "completed";

    export interface Milestone {
      id: number;
      goal_id: number;
      title: string;
      target_date: string | null;
      done: boolean;
    }

    export interface Goal {
      id: number;
      title: string;
      type: GoalType;
      description: string | null;
      target_date: string | null;
      status: GoalStatus;
      external_key: string | null;
      created_at: string;
      updated_at: string;
      progress_pct: number;
      milestones: Milestone[];
    }

    export interface GoalCreate {
      title: string;
      type: GoalType;
      description?: string;
      target_date?: string;
    }

    export interface GoalUpdate {
      title?: string;
      type?: GoalType;
      description?: string;
      target_date?: string;
      status?: GoalStatus;
    }

    export interface MilestoneCreate {
      title: string;
      target_date?: string;
    }

    export interface IngestEntityDiff {
      external_key: string;
      title: string;
      action: "create" | "update";
    }

    export interface IngestPreviewResult {
      goals: IngestEntityDiff[];
      tasks: IngestEntityDiff[];
      routines: IngestEntityDiff[];
      habits: IngestEntityDiff[];
    }
    ```

    2. In `frontend/src/types/task.ts`, add `goal_id?: number;` to BOTH the `Task` interface and the `TaskCreate` interface. Do not touch other fields.

    3. In `frontend/src/types/routine.ts`, add `goal_id?: number | null;` to BOTH the `Routine` interface and the `RoutineInput` interface. (Use `number | null` so the drawer can send explicit `null` to unlink — see Plan 04 / Pitfall 3.)

    4. Create `frontend/src/hooks/useGoals.ts` mirroring useTasks.ts exactly:
    ```typescript
    import { useEffect, useState } from "react";
    import type { Goal, GoalCreate, GoalUpdate } from "../types/goal";

    const API = "/api/v1/goals";

    export function useGoals() {
      const [goals, setGoals] = useState<Goal[]>([]);

      async function refresh() {
        const res = await fetch(API + "/");
        setGoals(await res.json());
      }

      async function createGoal(body: GoalCreate) {
        await fetch(API + "/", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(body),
        });
        await refresh();
      }

      async function patchGoal(id: number, body: Partial<GoalUpdate>) {
        await fetch(`${API}/${id}`, {
          method: "PATCH",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(body),
        });
        await refresh();
      }

      useEffect(() => {
        refresh();
      }, []);

      return { goals, refresh, createGoal, patchGoal };
    }
    ```
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export interface Goal" frontend/src/types/goal.ts` matches
    - `grep "progress_pct: number" frontend/src/types/goal.ts` matches
    - `grep "export interface IngestPreviewResult" frontend/src/types/goal.ts` matches
    - `grep "goal_id" frontend/src/types/task.ts` matches (2 occurrences: Task + TaskCreate)
    - `grep "goal_id" frontend/src/types/routine.ts` matches
    - `grep "export function useGoals" frontend/src/hooks/useGoals.ts` matches
    - `grep "/api/v1/goals" frontend/src/hooks/useGoals.ts` matches
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <done>Goal types, task/routine goal_id extensions, and useGoals hook compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: GoalSelect component + BottomNav tab + App routes + CSS</name>
  <files>frontend/src/components/GoalSelect.tsx, frontend/src/components/BottomNav.tsx, frontend/src/App.tsx, frontend/src/styles.css</files>
  <read_first>
    - frontend/src/components/BottomNav.tsx (mirror existing NavLink entries)
    - frontend/src/App.tsx (extend Routes + imports)
    - frontend/src/styles.css (append new rules; confirm classes referenced here don't already exist)
    - .planning/phases/09-goals-ingest-ui/09-UI-SPEC.md §"New CSS Required" (copy the CSS block verbatim)
  </read_first>
  <action>
    1. Create `frontend/src/components/GoalSelect.tsx` — a shared native dropdown used by TaskDrawer and RoutineDrawer (Plan 04). DELIBERATE PATTERN CHOICE: GoalSelect is PROP-DRIVEN — it accepts the goals list as a `goals: Goal[]` prop and does NOT call `useGoals()` internally. This intentionally OVERRIDES RESEARCH.md Pattern 4 (which sketches an internal-useGoals version); the prop-driven form is used per Research Pitfall 2 to avoid double-fetch / stale data — the parent (TaskDrawer's page, RoutineDrawer's page) owns the single `useGoals()` instance and passes goals down. Do not reintroduce an internal `useGoals()` here.
    ```typescript
    import type { Goal } from "../types/goal";

    interface Props {
      goals: Goal[];
      value: number | null;
      onChange: (goalId: number | null) => void;
    }

    export default function GoalSelect({ goals, value, onChange }: Props) {
      const active = goals.filter((g) => g.status === "active");
      return (
        <select
          value={value ?? ""}
          onChange={(e) => onChange(e.target.value ? Number(e.target.value) : null)}
        >
          <option value="">No goal</option>
          {active.map((g) => (
            <option key={g.id} value={g.id}>
              {g.title}
            </option>
          ))}
        </select>
      );
    }
    ```

    2. In `frontend/src/components/BottomNav.tsx`, add `Target` to the lucide-react import: `import { Calendar, ListTodo, Target, Settings } from "lucide-react";`. Insert a Goals NavLink BETWEEN the Tasks NavLink and the Settings NavLink (so tab order is Today / Tasks / Goals / Settings), copying the existing NavLink style-callback pattern exactly:
    ```tsx
    <NavLink
      to="/goals"
      style={({ isActive }) => ({
        ...tabStyle,
        color: isActive ? "var(--accent)" : "var(--text-secondary)",
      })}
    >
      <Target size={22} />
      Goals
    </NavLink>
    ```
    No CSS change needed for the nav — `flex: 1` redistributes the 4 tabs automatically.

    3. In `frontend/src/App.tsx`, add imports `import Goals from "./pages/Goals";` and `import Ingest from "./pages/Ingest";`, and add two routes inside `<Routes>` after the `/tasks` route:
    ```tsx
    <Route path="/goals" element={<Goals />} />
    <Route path="/ingest" element={<Ingest />} />
    ```
    Because Plans 03 (Goals.tsx) and 04 (Ingest.tsx) create the real pages, create MINIMAL placeholder page files so this plan compiles and routes resolve. Create `frontend/src/pages/Goals.tsx` and `frontend/src/pages/Ingest.tsx` each as a stub default-export component returning `<div className="page"><h1 className="page-title">Goals</h1></div>` (and "Import Data" for Ingest). These stubs ARE in files owned by Plans 03/04 — Plans 03/04 will fully replace them; mark each stub file with a top comment `// PLACEHOLDER — replaced in Plan 09-03 (Goals) / 09-04 (Ingest)`.

    4. In `frontend/src/styles.css`, APPEND the net-new CSS block from 09-UI-SPEC.md §"New CSS Required" verbatim. This includes: `.progress-bar-track`, `.progress-bar-fill`, `.type-badge` + `.type-career/.type-life/.type-health/.type-learning/.type-financial`, `.diff-badge` + `.diff-badge-create/.diff-badge-update`, `.prompt-block`, `.error-list` + `.error-list-item`, `.goal-row` + `.goal-row-header/.goal-row-title/.goal-row-pct`, `.milestone-row` + `.milestone-row-title` + `.milestone-row-title.done`, `.section-label`, `.btn-text-accent`, `.btn-text-destructive`. Do not modify any existing rule.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "to=\"/goals\"" frontend/src/components/BottomNav.tsx` matches and `Target` is in the lucide-react import line
    - `grep "path=\"/goals\"" frontend/src/App.tsx` and `grep "path=\"/ingest\"" frontend/src/App.tsx` both match
    - `frontend/src/pages/Goals.tsx` and `frontend/src/pages/Ingest.tsx` exist (placeholder stubs)
    - `grep "export default function GoalSelect" frontend/src/components/GoalSelect.tsx` matches
    - `grep "No goal" frontend/src/components/GoalSelect.tsx` matches
    - GoalSelect does NOT call useGoals internally (verify: `grep useGoals frontend/src/components/GoalSelect.tsx` returns nothing — it is prop-driven)
    - `grep ".progress-bar-fill" frontend/src/styles.css` and `grep ".diff-badge-create" frontend/src/styles.css` and `grep ".type-career" frontend/src/styles.css` all match
    - `npm run build` succeeds with no errors
  </acceptance_criteria>
  <done>Goals tab appears in nav, /goals + /ingest routes resolve, GoalSelect is reusable (prop-driven), all new CSS is present, build is green.</done>
</task>

</tasks>

<verification>
- `cd frontend; npm run build` produces a clean production build.
- The bottom nav shows four tabs in order Today / Tasks / Goals / Settings.
- Visiting /goals and /ingest renders placeholder pages (no router 404).
</verification>

<success_criteria>
- All shared contracts (goal types, useGoals, GoalSelect, routes, nav tab, CSS) exist so Plans 03 and 04 can build in parallel without redefining anything.
</success_criteria>

<output>
After completion, create `.planning/phases/09-goals-ingest-ui/09-02-SUMMARY.md`.
</output>
