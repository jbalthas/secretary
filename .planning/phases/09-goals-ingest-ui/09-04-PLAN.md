---
phase: 09-goals-ingest-ui
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - frontend/src/components/TaskDrawer.tsx
  - frontend/src/components/RoutineDrawer.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/pages/Tasks.tsx
  - frontend/src/lib/ingestPrompt.ts
  - frontend/src/hooks/useIngest.ts
  - frontend/src/pages/Ingest.tsx
autonomous: false
requirements: [GOAL-05, INGEST-03, INGEST-05]
must_haves:
  truths:
    - "Task drawer shows a goal dropdown (active goals + No goal); saving links the task to the goal"
    - "Routine drawer shows a goal dropdown; saving sends goal_id explicitly (null to unlink)"
    - "Ingest page accepts JSON via textarea paste OR .json file upload into one shared payload"
    - "Run Preview calls POST /ingest/preview and renders a grouped per-entity create/update diff without writing"
    - "Confirm posts the same payload to /ingest/confirm; the Confirm button is disabled while submitting"
    - "HTTP 422 from preview or confirm renders a readable field-level error list"
  artifacts:
    - path: "frontend/src/lib/ingestPrompt.ts"
      provides: "documented LLM prompt constant"
      contains: "schema_version"
    - path: "frontend/src/hooks/useIngest.ts"
      provides: "preview + confirm + loading/error state hook"
      contains: "export function useIngest"
    - path: "frontend/src/pages/Ingest.tsx"
      provides: "paste/upload + preview + confirm UI"
      contains: "Run Preview"
      min_lines: 80
  key_links:
    - from: "frontend/src/components/TaskDrawer.tsx"
      to: "GoalSelect"
      via: "goal dropdown field"
      pattern: "GoalSelect"
    - from: "frontend/src/components/RoutineDrawer.tsx"
      to: "GoalSelect"
      via: "goal dropdown field; goal_id sent explicitly"
      pattern: "GoalSelect"
    - from: "frontend/src/hooks/useIngest.ts"
      to: "/api/v1/ingest/preview and /api/v1/ingest/confirm"
      via: "fetch"
      pattern: "/api/v1/ingest/(preview|confirm)"
---

<objective>
Complete GOAL-05 (task + routine goal linking via shared GoalSelect) and INGEST-03/INGEST-05 (Ingest page: paste-or-upload JSON → dry-run preview with per-entity diff → confirm, disabled-on-submit, 422 field-level errors). Authors the documented LLM prompt artifact deferred from Phase 8.

Purpose: GOAL-05 needs goal dropdowns in both drawers; INGEST-05 needs paste + file upload; INGEST-03 needs the preview diff (endpoint from Plan 01). Confirm reuses the existing /ingest/confirm endpoint.
Output: TaskDrawer + RoutineDrawer goal dropdowns (and their parent wiring), lib/ingestPrompt.ts, hooks/useIngest.ts, pages/Ingest.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-goals-ingest-ui/09-CONTEXT.md
@.planning/phases/09-goals-ingest-ui/09-UI-SPEC.md
@.planning/phases/09-goals-ingest-ui/09-RESEARCH.md

<interfaces>
<!-- Contracts from Plans 01 + 02 (already on disk) + existing code. Use directly. -->

From Plan 02 `frontend/src/components/GoalSelect.tsx`:
```tsx
import GoalSelect from "../components/GoalSelect";
// <GoalSelect goals={Goal[]} value={number|null} onChange={(goalId: number|null) => void} />
```
GoalSelect takes the goals list as a PROP (parent owns useGoals — avoids stale data, Research Pitfall 2).

From Plan 02 `frontend/src/hooks/useGoals.ts`: `const { goals } = useGoals();`
From Plan 02 `frontend/src/types/goal.ts`: `IngestPreviewResult`, `IngestEntityDiff`.
From Plan 02 type extensions: `Task.goal_id?: number` / `TaskCreate.goal_id?: number`; `Routine.goal_id?: number|null` / `RoutineInput.goal_id?: number|null`.

From Plan 01 backend: `POST /api/v1/ingest/preview` -> `{goals,tasks,routines,habits}` each `IngestEntityDiff[]`; `POST /api/v1/ingest/confirm` -> `{created:{...}, updated:{...}}`. Both return 422 `{detail:[{loc, msg}]}` on invalid payload. `GET /api/v1/ingest/schema` returns the JSON schema (already live).

Existing `frontend/src/components/TaskDrawer.tsx`: props `{open, task, onClose, onSave(body: TaskCreate, id?), onDelete(id)}`. Builds `body: TaskCreate` in `handleSave`. Has a Priority `.drawer-field` with `.segmented-control`. Add the goal field AFTER Priority.

Existing `frontend/src/pages/Tasks.tsx`: renders `<TaskDrawer ... />` and owns `useTasks()`. This is where TaskDrawer's `goals` prop must be supplied (via `useGoals()` in Tasks.tsx).

Existing `frontend/src/components/RoutineDrawer.tsx`: props `{open, routine, onClose, onSave(body: RoutineInput, id?)=>Promise<boolean>, onDelete}`. Builds `body: RoutineInput = { name, cron, action }` in handleSave. Add a goal field AFTER the Action select. CRITICAL (Research Pitfall 3): because backend uses `exclude_unset=True`, ALWAYS include `goal_id` in the body (an integer or explicit `null`), never omit it, so unlink works.

Existing `frontend/src/pages/Settings.tsx`: renders `<RoutineDrawer ... />` and owns `useRoutines()`. Supply RoutineDrawer's `goals` prop here via `useGoals()`.

Existing `frontend/src/hooks/useRoutines.ts`: `update(id, body: Partial<RoutineInput>)` and `create(body: RoutineInput)`.

422 parse helper (Research Pattern 7):
```typescript
function parse422(detail: {loc:(string|number)[], msg:string}[]): string[] {
  return detail.map(e => `${e.loc.filter(p => p !== "body").join(".")}: ${e.msg}`);
}
```

New CSS already on disk from Plan 02: `.diff-badge`/`.diff-badge-create`/`.diff-badge-update`, `.prompt-block`, `.error-list`/`.error-list-item`, `.section-label`. Reuse `.page`, `.page-title`, `.btn-save`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Goal dropdown in TaskDrawer + RoutineDrawer (and parent wiring)</name>
  <files>frontend/src/components/TaskDrawer.tsx, frontend/src/components/RoutineDrawer.tsx, frontend/src/pages/Tasks.tsx, frontend/src/pages/Settings.tsx</files>
  <read_first>
    - frontend/src/components/TaskDrawer.tsx (add goal field + goal_id state)
    - frontend/src/components/RoutineDrawer.tsx (add goal field + goal_id state; send explicit null)
    - frontend/src/pages/Tasks.tsx (supply goals prop)
    - frontend/src/pages/Settings.tsx (supply goals prop)
    - frontend/src/components/GoalSelect.tsx (the shared component)
    - .planning/phases/09-goals-ingest-ui/09-RESEARCH.md §"Pitfall 3" (exclude_unset null semantics)
  </read_first>
  <action>
    1. TaskDrawer: add a `goals: Goal[]` prop to its Props interface. Add `const [goalId, setGoalId] = useState<number | null>(null);`. In the `useEffect([open, task])` reset block add `setGoalId(task?.goal_id ?? null);`. Add a new `.drawer-field` AFTER the Priority field:
    ```tsx
    <div className="drawer-field">
      <label>Goal</label>
      <GoalSelect goals={goals} value={goalId} onChange={setGoalId} />
    </div>
    ```
    In `handleSave`, add `goal_id: goalId ?? undefined,` to the `body: TaskCreate` object (tasks may omit it). Import `GoalSelect` and `type Goal`.

    2. Tasks.tsx: call `const { goals } = useGoals();` and pass `goals={goals}` to the `<TaskDrawer .../>`. Import useGoals.

    3. RoutineDrawer: add a `goals: Goal[]` prop. Add `const [goalId, setGoalId] = useState<number | null>(null);` and in the `useEffect([open, routine])` reset block add `setGoalId(routine?.goal_id ?? null);`. Add a `.drawer-field` AFTER the Action select:
    ```tsx
    <div className="drawer-field">
      <label htmlFor="routine-goal">Goal</label>
      <GoalSelect goals={goals} value={goalId} onChange={setGoalId} />
    </div>
    ```
    In `handleSave`, build the body as `const body: RoutineInput = { name, cron: cron.trim(), action, goal_id: goalId };` — ALWAYS include `goal_id` (integer or explicit `null`), never omit it, so unlinking works under the backend's `exclude_unset=True`. Import `GoalSelect` and `type Goal`.

    4. Settings.tsx: call `const { goals } = useGoals();` and pass `goals={goals}` to `<RoutineDrawer .../>`. Import useGoals.
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "GoalSelect" frontend/src/components/TaskDrawer.tsx` and `grep "GoalSelect" frontend/src/components/RoutineDrawer.tsx` both match
    - `grep "goal_id: goalId" frontend/src/components/RoutineDrawer.tsx` matches (explicit, not `?? undefined`)
    - `grep "goal_id" frontend/src/components/TaskDrawer.tsx` matches
    - `grep "goals={goals}" frontend/src/pages/Tasks.tsx` and `grep "goals={goals}" frontend/src/pages/Settings.tsx` both match
    - `grep "useGoals" frontend/src/pages/Tasks.tsx` and `grep "useGoals" frontend/src/pages/Settings.tsx` both match
    - `npm run build` clean
  </acceptance_criteria>
  <done>Both drawers show a goal dropdown; task linking omits goal_id when unset, routine linking always sends goal_id (incl. null).</done>
</task>

<task type="auto">
  <name>Task 2: LLM prompt artifact + useIngest hook</name>
  <files>frontend/src/lib/ingestPrompt.ts, frontend/src/hooks/useIngest.ts</files>
  <read_first>
    - backend/app/schemas/ingest.py (the exact payload shape the prompt must describe: schema_version "1.0", goals[], tasks[], routines[], habits[]; external_key, goal_key references, cron format)
    - frontend/src/hooks/useTasks.ts (fetch + state pattern)
    - .planning/phases/09-goals-ingest-ui/09-RESEARCH.md §"useIngest hook shape" + §"422 error parsing"
  </read_first>
  <action>
    1. Create `frontend/src/lib/ingestPrompt.ts` exporting `export const INGEST_PROMPT = \`...\`;` — a documented prompt the user pastes into any LLM to produce a compliant payload. It MUST describe, accurately from backend/app/schemas/ingest.py:
       - The top-level object: `{ "schema_version": "1.0", "goals": [...], "tasks": [...], "routines": [...], "habits": [...] }` — schema_version MUST be the literal string "1.0"; no extra fields allowed (extra="forbid").
       - GoalImport: external_key (stable slug, required), title, type (one of career|life|health|learning|financial), description?, target_date? (YYYY-MM-DD), milestones?: [{title, target_date?, done?}].
       - TaskImport: external_key, goal_key? (must match a goal's external_key), title, priority (high|medium|low), due_date? (ISO datetime), description?.
       - RoutineImport: external_key, goal_key?, name, cron (5-field cron, e.g. "0 8 * * *"), action ("send_daily_brief").
       - HabitImport: external_key, goal_key?, title, recurrence_cron (5-field cron), priority?, description?.
       - Guidance: use stable slugs for external_key (e.g. "learn-guitar-2026"), reference goals from tasks/routines/habits via goal_key matching the goal's external_key, and that the live JSON schema is available at GET /api/v1/ingest/schema.
       Also export `export const INGEST_SCHEMA_URL = "/api/v1/ingest/schema";`.

    2. Create `frontend/src/hooks/useIngest.ts` per Research §"useIngest hook shape". Include the `parse422` helper (module-private). State: `previewResult: IngestPreviewResult | null`, `errors: string[]`, `previewing: boolean`, `confirming: boolean`. Functions:
       - `preview(payload: unknown)`: POST to `/api/v1/ingest/preview`; on `res.ok` set previewResult; on `res.status === 422` set errors = parse422(body.detail) and clear previewResult.
       - `confirm(payload: unknown): Promise<boolean>`: POST to `/api/v1/ingest/confirm`; return true on ok; on 422 set errors and return false.
       - A `reset()` that clears previewResult + errors (used when input changes).
       The return statement MUST include `reset` — Ingest.tsx (Task 3) calls `ingest.reset()` on textarea change, so omitting it from the returned object causes an `undefined()` runtime crash on input after a preview. Use exactly:
       ```typescript
       return { previewResult, errors, previewing, confirming, preview, confirm, reset };
       ```
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "schema_version" frontend/src/lib/ingestPrompt.ts` matches and the file mentions all of: goals, tasks, routines, habits, external_key, goal_key
    - `grep "export const INGEST_PROMPT" frontend/src/lib/ingestPrompt.ts` matches
    - `grep "export function useIngest" frontend/src/hooks/useIngest.ts` matches
    - `grep "/api/v1/ingest/preview" frontend/src/hooks/useIngest.ts` and `grep "/api/v1/ingest/confirm" frontend/src/hooks/useIngest.ts` both match
    - `grep "parse422\|loc" frontend/src/hooks/useIngest.ts` matches (422 parsing present)
    - `grep "reset" frontend/src/hooks/useIngest.ts` matches AND the literal return statement includes `reset` (verify: `grep "return { previewResult" frontend/src/hooks/useIngest.ts` shows `reset` in the returned object)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>LLM prompt constant and useIngest hook (preview/confirm/422 parsing) exist and compile; the hook returns `reset` so Ingest.tsx can call it without crashing.</done>
</task>

<task type="auto">
  <name>Task 3: Ingest page — prompt, paste/upload, preview diff, confirm, errors</name>
  <files>frontend/src/pages/Ingest.tsx</files>
  <read_first>
    - frontend/src/hooks/useIngest.ts + frontend/src/lib/ingestPrompt.ts
    - .planning/phases/09-goals-ingest-ui/09-UI-SPEC.md §"Ingest Page" + §"Copywriting Contract" + §"Accessibility Contracts"
    - frontend/src/types/goal.ts (IngestPreviewResult shape)
  </read_first>
  <action>
    Replace the Plan-02 placeholder `frontend/src/pages/Ingest.tsx` with the full page (per UI-SPEC §"Ingest Page", top to bottom). Use `const ingest = useIngest();` and `const [rawJson, setRawJson] = useState("");` and `const [parseError, setParseError] = useState<string|null>(null);`. Use `useNavigate` for the post-confirm redirect.

    `.page` + `.page-title` "Import Data".

    1. LLM Prompt section: `.section-label` "LLM Prompt". A `<pre className="prompt-block">{INGEST_PROMPT}</pre>` with `aria-label`. A "Copy prompt" button (`.btn-text-accent`, aria-label "Copy LLM prompt to clipboard") calling `navigator.clipboard.writeText(INGEST_PROMPT)`; on success swap the label to "Copied!" for 2s then revert.

    2. Input section: `.section-label` "Paste JSON or upload a file". A `<textarea aria-label="JSON payload" className=... placeholder="Paste your JSON payload here…">` bound to rawJson (on change: setRawJson + setParseError(null) + ingest.reset()). A file `<input type="file" accept=".json">` (with an associated `<label htmlFor>` — Choose file) whose onChange reads via `FileReader.readAsText(file)` into rawJson (Research Pattern 6 — readAsText, NOT readAsDataURL). A "Run Preview" `.btn-save` (right-aligned, width auto) that: client-side `JSON.parse(rawJson)` in try/catch → on SyntaxError setParseError("Invalid JSON — check your payload and try again.") and return; else `await ingest.preview(parsed)`. Disable + opacity 0.6 while `ingest.previewing`; label "Run Preview"/"Previewing…". Render parseError below the textarea in `--destructive`.

    3. Preview results section (render only when `ingest.previewResult` is set): `.section-label` "Preview" + a counts summary like `{goals.length} goals · {tasks.length} tasks · {routines.length} routines · {habits.length} habits`. Four sub-groups (Goals/Tasks/Routines/Habits) — for each non-empty group, a group header (14px/600) and a list of rows; each row shows the entity title + a right-aligned badge: `create` → `.diff-badge diff-badge-create`, `update` → `.diff-badge diff-badge-update`. Omit empty groups. If all four are empty, show "Nothing to import — the payload contained no recognizable entities."

    4. Confirm section (render only when previewResult is present): a "Confirm Import" `.btn-save` (full width), disabled + opacity 0.6 while `ingest.confirming`, label "Confirm Import"/"Importing…", `aria-disabled={ingest.confirming}`. On click: `const ok = await ingest.confirm(JSON.parse(rawJson));` if ok → navigate("/goals") (Goals page refetches goals on mount). Disable-on-submit prevents double-commit (Success Criterion #4).

    5. Validation error section (render when `ingest.errors.length > 0`): `.section-label` "Validation Errors" in `--destructive`; an `.error-list` with one `.error-list-item` per error string (format `tasks.2.due_date: invalid datetime`).
  </action>
  <verify>
    <automated>cd frontend; npx tsc --noEmit; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Run Preview" frontend/src/pages/Ingest.tsx` matches
    - `grep "readAsText" frontend/src/pages/Ingest.tsx` matches (NOT readAsDataURL — verify `grep readAsDataURL frontend/src/pages/Ingest.tsx` returns nothing)
    - `grep "diff-badge-create" frontend/src/pages/Ingest.tsx` and `grep "diff-badge-update" frontend/src/pages/Ingest.tsx` both match
    - `grep "navigator.clipboard.writeText" frontend/src/pages/Ingest.tsx` matches
    - `grep "Confirm Import" frontend/src/pages/Ingest.tsx` and `grep "Importing" frontend/src/pages/Ingest.tsx` match (disable-on-submit label swap)
    - `grep "ingest.confirming" frontend/src/pages/Ingest.tsx` matches (Confirm disabled while submitting)
    - `grep "Validation Errors" frontend/src/pages/Ingest.tsx` matches
    - `grep "Invalid JSON" frontend/src/pages/Ingest.tsx` matches (client-side parse guard)
    - The placeholder comment `// PLACEHOLDER` is gone from Ingest.tsx
    - `npm run build` clean
  </acceptance_criteria>
  <done>Ingest page does the full loop: copy prompt → paste/upload → preview diff → confirm (disabled-on-submit) → /goals, with 422 + JSON parse errors surfaced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual golden-path verification of linking + ingest</name>
  <action>Pause for the user to manually verify the drawer goal-linking and the full ingest loop in a browser, following the how-to-verify steps below. No code is written in this task.</action>
  <what-built>Goal dropdown in TaskDrawer and RoutineDrawer (active goals + No goal), and the Ingest page (LLM prompt + copy, paste textarea + .json upload, Run Preview with per-entity create/update diff, Confirm disabled-on-submit, field-level 422 errors).</what-built>
  <how-to-verify>
    With backend + frontend dev servers running, on a phone-width browser:
    1. Tasks page → open a task drawer → confirm a "Goal" dropdown lists active goals + "No goal". Link a task to a goal, save. Open the goal detail (Goals tab) → confirm the task appears under Linked Tasks and progress updates.
    2. Settings page → open a routine drawer → confirm a "Goal" dropdown is present. Link a routine, save, reopen → confirm the goal stays selected. Set it back to "No goal", save, reopen → confirm it unlinked (verifies explicit null).
    3. Goals page → tap "Import" → Ingest page. Click "Copy prompt" → confirm clipboard has the prompt and the button shows "Copied!".
    4. Paste a valid payload (e.g. the test_ingest.py `_payload` shape with schema_version "1.0") → Run Preview → confirm a grouped diff renders with create/update badges and NO data is written yet (check Goals list unchanged).
    5. Click Confirm Import → confirm button disables/shows "Importing…", then you land on /goals and the imported goal(s) appear.
    6. Upload a `.json` file with a payload → confirm its content loads into the textarea and preview works.
    7. Paste malformed JSON → confirm the inline "Invalid JSON" message. Paste valid-JSON-but-wrong-schema (e.g. schema_version "2.0") → Run Preview → confirm a "Validation Errors" field-level list renders.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend; npm run build` is clean.
- `uv run --project backend python -m pytest backend/tests/ -q` green (preview endpoint backing the page).
- Manual golden path verified: drawer linking (task + routine, incl. unlink) and the full ingest loop.
</verification>

<success_criteria>
- GOAL-05 satisfied: task and routine both link to goals via the shared dropdown; links reflect in goal detail.
- INGEST-05 satisfied: paste AND .json upload feed one payload pipeline.
- INGEST-03 satisfied: dry-run preview shows per-entity create/update diff before any write; Confirm commits and is disabled on submit.
</success_criteria>

<output>
After completion, create `.planning/phases/09-goals-ingest-ui/09-04-SUMMARY.md`.
</output>
