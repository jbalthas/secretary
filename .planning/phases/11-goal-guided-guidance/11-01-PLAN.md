---
phase: 11-goal-guided-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/versions/0010_guidance_columns.py
  - backend/app/models/__init__.py
  - backend/app/schemas/settings.py
  - backend/app/routers/settings.py
  - backend/app/routers/tasks.py
  - backend/tests/test_guidance.py
  - backend/tests/test_tasks.py
  - backend/tests/test_settings.py
autonomous: true
requirements: [GUIDE-03]
must_haves:
  truths:
    - "A task's completed_at is stamped the moment completed flips False->True"
    - "stall_threshold_days is readable and writable via the settings API, defaulting to 7"
    - "All Wave 0 guidance test stubs exist and fail (red) before downstream plans implement against them"
  artifacts:
    - path: "backend/migrations/versions/0010_guidance_columns.py"
      provides: "completed_at on tasks + stall_threshold_days/last_guidance_sent_date on app_settings"
      contains: "completed_at"
    - path: "backend/app/models/__init__.py"
      provides: "Task.completed_at + AppSettings.stall_threshold_days + AppSettings.last_guidance_sent_date columns"
      contains: "completed_at"
    - path: "backend/tests/test_guidance.py"
      provides: "Wave 0 failing test stubs for GUIDE-01/02/03"
      contains: "def test_stall_detection_basic"
  key_links:
    - from: "backend/app/routers/tasks.py"
      to: "Task.completed_at"
      via: "transition guard in update_task PATCH"
      pattern: "completed_at"
---

<objective>
Lay the schema + settings foundation for Phase 11 and create all Wave 0 failing test stubs.

This plan adds migration 0010 (`completed_at` on tasks; `stall_threshold_days` + `last_guidance_sent_date` on app_settings), the matching ORM columns, the settings read/write surface for `stall_threshold_days`, the `completed_at` stamping in the tasks PATCH handler, and the red test stubs that 11-02/11-03/11-04 implement against.

Purpose: Every downstream guidance behavior depends on these columns and on a stable test contract. Defining them first prevents a scavenger hunt and gives executors a red-to-green target.
Output: Migration 0010, extended Task/AppSettings models, settings schema+router for stall threshold, completed_at stamp, and test_guidance.py + additions to test_tasks.py/test_settings.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-goal-guided-guidance/11-CONTEXT.md
@.planning/phases/11-goal-guided-guidance/11-RESEARCH.md

<interfaces>
<!-- Current Task model (backend/app/models/__init__.py) — add completed_at -->
Task: id, title, description, priority(Priority enum high/medium/low),
  due_date(DateTime tz), reminder_at, recurrence_cron, completed(bool),
  created_at, updated_at, goal_id(FK), external_key, is_habit(bool),
  estimated_minutes, goal(relationship lazy=selectin)

<!-- Current AppSettings model — singleton id=1 — add two columns -->
AppSettings: id(=1), brief_hour(=8), brief_minute(=0), tts_enabled(=True),
  work_start_hour(=9), work_start_minute(=0), work_end_hour(=18), work_end_minute(=0)

<!-- Migration 0009 batch_alter_table pattern (THE pattern to copy for app_settings) -->
with op.batch_alter_table("app_settings") as batch_op:
    batch_op.add_column(sa.Column("work_start_hour", sa.Integer(), nullable=True))

<!-- Settings router get/put pattern (routers/settings.py): coalesce None -> default on read -->
<!-- Settings schema pattern (schemas/settings.py): WorkHoursRead/WorkHoursUpdate with model_config from_attributes -->
<!-- tasks.py PATCH update_task currently: for k,v in body.model_dump(exclude_unset=True): setattr(...) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0010 + ORM columns</name>
  <read_first>
    - backend/migrations/versions/0009_create_scheduled_blocks.py (batch_alter_table pattern, revision chaining)
    - backend/app/models/__init__.py (Task + AppSettings current columns)
    - backend/app/models/goal.py (Date column import style for last_guidance_sent_date)
  </read_first>
  <action>
Create `backend/migrations/versions/0010_guidance_columns.py` with `revision = '0010'`, `down_revision = '0009'`. In `upgrade()`:
- `with op.batch_alter_table("tasks") as batch_op: batch_op.add_column(sa.Column("completed_at", sa.DateTime(timezone=True), nullable=True))`
- `with op.batch_alter_table("app_settings") as batch_op:` add `sa.Column("stall_threshold_days", sa.Integer(), nullable=True)` and `sa.Column("last_guidance_sent_date", sa.Date(), nullable=True)`
In `downgrade()` drop the three columns in reverse (app_settings columns first, then tasks.completed_at), each inside its own `batch_alter_table`. Use `nullable=True` and NO `server_default` (D-09/D-11/D-14; avoids NOT NULL failure on existing app_settings id=1 row).

In `backend/app/models/__init__.py`:
- Add to `Task`: `completed_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)`
- Add to `AppSettings`: `stall_threshold_days: Mapped[int | None] = mapped_column(Integer, nullable=True)` and `last_guidance_sent_date: Mapped[date | None] = mapped_column(Date, nullable=True)`. Import `date` from `datetime` and `Date` from `sqlalchemy` at the top of the file (currently only `datetime, timezone` and no `Date` are imported).

Run `cd backend && alembic upgrade head` to confirm the migration applies cleanly.
  </action>
  <verify>
    <automated>cd backend && alembic upgrade head && python -c "from app.models import Task, AppSettings; assert hasattr(Task,'completed_at'); assert hasattr(AppSettings,'stall_threshold_days'); assert hasattr(AppSettings,'last_guidance_sent_date'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - 0010_guidance_columns.py contains `completed_at`
    - 0010_guidance_columns.py contains `stall_threshold_days`
    - 0010_guidance_columns.py contains `last_guidance_sent_date`
    - 0010_guidance_columns.py contains `down_revision` with value `'0009'`
    - models/__init__.py contains `completed_at: Mapped[datetime | None]`
    - models/__init__.py contains `stall_threshold_days: Mapped[int | None]`
    - models/__init__.py contains `last_guidance_sent_date: Mapped[date | None]`
    - `alembic upgrade head` exits 0
  </acceptance_criteria>
  <done>Migration 0010 applies; Task.completed_at, AppSettings.stall_threshold_days, AppSettings.last_guidance_sent_date all exist as nullable columns.</done>
</task>

<task type="auto">
  <name>Task 2: completed_at stamp + stall-threshold settings API</name>
  <read_first>
    - backend/app/routers/tasks.py (update_task PATCH handler)
    - backend/app/routers/settings.py (work-hours get/put coalesce pattern)
    - backend/app/schemas/settings.py (Read/Update schema pattern)
  </read_first>
  <action>
**tasks.py (D-09):** In `update_task`, capture pre-patch state before the setattr loop: `was_completed = task.completed`. After the `for k, v in body.model_dump(exclude_unset=True).items(): setattr(task, k, v)` loop, add:
```python
from datetime import datetime, timezone
if task.completed and not was_completed:
    task.completed_at = datetime.now(timezone.utc)
```
Place this BEFORE `await session.commit()`. Only stamp on the False->True transition — do not reset on idempotent PATCH where completed was already True.

**schemas/settings.py (D-11):** Add two Pydantic models mirroring the WorkHours pattern:
```python
class StallThresholdRead(BaseModel):
    stall_threshold_days: int
    model_config = {"from_attributes": True}

class StallThresholdUpdate(BaseModel):
    stall_threshold_days: int = Field(ge=1, le=365)
```

**routers/settings.py (D-11):** Import the two new schemas. Add:
```python
@router.get("/stall-threshold", response_model=StallThresholdRead)
async def get_stall_threshold(session: AsyncSession = Depends(get_session)):
    cfg = await session.get(AppSettings, 1)
    days = cfg.stall_threshold_days if cfg and cfg.stall_threshold_days is not None else 7
    return StallThresholdRead(stall_threshold_days=days)

@router.put("/stall-threshold", response_model=StallThresholdRead)
async def set_stall_threshold(body: StallThresholdUpdate, session: AsyncSession = Depends(get_session)):
    cfg = await session.get(AppSettings, 1)
    if cfg is None:
        cfg = AppSettings(id=1, stall_threshold_days=body.stall_threshold_days)
        session.add(cfg)
    else:
        cfg.stall_threshold_days = body.stall_threshold_days
    await session.commit()
    return StallThresholdRead(stall_threshold_days=body.stall_threshold_days)
```
Coalesce None->7 on read (Phase 10 work-hours precedent). No scheduler call needed (the stall job reads the value at fire time).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_tasks.py::test_completed_at_stamped tests/test_settings.py::test_stall_threshold_roundtrip -x</automated>
  </verify>
  <acceptance_criteria>
    - tasks.py contains `was_completed = task.completed`
    - tasks.py contains `task.completed_at = datetime.now(timezone.utc)`
    - schemas/settings.py contains `class StallThresholdRead`
    - schemas/settings.py contains `class StallThresholdUpdate`
    - settings.py contains `"/stall-threshold"` (both get and put routes)
    - settings.py coalesces missing threshold to `7`
  </acceptance_criteria>
  <done>PATCH /tasks/{id} stamps completed_at on False->True; GET/PUT /settings/stall-threshold round-trips, defaulting to 7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 failing test stubs</name>
  <read_first>
    - backend/tests/test_brief.py (patch _Session + deferred import test patterns)
    - backend/tests/test_tasks.py (TestClient task-creation fixtures)
    - backend/tests/test_settings.py (settings round-trip pattern)
  </read_first>
  <behavior>
    - test_brief_body_includes_goal_snapshot: build_brief_body() output contains an active goal title and its progress "%" line (GUIDE-01)
    - test_brief_speech_goal_summary: build_brief_speech() mentions active goal count and at most 2-3 goal titles, not a full enumeration (D-04)
    - test_next_best_task_scoring: GET /guidance/next-best-task returns the highest-scoring pending non-habit task per D-06 formula (GUIDE-02)
    - test_next_best_task_empty: returns null/None when no pending tasks exist (D-08)
    - test_stall_detection_basic: a goal with a linked task whose completed_at is older than threshold (or null) is flagged stalled (GUIDE-03)
    - test_stall_no_tasks_not_stalled: a goal with zero linked tasks is NOT stalled (D-10)
    - test_stall_rate_limit: send_stall_nudge skips when last_guidance_sent_date == today (D-14)
    - test_completed_at_stamped (in test_tasks.py): PATCH completed=True stamps completed_at; idempotent re-PATCH does not change it (D-09)
    - test_stall_threshold_roundtrip (in test_settings.py): PUT then GET /settings/stall-threshold returns the saved value (D-11)
  </behavior>
  <action>
Create `backend/tests/test_guidance.py` with the seven guidance tests named in `<behavior>` (test_brief_body_includes_goal_snapshot, test_brief_speech_goal_summary, test_next_best_task_scoring, test_next_best_task_empty, test_stall_detection_basic, test_stall_no_tasks_not_stalled, test_stall_rate_limit). Mirror test_brief.py: use sync TestClient where hitting routes, and for service-level brief/guidance tests patch the module `_Session` to the test engine sessionmaker and use deferred imports inside the test body (so the file is collectable before guidance_service.py exists). Import guidance_service / build_brief_body inside each test function body, NOT at module top, so collection does not fail before 11-02/11-03 land.

Add `test_completed_at_stamped` to `backend/tests/test_tasks.py`: create a task, PATCH `{"completed": true}`, assert returned `completed_at` is not null; capture it, PATCH `{"completed": true}` again, assert `completed_at` unchanged.

Add `test_stall_threshold_roundtrip` to `backend/tests/test_settings.py`: PUT `/settings/stall-threshold` `{"stall_threshold_days": 14}`, then GET, assert 14.

These tests MUST fail/error now for guidance-service behaviors (endpoint + service not yet implemented) — that is the intended red state for Wave 0. test_completed_at_stamped and test_stall_threshold_roundtrip should already PASS after Task 1+2 of this plan.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_guidance.py --collect-only -q && pytest tests/test_tasks.py::test_completed_at_stamped tests/test_settings.py::test_stall_threshold_roundtrip -x</automated>
  </verify>
  <acceptance_criteria>
    - test_guidance.py contains `def test_brief_body_includes_goal_snapshot`
    - test_guidance.py contains `def test_next_best_task_scoring`
    - test_guidance.py contains `def test_stall_detection_basic`
    - test_guidance.py contains `def test_stall_no_tasks_not_stalled`
    - test_guidance.py contains `def test_stall_rate_limit`
    - test_tasks.py contains `def test_completed_at_stamped`
    - test_settings.py contains `def test_stall_threshold_roundtrip`
    - `pytest tests/test_guidance.py --collect-only` exits 0 (collectable, deferred imports)
  </acceptance_criteria>
  <done>test_guidance.py collects without import errors; the two roundtrip/stamp tests pass; the five guidance-behavior tests are present and red.</done>
</task>

</tasks>

<verification>
- `alembic upgrade head` applies migration 0010 cleanly on an existing DB.
- `pytest tests/test_guidance.py --collect-only` succeeds (no top-level import of not-yet-existing modules).
- `pytest tests/test_tasks.py::test_completed_at_stamped tests/test_settings.py::test_stall_threshold_roundtrip` passes.
</verification>

<success_criteria>
- Task.completed_at, AppSettings.stall_threshold_days, AppSettings.last_guidance_sent_date columns exist (nullable, no server_default).
- completed_at stamped only on False->True transition in PATCH /tasks/{id}.
- GET/PUT /settings/stall-threshold round-trips with default 7.
- All Wave 0 test stubs exist; guidance-behavior tests red, foundation tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-goal-guided-guidance/11-01-SUMMARY.md`
</output>
