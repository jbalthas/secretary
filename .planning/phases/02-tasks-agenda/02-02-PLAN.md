---
phase: 02-tasks-agenda
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/app/models/__init__.py
  - backend/app/schemas/task.py
  - backend/app/routers/tasks.py
  - backend/app/main.py
  - backend/migrations/versions/0002_add_tasks_table.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06, TASK-07]
must_haves:
  truths:
    - "User can create a task with title, description, due date, priority via POST"
    - "User can edit any field via PATCH without resetting unset fields"
    - "User can mark complete via PATCH completed=true"
    - "User can delete a task via DELETE"
    - "reminder_at and recurrence_cron persist to the DB"
    - "GET returns all tasks for client-side filter/sort"
  artifacts:
    - path: "backend/app/models/__init__.py"
      provides: "Task SQLAlchemy model + Priority enum"
      contains: "class Task"
    - path: "backend/app/routers/tasks.py"
      provides: "CRUD router for /tasks"
      exports: ["router"]
    - path: "backend/app/schemas/task.py"
      provides: "TaskCreate/TaskUpdate/TaskRead Pydantic schemas"
      contains: "class TaskRead"
    - path: "backend/migrations/versions/0002_add_tasks_table.py"
      provides: "tasks table DDL"
      contains: "create_table"
  key_links:
    - from: "backend/app/main.py"
      to: "tasks.router"
      via: "include_router"
      pattern: "include_router"
    - from: "backend/app/routers/tasks.py"
      to: "Task model"
      via: "session.get / select(Task)"
      pattern: "select\\(Task\\)"
---

<objective>
Build the backend Task feature: SQLAlchemy model + Alembic migration + Pydantic schemas + CRUD router mounted on the app. Satisfies the failing tests created in 02-01.

Purpose: One real DB table and a complete CRUD API — the foundation every frontend view consumes.
Output: tasks table, /api/v1/tasks CRUD endpoints, all 7 tests in test_tasks.py green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tasks-agenda/02-RESEARCH.md
@backend/app/db.py
@backend/app/config.py
@backend/app/main.py
@backend/tests/test_tasks.py

<interfaces>
From backend/app/db.py:
```python
class Base(DeclarativeBase): pass
async def get_session() -> AsyncSession  # FastAPI dependency
SessionLocal = async_sessionmaker(..., expire_on_commit=False)
```
From backend/app/config.py:
```python
settings.api_prefix  # "/api/v1"
settings.database_url # "sqlite+aiosqlite:///./secretary.db"
```
Alembic env.py already does `from app.db import Base` and strips +aiosqlite for the sync URL. Existing migration is 0001 (no-op init).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Task model, schemas, and Alembic migration</name>
  <read_first>
    - backend/app/db.py (Base, get_session, session factory)
    - backend/app/models/__init__.py (currently empty — model goes here)
    - backend/migrations/env.py (imports Base; autogenerate context)
    - backend/migrations/versions/ (find the 0001 init revision id for down_revision)
    - .planning/phases/02-tasks-agenda/02-RESEARCH.md (Pattern 1, Pattern 3, migration example, pitfalls 1/2/5)
  </read_first>
  <files>backend/app/models/__init__.py, backend/app/schemas/task.py, backend/migrations/versions/0002_add_tasks_table.py</files>
  <behavior>
    - A Task row persists all fields: title, description, priority, due_date, reminder_at, recurrence_cron, completed, created_at, updated_at
    - priority defaults to "medium"; completed defaults to False
    - created_at/updated_at auto-populate; no datetime.utcnow deprecation warnings
  </behavior>
  <action>
    Create the `Task` model in `backend/app/models/__init__.py`. Use SQLAlchemy 2.0 typed style (`Mapped` / `mapped_column`). Import `Base` from `app.db`. Define a `Priority(str, enum.Enum)` with members `high="high"`, `medium="medium"`, `low="low"`. Columns:
    - `id` Mapped[int], primary_key
    - `title` Mapped[str], String(255), nullable=False
    - `description` Mapped[str|None], Text, nullable=True
    - `priority` Mapped[Priority], SAEnum(Priority), default=Priority.medium
    - `due_date` Mapped[datetime|None], DateTime(timezone=True), nullable=True
    - `reminder_at` Mapped[datetime|None], DateTime(timezone=True), nullable=True
    - `recurrence_cron` Mapped[str|None], String(100), nullable=True
    - `completed` Mapped[bool], Boolean, default=False
    - `created_at` Mapped[datetime], DateTime(timezone=True), default=`lambda: datetime.now(timezone.utc)`
    - `updated_at` Mapped[datetime], DateTime(timezone=True), default=`lambda: datetime.now(timezone.utc)`, onupdate=`lambda: datetime.now(timezone.utc)`
    Do NOT use `datetime.utcnow` (deprecated in 3.12 — pitfall 5).

    Create `backend/app/schemas/task.py` with Pydantic v2 schemas (import `Priority` from `app.models`):
    - `TaskCreate`: title:str, description:str|None=None, priority:Priority=Priority.medium, due_date:datetime|None=None, reminder_at:datetime|None=None, recurrence_cron:str|None=None
    - `TaskUpdate(TaskCreate)`: override title:str|None=None, add completed:bool|None=None
    - `TaskRead(TaskCreate)`: add id:int, completed:bool, created_at:datetime, updated_at:datetime, and `model_config = {"from_attributes": True}`
    Add an empty `backend/app/schemas/__init__.py` if the package dir doesn't exist.

    Create migration `backend/migrations/versions/0002_add_tasks_table.py`. Set `down_revision` to the existing 0001 init revision id (read it from the versions dir). `revision = "0002"`. In `upgrade()` call `op.create_table('tasks', ...)` with columns matching the model: id Integer primary_key, title String(255) not null, description Text null, priority String(10) not null server_default='medium', due_date DateTime(timezone=True) null, reminder_at DateTime(timezone=True) null, recurrence_cron String(100) null, completed Boolean not null server_default='0', created_at DateTime(timezone=True) not null, updated_at DateTime(timezone=True) not null. In `downgrade()` call `op.drop_table('tasks')`. Import `sqlalchemy as sa` and `from alembic import op`.
  </action>
  <verify>
    <automated>cd backend && uv run alembic upgrade head && uv run python -c "from app.models import Task, Priority; print(Task.__tablename__, Priority.high.value)"</automated>
  </verify>
  <acceptance_criteria>
    - models/__init__.py contains `class Task`
    - models/__init__.py contains `class Priority`
    - models/__init__.py does NOT contain `datetime.utcnow`
    - schemas/task.py contains `class TaskRead`
    - schemas/task.py contains `from_attributes`
    - 0002_add_tasks_table.py contains `create_table`
    - `alembic upgrade head` exits 0 and prints `tasks high`
  </acceptance_criteria>
  <done>Model + schemas defined, migration applied, tasks table exists in the DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRUD router and mount on app</name>
  <read_first>
    - backend/app/main.py (mount point — bare app, no routers yet)
    - backend/app/db.py (get_session dependency)
    - backend/app/models/__init__.py (Task model from Task 1)
    - backend/app/schemas/task.py (schemas from Task 1)
    - backend/tests/test_tasks.py (the 7 tests this must satisfy)
    - .planning/phases/02-tasks-agenda/02-RESEARCH.md (Pattern 2, pitfalls 4/6)
  </read_first>
  <files>backend/app/routers/tasks.py, backend/app/main.py</files>
  <behavior>
    - POST /api/v1/tasks/ returns 201 with created task
    - PATCH updates only provided fields (exclude_unset), 404 if missing
    - DELETE returns 204, 404 if missing
    - GET / returns list of all tasks
  </behavior>
  <action>
    Create `backend/app/routers/tasks.py`. Add empty `backend/app/routers/__init__.py` if needed. Build an `APIRouter(prefix=f"{settings.api_prefix}/tasks", tags=["tasks"])` named `router` with:
    - `GET "/"` response_model=list[TaskRead]: `await session.execute(select(Task).order_by(Task.created_at.desc()))` then `result.scalars().all()`.
    - `POST "/"` response_model=TaskRead, status_code=201: `Task(**body.model_dump())`, add, commit, refresh, return.
    - `PATCH "/{task_id}"` response_model=TaskRead: `await session.get(Task, task_id)`; if None raise `HTTPException(404)`; loop `body.model_dump(exclude_unset=True).items()` and `setattr`; commit, refresh, return. (exclude_unset is critical — pitfall 4.)
    - `DELETE "/{task_id}"` status_code=204: get, 404 if None, `await session.delete(task)`, commit, return None.
    All handlers `async def`, session via `Depends(get_session)`. Import `select` from sqlalchemy, `AsyncSession` from sqlalchemy.ext.asyncio, schemas from `app.schemas.task`, `Task` from `app.models`, `settings` from `app.config`.

    In `backend/app/main.py`, import the tasks router and add `app.include_router(tasks.router)` after the app is created. Keep the existing health endpoint.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - routers/tasks.py contains `select(Task)`
    - routers/tasks.py contains `exclude_unset=True`
    - routers/tasks.py contains `status_code=201`
    - routers/tasks.py contains `HTTPException(404)`
    - main.py contains `include_router`
    - `uv run pytest tests/ -x -q` passes all tests including the 7 in test_tasks.py
  </acceptance_criteria>
  <done>All backend tests green; /api/v1/tasks supports full CRUD.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run alembic upgrade head` exits 0
- `cd backend && uv run pytest tests/ -v` — all tests pass (health + 7 task tests)
</verification>

<success_criteria>
TASK-01..07 backend behavior implemented and proven by green tests. tasks table migrated. Router mounted.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-agenda/02-02-SUMMARY.md`
</output>
