---
phase: 02-tasks-agenda
plan: 05
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - frontend/src/components/AgendaItem.tsx
  - frontend/src/lib/agenda.ts
  - frontend/src/lib/agenda.test.ts
  - frontend/src/pages/Today.tsx
  - frontend/vite.config.ts
  - frontend/package.json
autonomous: false
requirements: [CAL-05]
must_haves:
  truths:
    - "Today view merges today's tasks with hardcoded placeholder events sorted by time"
    - "All-day tasks appear at the top of the agenda"
    - "Calendar events render distinctly from tasks (italic, no priority badge)"
    - "buildAgenda merge logic is unit tested"
  artifacts:
    - path: "frontend/src/lib/agenda.ts"
      provides: "buildAgenda merge + sort logic + placeholder events"
      contains: "PLACEHOLDER_EVENTS"
    - path: "frontend/src/lib/agenda.test.ts"
      provides: "vitest coverage of merge/sort/all-day"
      contains: "buildAgenda"
    - path: "frontend/src/components/AgendaItem.tsx"
      provides: "Agenda row (task or event)"
      contains: "isEvent"
    - path: "frontend/src/pages/Today.tsx"
      provides: "Today agenda page"
      contains: "buildAgenda"
  key_links:
    - from: "frontend/src/pages/Today.tsx"
      to: "useTasks + buildAgenda"
      via: "merge tasks into agenda"
      pattern: "buildAgenda"
---

<objective>
Build the Today agenda view (CAL-05): merge today's tasks (with due dates) and hardcoded placeholder calendar events into a single chronological timeline, all-day items pinned to top, events visually distinct from tasks. Extract the merge logic into a unit-tested module and add vitest to the frontend.

Purpose: The default landing view — proves the merged-agenda concept that real calendar sync (Phase 4) will plug into.
Output: Today page rendering a sorted merged agenda, with tested buildAgenda logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tasks-agenda/02-UI-SPEC.md
@.planning/phases/02-tasks-agenda/02-RESEARCH.md
@.planning/phases/02-tasks-agenda/02-CONTEXT.md
@frontend/src/types/task.ts
@frontend/src/hooks/useTasks.ts
@frontend/src/pages/Today.tsx

<interfaces>
From frontend/src/types/task.ts (02-01):
```ts
export interface AgendaItem { id:string; title:string; time:string|null; priority:Priority|null; isEvent:boolean }
export interface Task { ... due_date: string|null; completed: boolean; ... }
```
From frontend/src/hooks/useTasks.ts (02-03): `useTasks(): { tasks: Task[], ... }`.
RESEARCH.md Pattern 6 shows the buildAgenda skeleton + PLACEHOLDER_EVENTS.
CONTEXT.md D-07 (chronological merged list), D-08 (all-day at top), D-09 (title+priority+time), D-10 (hardcoded events).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add vitest + buildAgenda merge logic (tested)</name>
  <read_first>
    - frontend/package.json (deps; no vitest yet)
    - frontend/vite.config.ts (add test config)
    - frontend/src/types/task.ts (Task, AgendaItem, Priority)
    - .planning/phases/02-tasks-agenda/02-RESEARCH.md (Pattern 6 buildAgenda, pitfall 2 datetimes as strings)
    - .planning/phases/02-tasks-agenda/02-CONTEXT.md (D-07, D-08, D-10)
  </read_first>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/src/lib/agenda.ts, frontend/src/lib/agenda.test.ts</files>
  <behavior>
    - buildAgenda returns all-day tasks first, then timed items sorted by time ascending
    - placeholder events (Team standup 09:00, Lunch 12:00) interleave with timed tasks by time
    - completed tasks and tasks not due today are excluded
    - a task due "2026-06-12T10:30:00Z" sorts between standup (09:00) and lunch (12:00)
  </behavior>
  <action>
    Run `cd frontend && npm install -D vitest jsdom` (adds to devDependencies). In `frontend/vite.config.ts` add a `test` block: `test: { environment: "jsdom" }` (keep existing plugins/build/server). Add `"test": "vitest"` to package.json scripts.

    Create `frontend/src/lib/agenda.ts`. Export `PLACEHOLDER_EVENTS: AgendaItem[]` = `[{id:"evt-1",title:"Team standup",time:"09:00",priority:null,isEvent:true},{id:"evt-2",title:"Lunch",time:"12:00",priority:null,isEvent:true}]` (D-10). Export `buildAgenda(tasks: Task[], now: Date = new Date()): AgendaItem[]`:
    - `today = now.toISOString().slice(0,10)`
    - filter tasks: `!t.completed && t.due_date && t.due_date.slice(0,10) === today`
    - a task is all-day if its due_date time component is `T00:00:00` (no meaningful time); otherwise timed. Derive `time` for timed tasks as the `HH:mm` from due_date.
    - map tasks to AgendaItem: `{id:"task-"+t.id, title:t.title, time, priority:t.priority, isEvent:false}`
    - `timed = [...timedTaskItems, ...PLACEHOLDER_EVENTS].sort((a,b) => (a.time!).localeCompare(b.time!))`
    - return `[...allDayItems, ...timed]` (all-day first per D-08)
    Accept an injectable `now` so tests are deterministic (pitfall 2 — work in UTC strings, no lexical comparison across tz).

    Create `frontend/src/lib/agenda.test.ts` with vitest covering the behavior block: a fixed `now`, a timed task at 10:30 sorting between standup and lunch, an all-day task appearing first, a completed task excluded, a non-today task excluded.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains `vitest`
    - package.json scripts contains `"test"`
    - vite.config.ts contains `jsdom`
    - agenda.ts contains `PLACEHOLDER_EVENTS`
    - agenda.ts contains `export function buildAgenda`
    - agenda.test.ts contains `buildAgenda`
    - `npm test -- --run` exits 0 with passing agenda tests
  </acceptance_criteria>
  <done>buildAgenda merge/sort/all-day logic implemented and green under vitest.</done>
</task>

<task type="auto">
  <name>Task 2: AgendaItem component + Today page</name>
  <read_first>
    - frontend/src/lib/agenda.ts (buildAgenda, AgendaItem shape from Task 1)
    - frontend/src/hooks/useTasks.ts (useTasks)
    - frontend/src/types/task.ts (AgendaItem)
    - frontend/src/pages/Today.tsx (stub to replace)
    - .planning/phases/02-tasks-agenda/02-UI-SPEC.md (Agenda Item Row, Empty States, Copywriting agenda strings)
    - .planning/phases/02-tasks-agenda/02-CONTEXT.md (D-09)
  </read_first>
  <files>frontend/src/components/AgendaItem.tsx, frontend/src/pages/Today.tsx</files>
  <action>
    Create `frontend/src/components/AgendaItem.tsx`. Props `{ item: AgendaItem }`. Same compact row style as TaskRow. Per UI-SPEC Agenda Item Row + D-09: task items show priority badge (`priority-{priority}` class) + time if set; event items (`item.isEvent`) render italic title, NO priority badge, show time formatted 12h (e.g. "9:00 AM"). All-day items (`item.time === null`) show no time. Display time via `Intl.DateTimeFormat` 12h where present.

    Replace `frontend/src/pages/Today.tsx` stub with the agenda page. Use `useTasks()`. Page title "Today" (20px/600, D-12 is the default route already). Compute `const agenda = buildAgenda(tasks)`. Split into all-day group (items with `time === null`) and timed group. If any all-day items exist, render a section header "All day" (14px slate-400) above that block (UI-SPEC). Render each item via `<AgendaItem>`. Empty state when agenda is empty (Copywriting): heading "Nothing scheduled today", body "Add a task with a due time or check back when events sync." Add `padding-bottom` via the `.page` class so the bottom nav doesn't overlap.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - AgendaItem.tsx contains `isEvent`
    - AgendaItem.tsx renders italic for events (contains `italic` or `fontStyle`)
    - Today.tsx contains `buildAgenda`
    - Today.tsx contains `useTasks`
    - Today.tsx contains `All day`
    - Today.tsx contains `Nothing scheduled today`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Today page renders a merged, sorted agenda with all-day section, distinct event styling, and empty state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify merged agenda on phone browser</name>
  <what-built>Today agenda view merging today's tasks with placeholder events (Team standup 9am, Lunch 12pm), all-day tasks pinned to top, events styled distinctly.</what-built>
  <how-to-verify>
    1. Run backend + frontend; open the app on a phone browser (default lands on Today, D-12).
    2. With no tasks due today, confirm placeholder events "Team standup" (9:00 AM) and "Lunch" (12:00 PM) show in time order.
    3. On the Tasks tab create a task due today at 10:30 with High priority; return to Today.
    4. Confirm the new task appears between Team standup and Lunch, with its priority badge and time.
    5. Create a task due today with no time (all-day); confirm it appears under an "All day" header at the top, above the timed items.
    6. Confirm events render visually distinct from tasks (italic, no priority badge).
  </how-to-verify>
  <action>Pause for the user to manually verify the merged Today agenda on a phone browser per the steps in how-to-verify. Do not proceed until approved.</action>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run` passes agenda tests
- `cd frontend && npm run build` exits 0
- Human checkpoint above passes (CAL-05 merged agenda on phone browser)
</verification>

<success_criteria>
CAL-05 satisfied: Today's agenda merges tasks with due times and hardcoded placeholder events into one chronological list, all-day tasks at top, events distinct from tasks. Merge logic unit tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-agenda/02-05-SUMMARY.md`
</output>
