---
phase: 02-tasks-agenda
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - frontend/package.json
  - frontend/src/App.tsx
  - frontend/src/components/BottomNav.tsx
  - frontend/src/hooks/useTasks.ts
  - frontend/src/pages/Today.tsx
  - frontend/src/pages/Tasks.tsx
  - frontend/src/styles.css
  - frontend/src/main.tsx
autonomous: true
requirements: [TASK-07, CAL-05]
must_haves:
  truths:
    - "App opens to Today's agenda by default"
    - "Bottom nav switches between Today and Tasks tabs"
    - "useTasks hook fetches and mutates tasks via /api/v1/tasks"
    - "Dark theme global styles applied per UI-SPEC"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Router + BottomNav shell, default redirect to /today"
      contains: "BrowserRouter"
    - path: "frontend/src/components/BottomNav.tsx"
      provides: "Two-tab bottom nav"
      contains: "NavLink"
    - path: "frontend/src/hooks/useTasks.ts"
      provides: "fetch/create/patch/delete task operations"
      exports: ["useTasks"]
    - path: "frontend/src/styles.css"
      provides: "Dark theme tokens + base layout"
      contains: "#0f172a"
  key_links:
    - from: "frontend/src/hooks/useTasks.ts"
      to: "/api/v1/tasks"
      via: "fetch"
      pattern: "/api/v1/tasks"
    - from: "frontend/src/App.tsx"
      to: "BottomNav"
      via: "component render"
      pattern: "BottomNav"
---

<objective>
Build the React SPA shell: install react-router-dom + lucide-react, replace the stub App.tsx with a router + bottom nav, create the useTasks data hook, stub the two pages, and lay down the dark-theme global stylesheet per the UI design contract.

Purpose: The app shell every feature view plugs into — navigation, data access, and the visual system.
Output: working two-tab navigation (Today/Tasks), default Today view, useTasks hook, themed styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-tasks-agenda/02-UI-SPEC.md
@.planning/phases/02-tasks-agenda/02-RESEARCH.md
@frontend/src/App.tsx
@frontend/src/main.tsx
@frontend/src/types/task.ts

<interfaces>
From frontend/src/types/task.ts (created in 02-01):
```ts
export type Priority = "high" | "medium" | "low";
export interface Task { id, title, description, priority, due_date, reminder_at, recurrence_cron, completed, created_at, updated_at }
export interface TaskCreate { title; description?; priority?; due_date?; reminder_at?; recurrence_cron? }
export interface AgendaItem { id, title, time, priority, isEvent }
```
React Router v7: use `useNavigate` (NOT useHistory — removed), `NavLink` for active state, `BrowserRouter`, `Routes`, `Route`, `Navigate`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, global theme styles, useTasks hook</name>
  <read_first>
    - frontend/package.json (current deps — no router/icons yet)
    - frontend/src/main.tsx (entry point — must import styles.css)
    - frontend/src/types/task.ts (Task, TaskCreate contracts)
    - .planning/phases/02-tasks-agenda/02-UI-SPEC.md (Color, Spacing, Typography sections — copy exact hex values)
    - .planning/phases/02-tasks-agenda/02-RESEARCH.md (useTasks hook pattern, pitfall 6 refetch)
  </read_first>
  <files>frontend/package.json, frontend/src/styles.css, frontend/src/hooks/useTasks.ts, frontend/src/main.tsx</files>
  <action>
    Run `cd frontend && npm install react-router-dom lucide-react` (adds them to package.json dependencies).

    Create `frontend/src/styles.css` implementing the UI-SPEC color/spacing/typography contract. Define `:root` CSS variables with EXACT values from UI-SPEC:
    `--bg:#0f172a; --surface:#1e293b; --accent:#6366f1; --destructive:#ef4444; --text:#f1f5f9; --text-secondary:#94a3b8; --text-disabled:#475569; --border:#334155;`
    Set `body { margin:0; background:var(--bg); color:var(--text); font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif; font-size:16px; line-height:1.5; }`.
    Add base classes the components will use: `.page` (padding 32px 16px top, padding-bottom 56px for nav clearance), `.page-title` (font-size 20px, font-weight 600), `.priority-badge` base (pill, 4px 8px padding, font-size 12px), and priority variants `.priority-high { background: rgba(239,68,68,0.15); color:#ef4444; }`, `.priority-medium { background: rgba(245,158,11,0.15); color:#f59e0b; }`, `.priority-low { background: rgba(100,116,139,0.15); color:#94a3b8; }`. Add `*:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }`.

    In `frontend/src/main.tsx`, add `import "./styles.css";` at the top.

    Create `frontend/src/hooks/useTasks.ts` following the research pattern. Export `useTasks()` returning `{ tasks, refresh, createTask, patchTask, deleteTask }`. Base URL constant `const API = "/api/v1/tasks";`. `refresh()` does `fetch(API + "/")` then `setTasks(await res.json())`. `createTask(body: TaskCreate)` POSTs JSON then `await refresh()`. `patchTask(id:number, body: Partial<Task>)` PATCHes `${API}/${id}` then refresh. `deleteTask(id:number)` DELETEs `${API}/${id}` then refresh. `useEffect(() => { refresh() }, [])`. Type `tasks` as `Task[]`. After every mutation re-fetch (pitfall 6).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && node -e "const p=require('./package.json'); if(!p.dependencies['react-router-dom']||!p.dependencies['lucide-react'])process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains `react-router-dom`
    - package.json dependencies contains `lucide-react`
    - styles.css contains `#0f172a`
    - styles.css contains `#6366f1`
    - styles.css contains `.priority-high`
    - main.tsx contains `import "./styles.css"`
    - useTasks.ts contains `export function useTasks`
    - useTasks.ts contains `/api/v1/tasks`
    - useTasks.ts contains `createTask`, `patchTask`, `deleteTask`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Deps installed, theme tokens defined, useTasks hook typed and compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Router shell, BottomNav, page stubs</name>
  <read_first>
    - frontend/src/App.tsx (stub to replace)
    - frontend/src/types/task.ts (types)
    - .planning/phases/02-tasks-agenda/02-UI-SPEC.md (Bottom Navigation Bar, Copywriting page titles)
    - .planning/phases/02-tasks-agenda/02-RESEARCH.md (Pattern 4 SPA shell, pitfall 3 router v7)
  </read_first>
  <files>frontend/src/App.tsx, frontend/src/components/BottomNav.tsx, frontend/src/pages/Today.tsx, frontend/src/pages/Tasks.tsx</files>
  <action>
    Replace `frontend/src/App.tsx` with the router shell (research Pattern 4): `BrowserRouter` wrapping a content div with `paddingBottom:56px`, `Routes` with `path="/"` → `<Navigate to="/today" replace />` (default to Today per D-12), `path="/today"` → `<Today/>`, `path="/tasks"` → `<Tasks/>`. Render `<BottomNav/>` after the routes. Import from `react-router-dom`.

    Create `frontend/src/components/BottomNav.tsx` per UI-SPEC Bottom Navigation Bar: fixed at viewport bottom, height 56px, background var(--surface), 1px top border var(--border). Two `NavLink`s — `to="/today"` labeled "Today" with lucide `Calendar` icon, `to="/tasks"` labeled "Tasks" with lucide `ListTodo` icon. Use NavLink's `className`/`style` callback with `isActive`: active = accent (#6366f1) icon+label, inactive = slate-400 (#94a3b8). Each tab min 44px touch target, label centered under icon (label 14px). Add `role="navigation"`; rely on NavLink's automatic `aria-current="page"`.

    Create `frontend/src/pages/Today.tsx` and `frontend/src/pages/Tasks.tsx` as minimal stubs for now: each a default-exported component rendering `<div className="page"><h1 className="page-title">Today</h1></div>` (and "Tasks" respectively). These are filled in by 02-04 and 02-05.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `BrowserRouter`
    - App.tsx contains `Navigate to="/today"` (default redirect)
    - App.tsx contains `BottomNav`
    - BottomNav.tsx contains `NavLink`
    - BottomNav.tsx contains `to="/today"` and `to="/tasks"`
    - BottomNav.tsx does NOT contain `useHistory`
    - pages/Today.tsx and pages/Tasks.tsx export a default component
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Two-tab nav works, app defaults to /today, build succeeds.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0
- Manual: `npm run dev`, open app, confirm it opens on Today and bottom nav switches tabs
</verification>

<success_criteria>
SPA shell with router, themed dark UI, two-tab bottom nav defaulting to Today, and a working useTasks data hook. Pages stubbed for 02-04/02-05 to fill.
</success_criteria>

<output>
After completion, create `.planning/phases/02-tasks-agenda/02-03-SUMMARY.md`
</output>
