---
phase: 08-goals-ingest-backend
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - backend/app/schemas/ingest.py
  - backend/app/services/ingest_service.py
  - backend/app/routers/ingest.py
  - backend/app/main.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-04, INGEST-06, INGEST-07]
must_haves:
  truths:
    - "GET /ingest/schema returns the versioned JSON schema generated from the Pydantic model"
    - "Mismatched schema_version, extra fields, and invalid cron return HTTP 422 field-level"
    - "POST /ingest/confirm writes goals->tasks->routines->habits in ONE transaction"
    - "Injecting a failure mid-commit leaves zero new rows"
    - "Re-posting the same payload is idempotent (matched on external_key); user-edited completed is preserved"
    - "habits array creates Task rows with is_habit=True + recurrence_cron, optionally linked to a goal"
  artifacts:
    - path: "backend/app/schemas/ingest.py"
      provides: "IngestPayload + per-entity import models (extra=forbid, Literal schema_version, cron validator)"
      contains: "Literal"
    - path: "backend/app/services/ingest_service.py"
      provides: "apply_import transactional upsert with flush-then-resolve; _upsert_task injection point"
      contains: "session.begin()"
    - path: "backend/app/routers/ingest.py"
      provides: "GET /schema, POST /confirm"
      exports: ["router"]
  key_links:
    - from: "backend/app/services/ingest_service.py"
      to: "session"
      via: "single async with session.begin() wrapping all upserts + flush after goals"
      pattern: "session.flush"
    - from: "backend/app/routers/ingest.py"
      to: "IngestPayload.model_json_schema"
      via: "GET /schema"
      pattern: "model_json_schema"
    - from: "backend/app/main.py"
      to: "app.routers.ingest"
      via: "include_router"
      pattern: "ingest.router"
---

<objective>
Build the versioned, validating, idempotent, transactional ingest endpoint: Pydantic import models (schema_version Literal, extra="forbid", cron validation), the ingest service with flush-then-resolve atomicity (D-09), and the router (GET /schema + POST /confirm). This turns the INGEST-* Wave 0 tests green and is success criteria #4 and #5 of the phase.

Honors: D-07 match on external_key (not title), D-08 preserve user-edited completed/reminder_at/enabled, D-09 commit order goals->tasks->routines->habits in one transaction with zero-row guarantee on mid-commit failure, D-04/D-05 habits map to Task with is_habit=True. PITFALLS: extra="forbid" (anti-pattern 3), single transaction (anti-pattern 4), select-then-update not INSERT OR REPLACE / merge (RESEARCH Pattern 2), cron validation (pitfall 15).

Purpose: The ingest contract is the import surface the whole v2.0 LLM workflow depends on.
Output: schemas/ingest.py, services/ingest_service.py, routers/ingest.py, main.py registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-goals-ingest-backend/08-CONTEXT.md
@.planning/phases/08-goals-ingest-backend/08-RESEARCH.md
@.planning/phases/08-goals-ingest-backend/08-02-SUMMARY.md
@.planning/phases/08-goals-ingest-backend/08-03-SUMMARY.md

<interfaces>
<!-- From Plan 02/03 — use directly. -->
Models: app.models.Task (goal_id, external_key, is_habit, recurrence_cron, completed, reminder_at, priority, title, description, due_date); app.models.Routine (goal_id, external_key, name, cron, action, enabled); app.models.goal.Goal (external_key, title, type:GoalType, description, target_date, status), Milestone (goal_id, title, target_date, done).
Enums: app.models.Priority (high|medium|low); app.models.goal.GoalType (career|life|health|learning|financial); app.models.RoutineAction (send_daily_brief).

Patterns:
- Cron validation: backend/app/schemas/routine.py defines _validate_cron(value) via CronTrigger.from_crontab in try/except raising ValueError. Reuse the same approach in a field_validator on recurrence_cron/cron.
- Router prefix: APIRouter(prefix=f"{settings.api_prefix}/ingest", tags=["ingest"]). main.py registration mirrors goals (Plan 03).
- Async transaction (08-RESEARCH Pattern 1 + Code Examples): async with session.begin(): ... await session.flush() after goals ...
- Upsert (08-RESEARCH Pattern 2): SELECT by external_key; if found update descriptive fields only (NEVER completed/reminder_at/enabled/status); else INSERT. Do NOT use session.merge() or INSERT OR REPLACE.
- Rollback test injection point (Wave 0 test monkeypatches this): module-level _upsert_task in ingest_service.

NOTE: get_session yields a session WITHOUT an open transaction. apply_import opens `async with session.begin()`. The FastAPI 422 is automatic from Pydantic — no custom handler.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ingest Pydantic models (versioned, extra=forbid, cron-validated)</name>
  <files>backend/app/schemas/ingest.py</files>
  <read_first>
    - backend/app/schemas/routine.py (cron field_validator pattern to reuse)
    - backend/app/schemas/task.py (Priority import, field shapes)
    - .planning/phases/08-goals-ingest-backend/08-RESEARCH.md Pattern 3 (exact IngestPayload/GoalImport/HabitImport with Literal + extra=forbid + cron validator)
    - .planning/research/PITFALLS.md (pitfall 3 extra=forbid; pitfall 15 length limits + cron validation)
  </read_first>
  <behavior>
    - IngestPayload(schema_version="2.0") fails validation (Literal["1.0"]).
    - Any extra key on any import model fails validation (extra="forbid").
    - HabitImport / RoutineImport with an invalid cron fails validation.
    - IngestPayload.model_json_schema() returns a dict with "properties" (including schema_version) and "required".
  </behavior>
  <action>
    Create `backend/app/schemas/ingest.py` per RESEARCH Pattern 3:
    - Imports: `from typing import Literal`; `from datetime import date, datetime`; `from pydantic import BaseModel, ConfigDict, Field, field_validator`; `from app.models import Priority, RoutineAction`; `from app.models.goal import GoalType`.
    - Shared cron validator helper `_validate_cron(v)` (copy routine.py logic: CronTrigger.from_crontab in try/except -> raise ValueError "Invalid cron expression.").
    - `MilestoneImport(BaseModel)` with `model_config = ConfigDict(extra="forbid")`: title:str = Field(max_length=255); target_date: date | None = None; done: bool = False.
    - `GoalImport` extra="forbid": external_key:str = Field(max_length=200); title:str = Field(max_length=255); type: GoalType; description: str | None = Field(None, max_length=2000); target_date: date | None = None; milestones: list[MilestoneImport] = [].
    - `TaskImport` extra="forbid": external_key:str = Field(max_length=200); goal_key: str | None = None; title:str = Field(max_length=255); priority: Priority = Priority.medium; due_date: datetime | None = None; description: str | None = Field(None, max_length=2000).
    - `RoutineImport` extra="forbid": external_key:str = Field(max_length=200); goal_key: str | None = None; name:str = Field(max_length=80); cron:str; action: RoutineAction = RoutineAction.send_daily_brief. field_validator("cron") classmethod -> _validate_cron.
    - `HabitImport` extra="forbid": external_key:str = Field(max_length=200); goal_key: str | None = None; title:str = Field(max_length=255); recurrence_cron:str; priority: Priority = Priority.medium; description: str | None = Field(None, max_length=2000). field_validator("recurrence_cron") classmethod -> _validate_cron.
    - `IngestPayload` extra="forbid": schema_version: Literal["1.0"]; goals: list[GoalImport] = []; tasks: list[TaskImport] = []; routines: list[RoutineImport] = []; habits: list[HabitImport] = [].
    - `IngestResult(BaseModel)`: created: dict[str, int]; updated: dict[str, int].
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ingest.py::test_schema_endpoint tests/test_ingest.py::test_schema_version_mismatch tests/test_ingest.py::test_extra_field_rejected tests/test_ingest.py::test_invalid_cron_rejected -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/schemas/ingest.py` contains `Literal["1.0"]`, `ConfigDict(extra="forbid")` on every import model, and `field_validator` on cron + recurrence_cron.
    - `cd backend && uv run python -c "from app.schemas.ingest import IngestPayload, GoalImport, TaskImport, RoutineImport, HabitImport, IngestResult; print('ok')"` exits 0.
    - Once Task 2 wires the router, the four validation tests (test_schema_endpoint, test_schema_version_mismatch, test_extra_field_rejected, test_invalid_cron_rejected) pass. (Schema-version/extra/cron 422s come purely from these models.)
  </acceptance_criteria>
  <done>Ingest models import cleanly, reject bad version/extra/cron, and generate a JSON schema with schema_version + required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ingest service (transactional upsert, flush-then-resolve, _upsert_task injection point)</name>
  <files>backend/app/services/ingest_service.py</files>
  <read_first>
    - .planning/phases/08-goals-ingest-backend/08-RESEARCH.md Pattern 1 (flush-then-commit), Pattern 2 (select-then-upsert), and Code Examples (apply_import + _upsert_goal full code)
    - backend/app/schemas/ingest.py (Task 1 output)
    - backend/app/models/__init__.py and backend/app/models/goal.py (Task/Routine/Goal/Milestone column names from Plan 02)
    - .planning/research/PITFALLS.md (anti-pattern 4 single transaction; pitfall 2 destructive overwrite — preserve completed/reminder_at/enabled)
  </read_first>
  <behavior>
    - apply_import wraps ALL upserts (goals, tasks, routines, habits) in one `async with session.begin()`.
    - session.flush() runs after goals so goal.id is available for child goal_id resolution by goal_key.
    - Existing entity (external_key match) -> update descriptive fields only; NEVER overwrite Task.completed/reminder_at or Routine.enabled or Goal.status.
    - A RuntimeError raised inside _upsert_task (2nd call) rolls back the whole transaction -> zero new rows.
    - habits create Task rows with is_habit=True and recurrence_cron set; goal_key resolves to goal_id.
    - apply_import returns IngestResult(created, updated) with per-type counts.
  </behavior>
  <action>
    Create `backend/app/services/ingest_service.py`:
    - Imports: `from sqlalchemy import select`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from app.models import Task, Routine`; `from app.models.goal import Goal, Milestone, GoalType, GoalStatus`; `from app.schemas.ingest import IngestPayload, GoalImport, TaskImport, RoutineImport, HabitImport, IngestResult`.
    - `async def _upsert_goal(g, session) -> tuple[Goal, bool]`: select Goal where external_key==g.external_key; if found, set title/description/target_date/type from g (NOT status), and reconcile milestones (for each MilestoneImport: match existing by title within goal or append new Milestone; do NOT flip done on existing — preserve), return (existing, False). Else create Goal(external_key, title, type=g.type, description, target_date, status=GoalStatus.active) with milestones=[Milestone(title=..., target_date=..., done=m.done) for m in g.milestones]; session.add; return (new, True).
    - `async def _upsert_task(t, goal_id, session) -> tuple[Task, bool]`: select Task where external_key==t.external_key; if found, update title/description/priority/due_date and goal_id (NEVER completed/reminder_at), return (existing, False); else Task(external_key, title, priority, due_date, description, goal_id=goal_id, is_habit=False); add; return (new, True).
    - `async def _upsert_routine(r, goal_id, session) -> tuple[Routine, bool]`: select by external_key; update name/cron/action/goal_id (NEVER enabled) or create Routine(external_key, name, cron, action, goal_id=goal_id); return tuple.
    - `async def _upsert_habit(h, goal_id, session) -> tuple[Task, bool]`: select Task where external_key==h.external_key; update title/description/priority/recurrence_cron/goal_id and ensure is_habit=True (NEVER completed/reminder_at); or create Task(external_key, title, priority, description, recurrence_cron=h.recurrence_cron, goal_id=goal_id, is_habit=True); return tuple.
    - `async def apply_import(payload: IngestPayload, session: AsyncSession) -> IngestResult:` per RESEARCH Code Examples:
      created={"goals":0,"tasks":0,"routines":0,"habits":0}; updated same.
      `async with session.begin():`
        goal_key_to_id={}; for g in payload.goals: row,was_created=await _upsert_goal(g,session); bump created/updated["goals"]; goal_key_to_id[g.external_key]=None placeholder.
        `await session.flush()`; then re-populate goal_key_to_id with row.id (capture row in a list during the loop, then after flush set goal_key_to_id[g.external_key]=row.id). Simplest: keep a list of (external_key,row) tuples, flush, then build the dict from row.id.
        for t in payload.tasks: goal_id = goal_key_to_id.get(t.goal_key) if t.goal_key else None; _,c=await _upsert_task(t,goal_id,session); bump tasks.
        for r in payload.routines: resolve goal_id; await _upsert_routine; bump routines.
        for h in payload.habits: resolve goal_id; await _upsert_habit; bump habits.
      return IngestResult(created=created, updated=updated).
    - CRITICAL: `_upsert_task` MUST be a module-level function (the rollback test monkeypatches `app.services.ingest_service._upsert_task`). Inside apply_import, call it as `_upsert_task(...)` referencing the module global (so monkeypatch takes effect) — i.e. do NOT bind it to a local alias before the loop.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ingest.py::test_rollback_on_mid_commit_failure -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/ingest_service.py` contains `async with session.begin()`, `await session.flush()`, and module-level `async def _upsert_task`.
    - No `session.merge(` and no `INSERT OR REPLACE` in the file.
    - _upsert_task / _upsert_habit / _upsert_routine do not assign `completed`, `reminder_at`, or `enabled` on the existing-row update branch (grep confirms these tokens absent from update branches).
    - `cd backend && uv run python -c "from app.services.ingest_service import apply_import, _upsert_task; print('ok')"` exits 0.
    - `cd backend && uv run pytest tests/test_ingest.py::test_rollback_on_mid_commit_failure -x` exits 0 (zero-row guarantee proven). (Requires Task 3 router; if run before Task 3, expect collection of the test to need the endpoint — run after Task 3.)
  </acceptance_criteria>
  <done>apply_import upserts atomically with flush-then-resolve, preserves user-edited fields, and _upsert_task is a monkeypatchable module global.</done>
</task>

<task type="auto">
  <name>Task 3: Ingest router (GET /schema, POST /confirm) + register in main.py</name>
  <files>backend/app/routers/ingest.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/tasks.py and the goals router from Plan 03 (router prefix + Depends(get_session) pattern)
    - backend/app/main.py (router import line + include_router block — goals already added by Plan 03)
    - backend/app/schemas/ingest.py and backend/app/services/ingest_service.py (Tasks 1-2 output)
  </read_first>
  <action>
    Create `backend/app/routers/ingest.py`:
    - Imports: `from fastapi import APIRouter, Depends`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from app.db import get_session`; `from app.config import settings`; `from app.schemas.ingest import IngestPayload, IngestResult`; `from app.services import ingest_service`.
    - `router = APIRouter(prefix=f"{settings.api_prefix}/ingest", tags=["ingest"])`.
    - `@router.get("/schema")` `async def get_schema(): return IngestPayload.model_json_schema()`. (Optionally also include an `llm_prompt` string per RESEARCH open question 3 — Claude's discretion; if added, return `{**IngestPayload.model_json_schema(), "llm_prompt": "..."}` and ensure the test's assertions on `properties`/`required` still hold by keeping those top-level keys. The test checks `properties` and `required` exist at top level — model_json_schema already puts them there, so wrapping must preserve them. Simplest compliant form: return model_json_schema() directly.)
    - `@router.post("/confirm", response_model=IngestResult)` `async def confirm(payload: IngestPayload, session: AsyncSession = Depends(get_session)): return await ingest_service.apply_import(payload, session)`. FastAPI returns 422 automatically on payload validation failure (schema_version/extra/cron). A RuntimeError from apply_import surfaces as 500 (acceptable — the rollback test asserts zero rows, not a specific status).

    In `backend/app/main.py`:
    - Add `ingest` to the routers import line (alongside `goals` added by Plan 03).
    - Add `app.include_router(ingest.router)` after `app.include_router(goals.router)`.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_ingest.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/ingest.py` contains `prefix=f"{settings.api_prefix}/ingest"`, `model_json_schema()`, and `apply_import(payload, session)`.
    - `backend/app/main.py` contains `ingest.router`.
    - `cd backend && uv run pytest tests/test_ingest.py -x` exits 0 (all 10 INGEST tests green).
    - `cd backend && uv run pytest` exits 0 (FULL suite green — goals + ingest + all pre-existing).
  </acceptance_criteria>
  <done>All INGEST-01/02/04/06/07 tests pass; ingest router registered; full phase suite green.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/test_ingest.py -x` — 10/10 green.
- `cd backend && uv run pytest` — entire backend suite green (Phase 8 complete gate).
- `cd backend && uv run alembic heads` — single head 0008 (no branch introduced).
- Manual (per 08-VALIDATION Manual-Only): on the Pi, a real milestone completion delivers Pushover + Google Home TTS.
</verification>

<success_criteria>
The ingest endpoint validates (422 on version/extra/cron), writes goals->tasks->routines->habits atomically (zero rows on mid-commit failure), is idempotent on external_key (no duplicates, preserves user-edited completed), creates habits as is_habit Task rows linked to goals, and serves the versioned JSON schema. All INGEST-* Wave 0 tests are green and the full suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-goals-ingest-backend/08-04-SUMMARY.md`. This completes Phase 8 backend; note the ingest contract version (schema_version "1.0") and that Phase 9 builds the UI (paste/upload + dry-run preview) on top.
</output>
