---
phase: 08-goals-ingest-backend
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - backend/app/schemas/goal.py
  - backend/app/services/goal_service.py
  - backend/app/services/celebrate.py
  - backend/app/routers/goals.py
  - backend/app/main.py
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-06]
must_haves:
  truths:
    - "User can create, read, list (incl. archived), and PATCH-archive a goal via the API"
    - "GoalRead returns a live-computed progress_pct and the goal's milestones"
    - "Milestone CRUD works; marking a milestone done False->True fires a celebration once"
    - "Setting goal status->completed fires a goal celebration once"
    - "Progress = (done tasks + done milestones)/(total tasks + total milestones), 0 when empty"
  artifacts:
    - path: "backend/app/services/goal_service.py"
      provides: "compute_progress unified ratio (D-01/D-02/D-03)"
      contains: "def compute_progress"
    - path: "backend/app/services/celebrate.py"
      provides: "fire_milestone_celebration, fire_goal_celebration (reuse Pushover+TTS)"
      contains: "def fire_milestone_celebration"
    - path: "backend/app/routers/goals.py"
      provides: "Goals CRUD + milestone CRUD + celebration triggers"
      exports: ["router"]
  key_links:
    - from: "backend/app/routers/goals.py"
      to: "app.services.celebrate"
      via: "run_in_threadpool on done/status transition"
      pattern: "run_in_threadpool"
    - from: "backend/app/services/celebrate.py"
      to: "PushoverClient / TTSClient"
      via: "send + speak"
      pattern: "PushoverClient\\(\\).send"
    - from: "backend/app/main.py"
      to: "app.routers.goals"
      via: "include_router"
      pattern: "goals.router"
---

<objective>
Build the Goals feature: schemas, the live-progress service (D-01/D-02/D-03 unified ratio), the celebration helper reusing existing Pushover+TTS (D-10/D-11/D-12), the goals + milestones CRUD router with celebration triggers, and register it in main.py. This turns the GOAL-* Wave 0 tests green.

Honors: D-13 status enum archive (no hard delete), D-15 fields, D-01 unified ratio, D-02 never stored / 0% when empty, D-10 celebrate on milestone done False→True AND goal status→completed, D-11 reuse PushoverClient.send + TTSClient.speak, D-12 warm+specific copy.

Purpose: Goals, progress, milestones, and celebrations are the user-facing half of Phase 8.
Output: schemas/goal.py, services/goal_service.py, services/celebrate.py, routers/goals.py, main.py registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-goals-ingest-backend/08-CONTEXT.md
@.planning/phases/08-goals-ingest-backend/08-RESEARCH.md
@.planning/phases/08-goals-ingest-backend/08-02-SUMMARY.md

<interfaces>
<!-- Built in Plan 02 — use directly. -->
From app/models/goal.py: Goal(id, external_key, title, type:GoalType, description, target_date, status:GoalStatus, created_at, updated_at, milestones:list[Milestone], tasks, routines); Milestone(id, goal_id, title, target_date, done:bool).
GoalType: career|life|health|learning|financial. GoalStatus: active|archived|completed.
From app/models/__init__.py: Task(... goal_id, completed ...).

Existing patterns to mirror:
- Router: backend/app/routers/tasks.py — APIRouter(prefix=f"{settings.api_prefix}/goals"), async handlers, Depends(get_session), session.get / session.execute(select(...)), exclude_unset PATCH.
- Schema trio: backend/app/schemas/task.py — Create/Update/Read with model_config={"from_attributes":True}.
- Sync clients (call via run_in_threadpool — they are sync, blocking):
  PushoverClient().send(title:str, message:str, priority:int=0)  [backend/app/services/pushover.py]
  TTSClient().speak(text:str)                                    [backend/app/services/tts.py]
  app.services.tts_settings.get_tts_enabled() -> bool            [gate TTS like brief does]
- run_in_threadpool usage example: backend/app/main.py and webhooks.py.

Compute progress query (two aggregate selects, no N+1) — 08-RESEARCH §"Pattern 5":
select(func.count(Task.id), func.sum(case((Task.completed==True,1),else_=0))).where(Task.goal_id==goal_id)
select(func.count(Milestone.id), func.sum(case((Milestone.done==True,1),else_=0))).where(Milestone.goal_id==goal_id)
pct = round(done/total*100) if total>0 else 0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Goal/Milestone schemas, compute_progress service, celebrate helper</name>
  <files>backend/app/schemas/goal.py, backend/app/services/goal_service.py, backend/app/services/celebrate.py</files>
  <read_first>
    - backend/app/schemas/task.py and backend/app/schemas/routine.py (Create/Update/Read shape + from_attributes)
    - backend/app/services/brief.py (proven pattern for reusing Pushover/TTS + gating TTS via get_tts_enabled)
    - backend/app/services/pushover.py, backend/app/services/tts.py, backend/app/services/tts_settings.py (exact call signatures)
    - .planning/phases/08-goals-ingest-backend/08-RESEARCH.md §"Pattern 5" (progress query) and §"Pattern 6" (celebrate.py code)
  </read_first>
  <behavior>
    - compute_progress(goal_id, session) returns {"total":int,"done":int,"pct":int}; pct==0 when total==0 (no divide-by-zero, D-02).
    - A done task and a done milestone each count as one unit in numerator and denominator (D-01); habit tasks (is_habit True) count as task units because they are Task rows with goal_id (D-03).
    - fire_milestone_celebration(milestone_title, goal_title) calls PushoverClient().send(...) and, if get_tts_enabled(), TTSClient().speak(...) with warm+specific copy naming the milestone and parent goal.
    - fire_goal_celebration(goal_title) does the same naming the goal.
  </behavior>
  <action>
    Create `backend/app/schemas/goal.py`:
    - `from app.models.goal import GoalType, GoalStatus`; `from datetime import date, datetime`; pydantic BaseModel.
    - `MilestoneCreate`: title:str; target_date: date | None = None.
    - `MilestoneUpdate`: title: str|None=None; target_date: date|None=None; done: bool|None=None.
    - `MilestoneRead`: id:int; goal_id:int; title:str; target_date:date|None; done:bool; model_config={"from_attributes":True}.
    - `GoalCreate`: title:str; type:GoalType; description:str|None=None; target_date:date|None=None; external_key:str|None=None.
    - `GoalUpdate`: title:str|None=None; type:GoalType|None=None; description:str|None=None; target_date:date|None=None; status:GoalStatus|None=None.
    - `GoalRead`: id:int; title:str; type:GoalType; description:str|None; target_date:date|None; status:GoalStatus; external_key:str|None; created_at:datetime; updated_at:datetime; progress_pct:int; milestones:list[MilestoneRead]=[]; model_config={"from_attributes":True}.

    Create `backend/app/services/goal_service.py`:
    - `from sqlalchemy import select, func, case`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from app.models import Task`; `from app.models.goal import Milestone`.
    - `async def compute_progress(goal_id:int, session:AsyncSession) -> dict:` — two aggregate queries per RESEARCH Pattern 5 (Task by goal_id, Milestone by goal_id), coalesce None to 0, total=tasks+ms, done=done_tasks+done_ms, pct=round(done/total*100) if total>0 else 0. Return {"total":total,"done":done,"pct":pct}.

    Create `backend/app/services/celebrate.py` per RESEARCH Pattern 6 (SYNC functions — they call sync clients):
    ```python
    from app.services.pushover import PushoverClient
    from app.services.tts import TTSClient
    import app.services.tts_settings as _tts_settings

    def fire_milestone_celebration(milestone_title: str, goal_title: str) -> None:
        msg = f'Nice work — you completed "{milestone_title}" on your {goal_title} goal.'
        PushoverClient().send(title="Milestone complete", message=msg)
        if _tts_settings.get_tts_enabled():
            TTSClient().speak(msg)

    def fire_goal_celebration(goal_title: str) -> None:
        msg = f"Congratulations! You reached your goal: {goal_title}."
        PushoverClient().send(title="Goal achieved", message=msg)
        if _tts_settings.get_tts_enabled():
            TTSClient().speak(msg)
    ```
    Import tts_settings as a module (`import app.services.tts_settings as _tts_settings`) so test monkeypatch on the source attribute works (per the STATE.md Phase 06 module-ref lesson).
  </action>
  <verify>
    <automated>cd backend && uv run python -c "from app.services.goal_service import compute_progress; from app.services.celebrate import fire_milestone_celebration, fire_goal_celebration; from app.schemas.goal import GoalRead, GoalCreate, MilestoneRead; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/goal_service.py` contains `def compute_progress` and uses `func.count` and `case(`.
    - `backend/app/services/celebrate.py` contains `def fire_milestone_celebration` and `PushoverClient().send` and `_tts_settings.get_tts_enabled()`.
    - `backend/app/schemas/goal.py` contains `progress_pct: int` and `milestones: list[MilestoneRead]` on GoalRead.
    - The verify import command exits 0.
  </acceptance_criteria>
  <done>Schemas, progress service, and celebrate helper import cleanly and match the locked decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Goals + milestones router with celebration triggers; register in main.py</name>
  <files>backend/app/routers/goals.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/tasks.py (router prefix, async CRUD, session.get, exclude_unset PATCH — mirror exactly)
    - backend/app/main.py (router registration block + run_in_threadpool import already present)
    - .planning/phases/08-goals-ingest-backend/08-RESEARCH.md §"Pattern 6" (router celebration trigger code: capture old value, commit, refresh, fire on transition via run_in_threadpool)
    - backend/app/schemas/goal.py and backend/app/services/goal_service.py and celebrate.py (from Task 1)
  </read_first>
  <action>
    Create `backend/app/routers/goals.py`:
    - Imports: `from fastapi import APIRouter, Depends, HTTPException`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from sqlalchemy import select`; `from sqlalchemy.orm import selectinload`; `from starlette.concurrency import run_in_threadpool`; `from app.db import get_session`; `from app.config import settings`; `from app.models.goal import Goal, Milestone, GoalStatus`; `from app.schemas.goal import GoalCreate, GoalUpdate, GoalRead, MilestoneCreate, MilestoneUpdate, MilestoneRead`; `from app.services import goal_service, celebrate`.
    - `router = APIRouter(prefix=f"{settings.api_prefix}/goals", tags=["goals"])`.
    - Helper `async def _to_read(goal, session) -> GoalRead`: progress = await goal_service.compute_progress(goal.id, session); return GoalRead.model_validate({**{c: getattr(goal,c) for c in ("id","title","type","description","target_date","status","external_key","created_at","updated_at")}, "progress_pct": progress["pct"], "milestones": goal.milestones}). (goal.milestones is co-loaded via selectin.)
    - `GET /` list_goals: select(Goal).order_by(Goal.created_at.desc()) with options(selectinload(Goal.milestones)); return [await _to_read(g, session) for g in rows]. MUST return archived goals too (no status filter) — satisfies test_no_hard_delete.
    - `POST /` create_goal (status_code=201): Goal(**body.model_dump()); session.add; commit; refresh; re-fetch with selectinload(Goal.milestones) (or session.refresh(goal, ["milestones"])); return _to_read.
    - `GET /{goal_id}` get_goal: session.get(Goal, goal_id, options=[selectinload(Goal.milestones)]); 404 if missing; return _to_read.
    - `PATCH /{goal_id}` update_goal: load goal with selectinload(milestones); 404 if missing; `old_status = goal.status`; apply body.model_dump(exclude_unset=True) via setattr; commit; refresh (with milestones); if old_status != GoalStatus.completed and goal.status == GoalStatus.completed: `await run_in_threadpool(celebrate.fire_goal_celebration, goal.title)`; return _to_read.
    - `POST /{goal_id}/milestones` add_milestone (status_code=201, response_model=MilestoneRead): verify goal exists (404 else); ms = Milestone(goal_id=goal_id, **body.model_dump()); add; commit; refresh; return ms.
    - `PATCH /{goal_id}/milestones/{ms_id}` update_milestone (response_model=MilestoneRead): goal = session.get(Goal, goal_id) (404 if missing); ms = session.get(Milestone, ms_id); 404 if missing or ms.goal_id != goal_id; `old_done = ms.done`; apply exclude_unset; commit; refresh; if not old_done and ms.done: `await run_in_threadpool(celebrate.fire_milestone_celebration, ms.title, goal.title)`; return ms.

    NOTE D-10/no-double-celebration: the `not old_done and ms.done` guard ensures re-PATCHing an already-done milestone does NOT re-fire (satisfies test_no_double_celebration). Same shape for goal status.

    In `backend/app/main.py`:
    - Add `goals` to the routers import line: `from app.routers import tasks, auth, calendar_status, events, settings as settings_router, routines, tts, webhooks, goals`.
    - Add `app.include_router(goals.router)` after the existing include_router calls.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_goals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/goals.py` contains `prefix=f"{settings.api_prefix}/goals"`, `run_in_threadpool(celebrate.fire_goal_celebration` and `run_in_threadpool(celebrate.fire_milestone_celebration`.
    - `backend/app/main.py` contains `goals.router`.
    - GET / handler has NO status filter (archived goals returned).
    - `cd backend && uv run pytest tests/test_goals.py -x` exits 0 (all 10 GOAL tests green).
    - `cd backend && uv run pytest tests/test_tasks.py tests/test_routines.py -x` still green (no regression from main.py edit).
  </acceptance_criteria>
  <done>All GOAL-01/02/03/06 tests pass; goals router registered; existing suites unbroken.</done>
</task>

</tasks>

<verification>
- `cd backend && uv run pytest tests/test_goals.py -x` — 10/10 green.
- `cd backend && uv run pytest` — full suite green except test_ingest.py (built in Plan 04).
- Celebrations fire only on transition (test_no_double_celebration green) and reuse Pushover+TTS (monkeypatch asserts).
</verification>

<success_criteria>
Goals CRUD + archive (no hard delete), live progress (unified ratio, 0% empty), milestone CRUD, and transition-gated celebrations reusing existing notification infra all work; the GOAL-* Wave 0 tests are green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-goals-ingest-backend/08-03-SUMMARY.md`. Note: goals router registered in main.py; Plan 04 (ingest) appends its own include_router line after goals.
</output>
