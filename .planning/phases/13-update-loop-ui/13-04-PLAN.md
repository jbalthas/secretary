---
phase: 13-update-loop-ui
plan: 04
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - frontend/src/hooks/useCheckInSettings.ts
  - frontend/src/pages/Settings.tsx
autonomous: false
requirements: [NOTIF-08]
must_haves:
  truths:
    - "Settings shows a Check-in Notification section with an enable/disable toggle and a time input"
    - "Saving sends PUT /api/v1/settings/check-in-time with { hour, minute, enabled } integers (split from HH:MM)"
    - "The saved schedule takes effect without a server restart and persists across reboots (Phase 12 + Wave 1 guarantee)"
    - "Disabling the toggle and saving unregisters the mid-day check-in job"
  artifacts:
    - path: "frontend/src/hooks/useCheckInSettings.ts"
      provides: "GET/PUT check-in time + enabled hook mirroring useBriefSettings"
      contains: "check-in-time"
    - path: "frontend/src/pages/Settings.tsx"
      provides: "Check-in Notification settings section"
      contains: "Check-in Notification"
  key_links:
    - from: "frontend/src/hooks/useCheckInSettings.ts"
      to: "/api/v1/settings/check-in-time"
      via: "GET on mount, PUT on save with hour/minute/enabled"
      pattern: "settings/check-in-time"
    - from: "frontend/src/pages/Settings.tsx"
      to: "useCheckInSettings"
      via: "section render + handleSave"
      pattern: "useCheckInSettings"
---

<objective>
Add the Check-in Notification section to Settings (NOTIF-08): a toggle to enable/disable the mid-day check-in and a time input, saved together via the Wave 1 `PUT /api/v1/settings/check-in-time` (which now accepts `enabled` and skips/removes the job when disabled). Mirrors `useBriefSettings`/`useWorkHours` exactly. Ends with a human-verify checkpoint that covers the full phase UI golden path (this section + the Today surfaces from plan 03), including the reboot-persistence check that can only be confirmed manually.

Purpose: Lets the user control the check-in cadence from the web UI — the last requirement of the phase.
Output: `useCheckInSettings` hook, a new Settings section, and a verified end-to-end manual pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-update-loop-ui/13-RESEARCH.md
@.planning/phases/13-update-loop-ui/13-UI-SPEC.md
@.planning/phases/13-update-loop-ui/13-02-SUMMARY.md

<interfaces>
<!-- Verified contracts. Endpoint is PUT (not PATCH); body is integers (not HH:MM string). -->

Backend GET /api/v1/settings/check-in-time → `{ hour: int, minute: int, enabled: bool }` (Wave 1 added `enabled`; default hour=12, minute=0, enabled=true).
Backend PUT /api/v1/settings/check-in-time body → `{ hour: int (0-23), minute: int (0-59), enabled: bool }`. PUT side effect: enabled→schedule_checkin; disabled→remove_checkin. Confirm the exact `enabled` field name from 13-02-SUMMARY.md.

useBriefSettings template (frontend/src/hooks/useBriefSettings.ts) — GET maps `{hour,minute}`→"HH:MM" with padStart; PUT splits "HH:MM"→`{hour,minute}`; returns boolean from save; error string state.

Settings.tsx section pattern (frontend/src/pages/Settings.tsx): each section is `<section style={{ marginBottom: 24 }}>` with `<p style={SECTION_LABEL_STYLE}>` + `<div style={CARD_STYLE}>`; loading shows `<p ...>Loading…</p>`; inputs use `.drawer-field` + inline-styled `<input>`; save is `<button className="btn-save">` with "Saving…" busy label; error is 12px var(--destructive). The Daily Brief section (lines 216-263) is the closest template. New section goes at the bottom of the sections list, just before `<RoutineDrawer>`.

Copy (UI-SPEC § 4 + Copywriting Contract): section label "Check-in Notification"; toggle label "Enable mid-day check-in"; time label "Check-in time"; helper "A Pushover reminder to log progress. Default: 12:00."; save idle "Save Check-in Settings"; save busy "Saving…"; error "Failed to save. Check your connection and try again."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useCheckInSettings hook</name>
  <files>frontend/src/hooks/useCheckInSettings.ts</files>
  <read_first>
    - frontend/src/hooks/useBriefSettings.ts (the exact template — GET map, PUT split, boolean return, error state)
    - .planning/phases/13-update-loop-ui/13-02-SUMMARY.md (confirm the GET/PUT `enabled` field name added in Wave 1)
    - .planning/phases/13-update-loop-ui/13-RESEARCH.md (§ useCheckInSettings pattern; Pitfalls 2 & 3 — PUT not PATCH, integers not HH:MM)
  </read_first>
  <action>
    Create `frontend/src/hooks/useCheckInSettings.ts` mirroring `useBriefSettings`. State: `checkInTime: string | null` ("HH:MM"), `checkInEnabled: boolean` (default true), `loading`, `error`. On mount GET `/api/v1/settings/check-in-time`: set `checkInTime = \`${String(d.hour).padStart(2,"0")}:${String(d.minute).padStart(2,"0")}\`` and `checkInEnabled = d.enabled` (coalesce `?? true` for safety). On failure set error "Could not load check-in settings."

    `save(time: string, enabled: boolean): Promise<boolean>`:
    ```typescript
    const [h, m] = time.split(":").map(Number);
    const res = await fetch("/api/v1/settings/check-in-time", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ hour: h, minute: m, enabled }),
    });
    if (res.ok) { setCheckInTime(time); setCheckInEnabled(enabled); return true; }
    return false;
    ```
    Return `{ checkInTime, checkInEnabled, loading, error, save }`. Use PUT (not PATCH) and integer body (not `{time}`) — RESEARCH Pitfalls 2 & 3.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/hooks/useCheckInSettings.ts contains `method: "PUT"` and `JSON.stringify({ hour: h, minute: m, enabled })`
    - frontend/src/hooks/useCheckInSettings.ts does NOT contain `PATCH` and does NOT send a `time:` body field
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Hook GETs current check-in time+enabled and PUTs integer hour/minute/enabled, returning boolean success.</done>
</task>

<task type="auto">
  <name>Task 2: Check-in Notification section in Settings.tsx</name>
  <files>frontend/src/pages/Settings.tsx</files>
  <read_first>
    - frontend/src/pages/Settings.tsx (the file being modified — SECTION_LABEL_STYLE, CARD_STYLE, the Daily Brief section as template, RoutineDrawer placement, sync-input useEffect idiom lines 67-81)
    - frontend/src/hooks/useCheckInSettings.ts (just created)
    - frontend/src/pages/Settings.tsx lines 430-440 (the existing tts-enabled checkbox row idiom to mirror for the toggle)
    - .planning/phases/13-update-loop-ui/13-UI-SPEC.md (§ Surface 4 Check-In Settings — layout, toggle gap 8px, copy)
  </read_first>
  <action>
    Modify `frontend/src/pages/Settings.tsx`. Add hook + local state near the other hooks:
    ```typescript
    const { checkInTime, checkInEnabled, loading: ciLoading, error: ciLoadError, save: saveCheckIn } = useCheckInSettings();
    const [ciTime, setCiTime] = useState("");
    const [ciEnabled, setCiEnabled] = useState(true);
    const [ciSaving, setCiSaving] = useState(false);
    const [ciError, setCiError] = useState<string | null>(null);
    ```
    Sync effect (mirror the briefTime sync, lines 67-70):
    ```typescript
    useEffect(() => { if (checkInTime !== null) setCiTime(checkInTime); }, [checkInTime]);
    useEffect(() => { setCiEnabled(checkInEnabled); }, [checkInEnabled]);
    ```
    Handler:
    ```typescript
    async function handleSaveCheckIn() {
      setCiError(null); setCiSaving(true);
      try {
        const ok = await saveCheckIn(ciTime, ciEnabled);
        if (!ok) setCiError("Failed to save. Check your connection and try again.");
      } finally { setCiSaving(false); }
    }
    ```
    Add a new `<section style={{ marginBottom: 24 }}>` immediately before `<RoutineDrawer ... />`:
    - `<p style={SECTION_LABEL_STYLE}>Check-in Notification</p>`
    - `<div style={CARD_STYLE}>`: if `ciLoading && checkInTime === null` → Loading… `<p>`; else if `ciLoadError` → error `<p>`; else a fragment with:
      - Toggle row `<div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 12 }}>`: `<input id="checkin-enabled" type="checkbox" checked={ciEnabled} onChange={() => setCiEnabled(!ciEnabled)} style={{ accentColor: "var(--accent)" }} />` + `<label htmlFor="checkin-enabled" style={{ fontSize: 14, color: "var(--text)", cursor: "pointer" }}>Enable mid-day check-in</label>`.
      - Time input wrapped in `<div className="drawer-field" style={{ marginBottom: 8 }}>` with `<label htmlFor="checkin-time">Check-in time</label>` and `<input id="checkin-time" type="time" value={ciTime} onChange={(e) => setCiTime(e.target.value)} disabled={!ciEnabled} style={...same inline style as Brief time input...} />`.
      - Helper `<p style={{ margin: "0 0 12px", fontSize: 12, color: "var(--text-secondary)" }}>A Pushover reminder to log progress. Default: 12:00.</p>`.
      - `{ciError && <p style={{ margin: "0 0 8px", fontSize: 12, color: "var(--destructive)" }}>{ciError}</p>}`
      - `<button type="button" className="btn-save" onClick={handleSaveCheckIn} disabled={ciSaving} style={{ opacity: ciSaving ? 0.6 : 1, cursor: ciSaving ? "not-allowed" : "pointer" }}>{ciSaving ? "Saving…" : "Save Check-in Settings"}</button>`

    Import `useCheckInSettings`. Do NOT change the existing 13px `SECTION_LABEL_STYLE` value (UI-SPEC: existing components keep 13px).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Settings.tsx contains `useCheckInSettings` and the literal `Check-in Notification`
    - frontend/src/pages/Settings.tsx contains `Enable mid-day check-in` and the `checkin-time` input
    - the time input is `disabled={!ciEnabled}`
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Settings renders the Check-in Notification section; toggle + time save together via the hook.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full update-loop UI golden path</name>
  <files>frontend/src/pages/Today.tsx, frontend/src/pages/Settings.tsx</files>
  <what-built>
    The complete Phase 13 UI: Today quick-update box + candidate confirmation + end-of-day rollup (plan 03) and the Settings Check-in Notification section (this plan). Backend now mutates on resolved updates and persists check_in_enabled (plan 02).
  </what-built>
  <action>
    This is a human-verification checkpoint. Claude must first ensure the frontend builds (`cd frontend && npm run build`) and the backend test suite is green (`cd backend && uv run pytest tests/test_updates.py`), start/confirm the dev environment is reachable, then PAUSE and present the how-to-verify steps below to the user. Do not mark the phase complete until the user types "approved".
  </action>
  <how-to-verify>
    Run frontend dev build + backend, then on a phone browser (or desktop):
    1. Today tab: confirm the quick-update textarea is above the Focus banner with placeholder "What got done? e.g. finished standup".
    2. Type the title of a real scheduled task/block (e.g. "finished standup"), submit (button or Enter). Expect a brief "Done" flash and the item to show completed WITHOUT a page reload.
    3. Type something that matches two similar items (e.g. a partial title shared by two). Expect the CandidateCard with an amber banner and candidate rows. Click "Confirm match" on one → "Done" flash + the chosen item updates. Re-trigger, click "Skip" on a candidate → it disappears from the list. Click "None of these — dismiss" → card clears, textarea clears.
    4. Settings tab: confirm a "Check-in Notification" section with an enable toggle + time input + "Save Check-in Settings" button. Change the time, Save → no error. Toggle OFF, Save → the time input disables.
    5. Rollup gate: set Work Hours end to a time just passed (or check after work hours), reload Today → confirm the "Day Rollup" card shows N completed • M slipped. Set work-end to the future → confirm the card is hidden.
    6. Persistence (manual-only): restart the `secretary` service; confirm the saved check-in schedule is still registered (APScheduler SQLAlchemyJobStore) — e.g. via logs or by confirming the check-in still fires at the set time.
    7. Deep link: open `/today?update=1` → the update textarea should be focused and the URL cleaned to `/today`.
  </how-to-verify>
  <verify>
    <automated>cd frontend && npm run build && npm test -- --run</automated>
  </verify>
  <done>User has exercised all seven golden-path steps and typed "approved" (or filed issues that were then resolved).</done>
  <resume-signal>Type "approved" or describe any issues (which surface, what was wrong).</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npm test -- --run` green
- `cd backend && uv run pytest tests/test_updates.py -k checkin` green (enabled persistence proven in plan 02)
- Human-verify checkpoint approved
</verification>

<success_criteria>
- NOTIF-08: user can enable/disable and set the check-in time from Settings; schedule takes effect without restart and persists across reboots
- Full update-loop golden path confirmed working end to end
</success_criteria>

<output>
After completion, create `.planning/phases/13-update-loop-ui/13-04-SUMMARY.md`
</output>
