---
phase: 10-day-auto-organize
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - frontend/src/types/plan.ts
  - frontend/src/types/task.ts
  - frontend/src/hooks/usePlan.ts
  - frontend/src/hooks/useWorkHours.ts
  - frontend/src/lib/agenda.ts
  - frontend/src/components/AgendaItem.tsx
  - frontend/src/components/BottomNav.tsx
  - frontend/src/pages/Organize.tsx
  - frontend/src/pages/Today.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [PLAN-01, PLAN-02]

must_haves:
  truths:
    - "User can open Organize, get a proposal, remove/reorder(up-down)/adjust blocks, and approve once"
    - "Already-approved date loads committed blocks in an approved state with a Re-plan button"
    - "Didn't-fit list and fully-booked message render on Organize"
    - "Approved blocks appear in Today alongside events/tasks; conflicting blocks show a per-block staleness badge"
    - "Work hours are editable on the Settings page"
  artifacts:
    - path: "frontend/src/pages/Organize.tsx"
      provides: "propose/review/edit/approve + already-approved + re-plan flow"
      contains: "usePlan"
      min_lines: 120
    - path: "frontend/src/hooks/usePlan.ts"
      provides: "plan API client hook"
      contains: "propose"
    - path: "frontend/src/types/plan.ts"
      provides: "ScheduledBlock/ProposedBlock/ProposedDayPlan TS types"
      contains: "ScheduledBlock"
  key_links:
    - from: "frontend/src/hooks/usePlan.ts"
      to: "/api/v1/plan"
      via: "fetch propose/approve/replan/blocks/delete"
      pattern: "/api/v1/plan"
    - from: "frontend/src/pages/Today.tsx"
      to: "usePlan blocks"
      via: "fetch blocks for today, pass to buildWeekAgenda"
      pattern: "usePlan"
    - from: "frontend/src/components/AgendaItem.tsx"
      to: "conflict_with"
      via: "staleness badge render"
      pattern: "conflict_with"
    - from: "frontend/src/App.tsx"
      to: "/organize"
      via: "Route element Organize"
      pattern: "/organize"
---

<objective>
Build the frontend: plan types, `usePlan` and `useWorkHours` hooks, the Organize page (propose/review/edit/approve + already-approved/re-plan + didn't-fit + fully-booked), Today integration with the per-block staleness badge, the work-hours Settings card, the Organize nav tab, and the /organize route.

Purpose: Completes the user-facing half of PLAN-01/PLAN-02 (D-10/D-11/D-12/D-13/D-14 + D-05 Settings UI). Ends with a human checkpoint to verify the touch UI on a phone browser.
Output: types/plan.ts, usePlan.ts, useWorkHours.ts, Organize.tsx, plus targeted edits to agenda.ts, AgendaItem.tsx, BottomNav.tsx, Today.tsx, Settings.tsx, App.tsx, types/task.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-day-auto-organize/10-CONTEXT.md
@.planning/phases/10-day-auto-organize/10-RESEARCH.md

<interfaces>
Backend endpoints live (10-03): `GET /api/v1/plan/propose?date=YYYY-MM-DD` → ProposedDayPlan; `POST /api/v1/plan/approve` {date, blocks} → ScheduledBlock[] (409 if exists); `POST /api/v1/plan/replan` {date, blocks} → ScheduledBlock[]; `GET /api/v1/plan/blocks?date=YYYY-MM-DD` → ScheduledBlock[] (each with conflict_with); `DELETE /api/v1/plan/blocks/{id}` → 204. Work hours: `GET/PUT /api/v1/settings/work-hours` {work_start:"HH:MM", work_end:"HH:MM"}.

Backend JSON shapes:
ProposedDayPlan = { date: string; blocks: ProposedBlock[]; unplaced_task_ids: number[]; fully_booked: boolean }
ProposedBlock = { task_id: number|null; title: string; start_dt: string; end_dt: string }
ScheduledBlock = { id; task_id; title; start_dt; end_dt; date_key; approved_at; conflict_with: string|null }

From frontend/src/types/task.ts — current AgendaItem (extend, do not break):
```typescript
export interface AgendaItem {
  id: string; title: string; time: string | null; priority: Priority | null;
  isEvent: boolean; completed: boolean; taskId?: number; googleId?: string;
}
```
From frontend/src/lib/agenda.ts — buildDayItems(tasks, events, dateKey); buildWeekAgenda(tasks, events, now); start_dt parsed via new Date() then getHours() for time. NOTE the research example uses getUTCHours but existing event code uses getHours() — match the existing event rendering (getHours) for consistency within buildDayItems.
From frontend/src/hooks/useCalendarEvents.ts — fetch hook pattern (useState + refresh + useEffect).
From frontend/src/components/BottomNav.tsx — 4 NavLinks (Today/Tasks/Goals/Settings), lucide-react icons, tabStyle, isActive color callback.
From frontend/src/App.tsx — Routes with /today /tasks /goals /ingest /settings; import page, add Route.
From frontend/src/pages/Today.tsx — useTasks + useCalendarEvents → buildWeekAgenda(tasks, events) → DaySection per group; handleToggle.
From frontend/src/pages/Settings.tsx — SECTION_LABEL_STYLE, CARD_STYLE, btn-save button, section structure (mirror the Daily Brief card for a Work Hours card).
From frontend/src/components/AgendaItem.tsx — rowStyle/titleStyle/timeStyle inline styles; renders checkbox + title + priority badge + time; extend to render Planned + conflict badges.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan types + AgendaItem type fields + usePlan + useWorkHours hooks + agenda.ts/AgendaItem block rendering</name>
  <read_first>
    - frontend/src/types/task.ts (AgendaItem, Priority, CalendarEvent)
    - frontend/src/hooks/useCalendarEvents.ts (hook fetch pattern)
    - frontend/src/hooks/useBriefSettings.ts (settings GET/PUT hook pattern, if present)
    - frontend/src/lib/agenda.ts (buildDayItems/buildWeekAgenda — exact current code)
    - frontend/src/components/AgendaItem.tsx (inline-style render, where to add badges)
    - .planning/phases/10-day-auto-organize/10-RESEARCH.md (§9 frontend types, usePlan, agenda.ts mod, AgendaItem badge)
  </read_first>
  <action>
    Create `frontend/src/types/plan.ts` with `ProposedBlock`, `ProposedDayPlan`, `ScheduledBlock` interfaces exactly as in RESEARCH §9 (ProposedBlock: task_id number|null, title, start_dt, end_dt; ProposedDayPlan: date, blocks, unplaced_task_ids number[], fully_booked; ScheduledBlock: id, task_id, title, start_dt, end_dt, date_key, approved_at, conflict_with string|null).

    In `frontend/src/types/task.ts`, extend `AgendaItem` with three optional fields (do not remove existing ones):
    ```typescript
    isBlock?: boolean;
    conflict_with?: string | null;
    blockId?: number;
    ```

    Create `frontend/src/hooks/usePlan.ts` (mirror RESEARCH §9, fetch-based, no new deps). Export `usePlan(dateKey: string)` returning `{ blocks, loading, fetchBlocks, propose, approve, replan, deleteBlock }` where:
    - `const API = "/api/v1/plan";`
    - `fetchBlocks()` GETs `${API}/blocks?date=${dateKey}` → setBlocks (or [] on !ok).
    - `propose(date)` GETs `${API}/propose?date=${date}` → returns ProposedDayPlan | null.
    - `approve(date, blocks)` POSTs `${API}/approve` JSON {date, blocks}; if res.status === 409 `throw new Error("already_approved")`; else return ScheduledBlock[] | null.
    - `replan(date, blocks)` POSTs `${API}/replan` JSON {date, blocks} → return ScheduledBlock[].
    - `deleteBlock(id)` DELETEs `${API}/blocks/${id}` then `await fetchBlocks()`.
    - `useEffect(() => { fetchBlocks(); }, [dateKey])`.

    Create `frontend/src/hooks/useWorkHours.ts` — GET/PUT `/api/v1/settings/work-hours`. Export `useWorkHours()` returning `{ workStart, workEnd, loading, save }` where `save(work_start: string, work_end: string)` PUTs the body and returns boolean ok; on mount GET populates workStart/workEnd ("HH:MM" strings).

    In `frontend/src/lib/agenda.ts`:
    - Import `ScheduledBlock` from "../types/plan".
    - Change `buildDayItems(tasks, events, dateKey, blocks?: ScheduledBlock[])` and `buildWeekAgenda(tasks, events, now, blocks?: ScheduledBlock[])` to accept an optional blocks param. `buildAgenda` may also accept it. For `buildWeekAgenda`, only inject blocks into the day whose dateKey matches the block's date_key (pass the full blocks list; filter inside buildDayItems by `b.date_key === dateKey`).
    - Inside `buildDayItems`, after building event items and before the timed sort, push a timed AgendaItem for each block where `b.date_key === dateKey`:
    ```typescript
    for (const b of (blocks ?? [])) {
      if (b.date_key !== dateKey) continue;
      const d = new Date(b.start_dt);
      const time = `${String(d.getHours()).padStart(2, "0")}:${String(d.getMinutes()).padStart(2, "0")}`;
      timedItems.push({
        id: `block-${b.id}`, title: b.title, time, priority: null,
        isEvent: false, isBlock: true, completed: false,
        blockId: b.id, conflict_with: b.conflict_with,
      });
    }
    ```
    (Use getHours()/getMinutes() to match existing event time rendering in this file.)

    In `frontend/src/components/AgendaItem.tsx`, after the priority badge / before or after the time label, render a "Planned" badge when `item.isBlock` and a conflict badge when `item.conflict_with` (RESEARCH §9 inline styles):
    ```tsx
    {item.isBlock && (
      <span style={{ fontSize: 11, background: "var(--accent)", color: "#fff", borderRadius: 4, padding: "1px 5px", flexShrink: 0 }}>Planned</span>
    )}
    {item.conflict_with && (
      <span style={{ fontSize: 11, background: "var(--destructive, #ef4444)", color: "#fff", borderRadius: 4, padding: "1px 5px", flexShrink: 0 }} title={`Conflicts with: ${item.conflict_with}`}>! {item.conflict_with}</span>
    )}
    ```
    For block items (isBlock && !taskId/!googleId), the checkbox toggle is a no-op — guard handleToggle / onToggle so a planned block without taskId does not attempt a PATCH. Keep events/tasks behavior unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/plan.ts` contains `export interface ScheduledBlock` with `conflict_with: string | null`
    - `frontend/src/types/task.ts` AgendaItem contains `isBlock?`, `conflict_with?`, `blockId?`
    - `frontend/src/hooks/usePlan.ts` contains `/api/v1/plan` and exports `propose`, `approve`, `replan`, `deleteBlock`; approve throws on status 409
    - `frontend/src/hooks/useWorkHours.ts` contains `/api/v1/settings/work-hours`
    - `frontend/src/lib/agenda.ts` `buildDayItems` accepts a `blocks` param and pushes items with `isBlock: true` filtered by `b.date_key === dateKey`
    - `frontend/src/components/AgendaItem.tsx` contains `item.isBlock` and `item.conflict_with`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Types, hooks, and agenda/AgendaItem block+staleness rendering compile cleanly; blocks fold into the agenda by date_key.</done>
</task>

<task type="auto">
  <name>Task 2: Organize page + Today integration + Settings work-hours card + nav tab + route</name>
  <read_first>
    - frontend/src/hooks/usePlan.ts + frontend/src/hooks/useWorkHours.ts (created Task 1)
    - frontend/src/types/plan.ts (created Task 1)
    - frontend/src/hooks/useTasks.ts (to map unplaced_task_ids → titles via loaded tasks)
    - frontend/src/pages/Today.tsx (current structure — add usePlan + pass blocks)
    - frontend/src/pages/Settings.tsx (CARD_STYLE, SECTION_LABEL_STYLE, btn-save — mirror Daily Brief card)
    - frontend/src/components/BottomNav.tsx (NavLink + tabStyle pattern; add 5th tab)
    - frontend/src/App.tsx (Routes — add /organize)
    - .planning/phases/10-day-auto-organize/10-RESEARCH.md (§9 Organize state machine, moveBlock, already-approved render; D-10/D-11/D-12/D-14)
  </read_first>
  <action>
    Create `frontend/src/pages/Organize.tsx`. Use `const todayKey = ...` computed as local YYYY-MM-DD (match agenda.ts toDateKey using getFullYear/getMonth/getDate, NOT toISOString which is UTC). Use `usePlan(todayKey)` and `useTasks()`.

    State machine (RESEARCH §9): `"loading" | "approved" | "proposing" | "editing" | "fully_booked" | "saving" | "done"`.
    - On mount, after usePlan loading resolves: if committed `blocks.length > 0` → state "approved" (D-10: load committed blocks, show approved state). Else call `propose(todayKey)`:
      - if result.fully_booked → state "fully_booked", render message "No free time today — your calendar is full." (D-12).
      - else set local editable `draftBlocks` from result.blocks, store `unplacedTaskIds = result.unplaced_task_ids`, state "editing".
    - "editing" render: list each draftBlock as a row with title, start/end time, and controls:
      - Remove button (drop from draftBlocks).
      - Up/Down buttons via array index swap `moveBlock(index, dir)` (RESEARCH §9 — NO drag-drop, D-14).
      - Adjust start time + duration: an editable time input for start and a number input for duration minutes; on change recompute that block's start_dt/end_dt (D-14 "adjust start/duration"). Keep it simple — local state only; submitted verbatim on approve.
      - A "Didn't fit" section listing titles for `unplacedTaskIds` mapped against `useTasks().tasks` (D-11). If empty, omit.
      - Approve button → setState "saving", disable button (prevents double-commit, RESEARCH Pitfall 3), call `approve(todayKey, draftBlocks)`; on success state "done"; on `Error("already_approved")` re-fetch blocks and show "approved" state.
    - "approved" render (D-10): "Plan approved for today." + list committed blocks (read-only) + a "Re-plan" button. Re-plan shows a confirm ("Re-plan will replace your approved plan. Continue?"), then calls `propose(todayKey)` fresh → goes to "editing" with a flag so Approve calls `replan(todayKey, draftBlocks)` instead of `approve` (RESEARCH Pitfall 7 / D-09).
    - "done" render: success message + link back to Today.
    Use the existing inline-style conventions (no CSS framework). Reuse page wrapper `<div className="page">` + `<h1 className="page-title">Organize</h1>`.

    In `frontend/src/components/BottomNav.tsx`: add a 5th NavLink to `/organize` between Goals and Settings (or after Tasks), label "Organize", icon `CalendarCheck` from lucide-react (add to the import). Mirror the existing NavLink style+isActive callback exactly.

    In `frontend/src/App.tsx`: `import Organize from "./pages/Organize";` and add `<Route path="/organize" element={<Organize />} />` inside Routes.

    In `frontend/src/pages/Today.tsx`: add `const todayKey = ...` (local date key matching agenda.ts), `const { blocks } = usePlan(todayKey);` alongside useTasks/useCalendarEvents. Pass `blocks` as the 4th arg: `buildWeekAgenda(tasks, events, new Date(), blocks)`. (buildWeekAgenda filters blocks to the matching day by date_key.) No other change to Today's render; the Planned + staleness badges come from AgendaItem (Task 1).

    In `frontend/src/pages/Settings.tsx`: add a new "Work Hours" `<section>` (mirror the Daily Brief card: SECTION_LABEL_STYLE label "Work Hours", CARD_STYLE container). Use `useWorkHours()`. Two `type="time"` inputs (start, end) bound to local state seeded from the hook, and a `className="btn-save"` button "Save Work Hours" calling `save(start, end)`. Include a small note: "Used to plan your day. Times follow the Pi's timezone." (RESEARCH §10 Open Question 1).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/pages/Organize.tsx` contains `usePlan`, `moveBlock`, a Remove control, an Approve button disabled while saving, a "Didn't fit" section, a fully-booked message string "No free time today", and a "Re-plan" button
    - `frontend/src/components/BottomNav.tsx` contains a NavLink `to="/organize"` and imports `CalendarCheck`
    - `frontend/src/App.tsx` contains `path="/organize"` and `import Organize`
    - `frontend/src/pages/Today.tsx` contains `usePlan` and passes blocks into `buildWeekAgenda(`
    - `frontend/src/pages/Settings.tsx` contains `useWorkHours` and a "Work Hours" section with two time inputs and a Save button
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Organize page renders the full propose/edit/approve + approved/re-plan + didn't-fit + fully-booked flow; Today shows planned blocks with staleness badges; Settings edits work hours; nav + route wired; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Organize touch flow + Today staleness on a phone browser</name>
  <what-built>
    The Organize page (propose → review/edit → approve, with up/down reorder tap buttons, remove, adjust start/duration, a Didn't-fit list, a fully-booked message, and an already-approved state with Re-plan), Today integration showing planned blocks with a per-block "conflicts with [event]" staleness badge, and a Work Hours card on Settings.
  </what-built>
  <how-to-verify>
    1. Ensure migration is applied on the dev/Pi DB: `cd backend && alembic upgrade head` (creates scheduled_blocks + work columns). Start backend + `cd frontend && npm run dev`.
    2. On a phone browser (primary device), open Settings → Work Hours, set e.g. 09:00–18:00, Save.
    3. Create a few pending tasks (mix of due-today/overdue/no-due, varying priority, some with estimated_minutes) and at least one timed Google Calendar event for today so a gap exists.
    4. Open the Organize tab. Confirm a proposal appears; blocks fit only free time around the event; verify Up/Down reorder works by tap, Remove drops a block, and adjusting a block's start/duration updates it. Confirm any non-fitting task shows under "Didn't fit".
    5. Tap Approve. Confirm it commits (button disables during submit) and the page shows the approved state with a Re-plan button.
    6. Open Today. Confirm the approved blocks appear in today's agenda with a "Planned" badge.
    7. Add a Google Calendar event that overlaps an approved block (wait for sync or trigger it). Reload Today. Confirm the conflicting block shows a per-block "conflicts with [event title]" badge (D-13).
    8. Back on Organize, tap Re-plan, confirm the replace-confirm prompt, regenerate, and re-approve — confirm no duplicate/409 error (replan path).
    9. (Optional) Block out the whole work window with events, re-propose, confirm the "No free time today — your calendar is full." message (D-12).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (layout, touch targets, badge wording, reorder/adjust behavior).</resume-signal>
  <action>This is a human-verify checkpoint — no code changes. Before pausing, the executor MUST run `cd backend && alembic upgrade head` to apply migration 0009 (creates scheduled_blocks + the four app_settings work columns on the dev DB) and confirm `cd frontend && npm run build` succeeded, then present the how-to-verify steps to the user and wait for the resume signal.</action>
  <verify>
    <automated>cd backend && alembic upgrade head && python -c "import sqlite3,glob,sys; print('migration applied')"</automated>
  </verify>
  <done>User has exercised the Organize flow on a phone browser (propose, reorder, remove, adjust, approve, re-plan, staleness badge, fully-booked) and replied "approved" or filed issues.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` (typecheck clean)
- `cd frontend && npm run build` (production build succeeds)
- `cd backend && alembic upgrade head` applies migration 0009 cleanly on the dev DB
- Human checkpoint confirms the touch flow + staleness badge on a phone browser
</verification>

<success_criteria>
- Organize page delivers the full review/edit/approve + approved/re-plan + didn't-fit + fully-booked flow with tap-based reorder (no drag)
- Today renders approved blocks with per-block staleness badges
- Settings edits the work window; nav tab + route wired; build green; human verification passed
</success_criteria>

<output>
After completion, create `.planning/phases/10-day-auto-organize/10-04-SUMMARY.md`
</output>
