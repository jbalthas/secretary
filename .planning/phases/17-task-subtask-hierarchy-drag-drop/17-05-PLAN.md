---
phase: 17-task-subtask-hierarchy-drag-drop
plan: 05
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - frontend/src/pages/Tasks.tsx
  - frontend/src/components/TaskCard.tsx
  - frontend/src/components/SubtaskSelect.tsx
  - frontend/src/components/TaskDrawer.tsx
autonomous: false
requirements: [HIER-02, HIER-03, HIER-04]
must_haves:
  truths:
    - "The Tasks page groups nested children beneath their parent card, indented, expanded by default, with a progress badge"
    - "Dragging a card onto another card nests it; dragging a nested card into open grid space un-nests it"
    - "The task edit drawer has a 'Subtask of' dropdown that can set or clear a task's parent without dragging"
    - "The Subtask-of dropdown excludes the task itself and any task that already has a parent"
  artifacts:
    - path: "frontend/src/components/SubtaskSelect.tsx"
      provides: "Subtask-of dropdown component, mirrors GoalSelect.tsx exactly"
    - path: "frontend/src/pages/Tasks.tsx"
      provides: "groupTasksByParent-based card grouping + DndContext wiring"
  key_links:
    - from: "frontend/src/pages/Tasks.tsx"
      to: "frontend/src/lib/taskHierarchy.ts"
      via: "groupTasksByParent(sorted)"
      pattern: "groupTasksByParent"
    - from: "frontend/src/components/TaskDrawer.tsx"
      to: "frontend/src/components/SubtaskSelect.tsx"
      via: "parent_task_id state wired into handleSave's TaskCreate body"
      pattern: "SubtaskSelect"
---

<objective>
Bring task/subtask nesting (D-01, D-02, D-06 through D-11 from `17-CONTEXT.md`) to the Tasks page card grid and to the task edit drawer's manual "Subtask of" dropdown — the two remaining surfaces per D-02 and D-11, alongside the Today timeline delivered in Plan 17-04.

Scope note (resolves Pitfall 2 from `17-RESEARCH.md`): the Tasks page is a card GRID, not a linear list, so "drop in the gap to reorder" (D-03) does not apply here — the grid keeps its existing due-date/priority auto-sort. Only the "nest" and "un-nest" drag gestures are implemented on this page; reordering is Today-timeline-specific tooling that this plan does not replicate (see Plan 17-04's dragIntent scope note — "before"/"after" intents are no-ops there too, so there is no asymmetry being introduced).

Purpose: Complete D-02's "visible and editable on both" requirement, and give the user a non-drag path (D-11) to set/clear nesting from any device.
Output: Grouped, nestable Tasks-page card grid; a reusable `SubtaskSelect` component; TaskDrawer wired to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-CONTEXT.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-RESEARCH.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-UI-SPEC.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-VALIDATION.md

<interfaces>
From Plan 17-02 (already landed — Wave 1): `frontend/src/lib/taskHierarchy.ts` exports `groupTasksByParent(tasks: Task[]): { parents: Task[]; childrenByParentId: Map<number, Task[]> }` and `subtaskProgress(children: Task[]): { done: number; total: number }`. `Task`/`TaskCreate` types carry `parent_task_id?: number | null`.

From Plan 17-01 (already landed — Wave 1): `PATCH /api/v1/tasks/{id}` and `POST /api/v1/tasks/` both accept `parent_task_id: number | null`, return 422 on invalid nesting (already-nested parent, or nesting a task that has its own children).

Existing `frontend/src/components/GoalSelect.tsx` (full file, 25 lines) — the exact pattern `SubtaskSelect.tsx` must mirror:
```tsx
import type { Goal } from "../types/goal";

interface Props {
  goals: Goal[];
  value: number | null;
  onChange: (goalId: number | null) => void;
}

export default function GoalSelect({ goals, value, onChange }: Props) {
  const active = goals.filter((g) => g.status === "active");
  return (
    <select value={value ?? ""} onChange={(e) => onChange(e.target.value ? Number(e.target.value) : null)}>
      <option value="">No goal</option>
      {active.map((g) => (
        <option key={g.id} value={g.id}>{g.title}</option>
      ))}
    </select>
  );
}
```

Current `frontend/src/pages/Tasks.tsx` (full file, 266 lines) — the `.tasks-card-grid` render block is at lines 229-243 (`sorted.map((task) => <TaskCard .../>)`). `sortTasks`/`filtered`/`sorted` are computed at lines 24-61.

Current `frontend/src/components/TaskCard.tsx` (full file, 81 lines) — single card: thumb icon, title, meta row, checkbox; `onClick={() => onEdit(task)}` on the whole card (drag handle must not conflict with this click-to-edit behavior).

Current `frontend/src/components/TaskDrawer.tsx` (full file, 355 lines) — `GoalSelect` is wired at lines 179-182; `handleSave` builds the `TaskCreate` body at lines 111-127; `task` prop (the task being edited, or `null` for a new task) is available throughout.

CSS classes available from Plan 17-03 (already landed): `.tasks-card-children`, `.tasks-card--nested`, `.tasks-card-progress-badge`, `.tasks-card-drag-handle`, `.tasks-card--nest-target`, `.tasks-card--dragging`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tasks.tsx / TaskCard.tsx — grouped nested rendering + drag-to-nest/un-nest</name>
  <files>frontend/src/pages/Tasks.tsx, frontend/src/components/TaskCard.tsx</files>
  <read_first>
    frontend/src/pages/Tasks.tsx (full file, 266 lines)
    frontend/src/components/TaskCard.tsx (full file, 81 lines)
    frontend/src/lib/taskHierarchy.ts (groupTasksByParent, subtaskProgress signatures)
  </read_first>
  <action>
    1. In `frontend/src/components/TaskCard.tsx`:
       - Add new optional props: `childTasks?: Task[]`, `isNested?: boolean` (renders with `.tasks-card--nested` modifier, no thumb icon needed at nested size — keep it simple, reuse existing thumb), `dragHandleProps?: { listeners: any; attributes: any; setActivatorNodeRef: (el: HTMLElement | null) => void }`, `isDragging?: boolean`, `isNestTarget?: boolean`.
       - Import `GripVertical` from `lucide-react` (alongside existing `Bell, Repeat` import).
       - Add a drag handle button inside the card, positioned so it doesn't trigger `onClick={() => onEdit(task)}` (call `e.stopPropagation()` in the handle's own `onPointerDown`/`onClick`, matching the existing `stopPropagation()` pattern already used on the checkbox at line 27 and 75): `<span className="tasks-card-drag-handle" {...dragHandleProps?.listeners} {...dragHandleProps?.attributes} ref={dragHandleProps?.setActivatorNodeRef} onClick={(e) => e.stopPropagation()} aria-label={\`Drag to reorder or nest "${task.title}"\`}><GripVertical size={16} aria-hidden="true" /></span>`.
       - Append `" tasks-card--nested"` to the card's className when `isNested`, `" tasks-card--dragging"` when `isDragging`, `" tasks-card--nest-target"` when `isNestTarget`.
       - When `childTasks && childTasks.length > 0`, render a progress badge next to the title using `subtaskProgress(childTasks)`: `<span className="tasks-card-progress-badge" aria-label={\`${done} of ${total} subtasks complete\`}>{done}/{total}</span>`.
       - When `childTasks && childTasks.length > 0`, render `<div className="tasks-card-children">{childTasks.map((child) => <TaskCard key={child.id} task={child} goals={goals} onEdit={onEdit} onToggle={onToggle} isNested dragHandleProps={...} isDragging={...} isNestTarget={...} />)}</div>` immediately after the card's own body (children are themselves draggable, per D-10 symmetry — a nested card can be dragged back out to un-nest).

    2. In `frontend/src/pages/Tasks.tsx`:
       - Import `DndContext, PointerSensor, useSensor, useSensors, type DragEndEvent` from `@dnd-kit/core` and `groupTasksByParent` from `../lib/taskHierarchy`.
       - Replace the `sorted.map((task) => <TaskCard .../>)` block (lines 230-241) with: compute `const { parents, childrenByParentId } = groupTasksByParent(sorted);` then render `parents.map((task) => <TaskCard key={task.id} task={task} goals={goals} childTasks={childrenByParentId.get(task.id)} onEdit={...} onToggle={...} .../>)`. A task that is itself a child (has `parent_task_id` set) is excluded from `parents` automatically by `groupTasksByParent` — it only renders nested under its parent's card, so it must NOT also be filtered out of `sorted`/`filtered` upstream (leave the existing filter/sort logic untouched; grouping happens only at render time, after sort).
       - Wrap the `.tasks-card-grid` container in a `<DndContext sensors={...} onDragEnd={handleDragEnd}>` where `handleDragEnd` implements: `if (!event.over) { /* dropped in open grid space */ const dragged = tasks.find(t => t.id === event.active.id); if (dragged?.parent_task_id != null) await patchTask(dragged.id, { parent_task_id: null }); return; }` else `{ const targetId = Number(event.over.id); const draggedId = Number(event.active.id); if (targetId !== draggedId) await patchTask(draggedId, { parent_task_id: targetId }); }`. Use `useSensor(PointerSensor, { activationConstraint: { distance: 4 } })` (matches `17-RESEARCH.md`'s recommended activation distance, preventing accidental drags from a simple click-to-edit tap).
       - Wrap any 422 rejection from `patchTask` in a try/catch inside `handleDragEnd`; on failure, surface an inline message — reuse the existing `tasks-card-error` CSS class (already defined in `styles.css` from the original card redesign) by tracking a local `dragError: string | null` state in `Tasks.tsx` and rendering it near the top of the grid (e.g. directly above `.tasks-card-grid`) when set; clear it at the start of each new `handleDragEnd` call.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "groupTasksByParent\|DndContext" frontend/src/pages/Tasks.tsx` shows both used
    - `grep -n "tasks-card-drag-handle\|tasks-card-progress-badge\|tasks-card-children" frontend/src/components/TaskCard.tsx` shows all three present
    - `grep -n "aria-label.*Drag to reorder or nest" frontend/src/components/TaskCard.tsx` matches the UI-SPEC's exact accessibility copy
    - `cd frontend && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>Tasks page groups nested children under parent cards (indented, badge, expanded by default); drag-to-nest and drag-to-unnest work via DndContext; no reorder-by-drag is implemented on this page (documented scope decision).</done>
</task>

<task type="auto">
  <name>Task 2: SubtaskSelect component + TaskDrawer wiring</name>
  <files>frontend/src/components/SubtaskSelect.tsx, frontend/src/components/TaskDrawer.tsx</files>
  <read_first>
    frontend/src/components/GoalSelect.tsx (full file, 25 lines — exact pattern to mirror)
    frontend/src/components/TaskDrawer.tsx (full file, 355 lines — especially the `goalId` state at line 71, the `GoalSelect` field at lines 179-182, and `handleSave` at lines 111-129)
  </read_first>
  <action>
    1. Create `frontend/src/components/SubtaskSelect.tsx`:
    ```tsx
    import type { Task } from "../types/task";

    interface Props {
      tasks: Task[];
      currentTaskId: number | null;
      value: number | null;
      onChange: (parentTaskId: number | null) => void;
    }

    export default function SubtaskSelect({ tasks, currentTaskId, value, onChange }: Props) {
      const options = tasks.filter(
        (t) => t.id !== currentTaskId && t.parent_task_id == null
      );
      return (
        <select
          value={value ?? ""}
          onChange={(e) => onChange(e.target.value ? Number(e.target.value) : null)}
        >
          <option value="">No parent (top-level task)</option>
          {options.map((t) => (
            <option key={t.id} value={t.id}>{t.title}</option>
          ))}
        </select>
      );
    }
    ```
    Exclusion rule matches `17-UI-SPEC.md`'s copywriting contract exactly: excludes (a) the task being edited itself (`currentTaskId`), (b) any task that already has a parent (`t.parent_task_id == null` filter — a child cannot become a parent, per D-01). `currentTaskId` is `null` when creating a brand-new task (nothing to exclude by id).

    2. In `frontend/src/components/TaskDrawer.tsx`:
       - Add `import SubtaskSelect from "./SubtaskSelect";` and change `Props` to accept `tasks: Task[]` (full task list, needed to populate the dropdown — passed down from `Tasks.tsx`, which already has `tasks` in scope from `useTasks()`).
       - Add `const [parentTaskId, setParentTaskId] = useState<number | null>(null);` alongside the existing `goalId` state (line 71).
       - In the `useEffect` that resets form state on open (lines 84-109), add `setParentTaskId(task?.parent_task_id ?? null);`.
       - Add a new `<div className="drawer-field">` block directly after the existing Goal field (after line 182):
       ```tsx
       <div className="drawer-field">
         <label>Subtask of</label>
         <SubtaskSelect
           tasks={tasks}
           currentTaskId={task?.id ?? null}
           value={parentTaskId}
           onChange={setParentTaskId}
         />
       </div>
       ```
       - In `handleSave` (lines 111-129), add `parent_task_id: parentTaskId,` to the `TaskCreate` body object.
    3. Update the `Tasks.tsx` `<TaskDrawer .../>` invocation (near the end of the file) to pass the new required `tasks={tasks}` prop.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/SubtaskSelect.tsx` exists, exports a default component matching `GoalSelect.tsx`'s prop-shape convention
    - `grep -n "No parent (top-level task)" frontend/src/components/SubtaskSelect.tsx` matches the UI-SPEC's exact copy
    - `grep -n "parent_task_id == null" frontend/src/components/SubtaskSelect.tsx` confirms the "already-has-a-parent" exclusion
    - `grep -n "SubtaskSelect" frontend/src/components/TaskDrawer.tsx` shows it imported and rendered
    - `grep -n "tasks={tasks}" frontend/src/pages/Tasks.tsx` shows the new prop passed to `TaskDrawer`
    - `cd frontend && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>SubtaskSelect mirrors GoalSelect's exact pattern; TaskDrawer's "Subtask of" field sets/clears parent_task_id on save without requiring a drag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Tasks-page nesting drag and TaskDrawer Subtask-of field</name>
  <what-built>
    Nested subtask cards on the Tasks page (drag-to-nest, drag-to-unnest, progress badge, indentation) and a "Subtask of" dropdown in the task edit drawer for setting/clearing a parent without dragging.
  </what-built>
  <how-to-verify>
    1. Open the app, go to Tasks, drill into any group (or "All groups" → Unsorted) so the card grid is visible.
    2. Press-and-hold the grip icon on one card and drag it onto another card's body — confirm it renders as a nested, indented card beneath the target, and the target shows a progress badge (e.g. "0/1").
    3. Drag the nested card out into empty grid space (not onto any card) — confirm it returns to being a standalone top-level card.
    4. Tap a card to open its edit drawer (not the grip icon) — confirm the drawer still opens normally (drag handle doesn't interfere with tap-to-edit).
    5. In the drawer, find the "Subtask of" dropdown — confirm it lists other tasks, excluding the task itself and excluding tasks that already have a parent. Select a parent, save — confirm the task now renders nested on the grid. Reopen the drawer, select "No parent (top-level task)", save — confirm it returns to top-level.
  </how-to-verify>
  <action>Pause for the user to manually verify the Tasks-page drag-nest flow and the TaskDrawer Subtask-of dropdown per the steps in how-to-verify. Do not proceed until approved.</action>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
`cd frontend && npx tsc -b --noEmit && npx vitest run` — confirms no type errors and no regressions.
</verification>

<success_criteria>
1. Tasks page groups nested children under their parent card, indented, expanded by default, with a progress badge.
2. Drag-to-nest and drag-to-unnest work via the card grid's DndContext; no manual reorder is introduced (documented scope decision).
3. TaskDrawer's "Subtask of" dropdown sets/clears parent_task_id, correctly excluding invalid options.
4. Human verification checkpoint passed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-task-subtask-hierarchy-drag-drop/17-05-SUMMARY.md`
</output>
