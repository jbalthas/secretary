---
phase: 17-task-subtask-hierarchy-drag-drop
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - frontend/src/hooks/usePlan.ts
  - frontend/src/components/TodayTimeline.tsx
  - frontend/src/components/AgendaItem.tsx
  - frontend/src/pages/Today.tsx
autonomous: false
requirements: [HIER-01, HIER-03, HIER-05]
must_haves:
  truths:
    - "Dragging a task or planned block onto the center of another task's row on the Today timeline nests it as that task's subtask"
    - "Dragging a nested item into empty timeline space clears its parent (un-nests it)"
    - "Dropping in the gap at a row's top/bottom edge reorders the flat top-level list immediately (in-memory/session-level, per D-03 — not persisted to the backend or across reloads)"
    - "Calendar events are never draggable and never a valid nest target"
    - "A parent row shows a live '{done}/{total}' subtask progress badge; nested children render indented and expanded by default"
    - "A rejected nest (422 from the backend) shows an inline error and does not change the UI state"
  artifacts:
    - path: "frontend/src/components/TodayTimeline.tsx"
      provides: "DndContext wiring, groupAgendaItemsByParent-based nested rendering, onDragEnd nest/unnest resolution"
    - path: "frontend/src/components/AgendaItem.tsx"
      provides: "drag handle, indentation, progress badge, collapse toggle, nest-target/dragging visual states"
    - path: "frontend/src/hooks/usePlan.ts"
      provides: "patchBlockParent(id, parentTaskId) function"
      exports: ["patchBlockParent"]
  key_links:
    - from: "frontend/src/components/TodayTimeline.tsx"
      to: "frontend/src/lib/taskHierarchy.ts"
      via: "groupAgendaItemsByParent(items) / moveInOrder(...) / applyManualOrder(...)"
      pattern: "groupAgendaItemsByParent|moveInOrder|applyManualOrder"
    - from: "frontend/src/components/TodayTimeline.tsx"
      to: "frontend/src/lib/dragIntent.ts"
      via: "resolveDropIntent(pointerY, rect.top, rect.height)"
      pattern: "resolveDropIntent"
    - from: "frontend/src/pages/Today.tsx"
      to: "frontend/src/hooks/usePlan.ts"
      via: "patchBlockParent used inside handleSetParent"
      pattern: "patchBlockParent"
---

<objective>
Wire drag-and-drop task/subtask nesting into the "Your day" Today timeline (D-01 through D-05, D-06, D-07, D-09, D-10 from `17-CONTEXT.md`), using the types/libs from Plan 17-02 and the CSS from Plan 17-03 against the validated backend API from Plan 17-01.

Scope note: this plan touches `TodayTimeline.tsx`/`AgendaItem.tsx` (the "Your day" linear timeline) only — NOT the "Later this week" `DaySection` view in `Today.tsx`, which reuses `AgendaItem` but never groups by parent and has no `DndContext`. Per D-02, nesting must be visible/editable on "the Today timeline AND the Tasks list page" — the week view is out of scope, matching `17-CONTEXT.md`'s canonical_refs which name `TodayTimeline.tsx`/`AgendaItem.tsx` specifically.

Purpose: Deliver the phase's headline interaction — dragging grocery-list-style tasks onto an errand task to nest them — on the primary "Your day" surface.
Output: Drag-to-nest, drag-to-unnest, progress badge, indentation/collapse, all wired end-to-end against the live backend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-CONTEXT.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-RESEARCH.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-UI-SPEC.md
@.planning/phases/17-task-subtask-hierarchy-drag-drop/17-VALIDATION.md

<interfaces>
From Plan 17-02 (already landed — Wave 1): `frontend/src/lib/taskHierarchy.ts` exports `groupAgendaItemsByParent(items: AgendaItem[]): { topLevel: AgendaItem[]; childrenByTaskId: Map<number, AgendaItem[]> }`, `subtaskProgress(children)`, `moveInOrder<T extends string|number>(currentOrder: T[], draggedId: T, targetId: T, position: "before"|"after"): T[]`, and `applyManualOrder<T extends {id: string|number}>(items: T[], orderIds: Array<string|number>): T[]`. `frontend/src/lib/dragIntent.ts` exports `resolveDropIntent(pointerY, rectTop, rectHeight): "nest" | "before" | "after"`. `AgendaItem` type now carries `parentTaskId?: number | null`.

**D-03 is a locked decision, not discretionary** (re-confirmed in this revision): "Drop directly on the center of a row = nest as subtask. Drop in the gap between rows = reorder in the flat list." Only the *persistence mechanism* for reordering was left to Claude's discretion (CONTEXT.md: "Whether reordering ... requires a persisted order/position column or can stay implicit"). This plan implements the "stay implicit" option: `moveInOrder`/`applyManualOrder` drive an in-memory, session-level manual order held in `TodayTimeline`'s local state — reordering visibly happens and survives normal re-renders/refetches within the session, but is not written to the backend and does not survive a full page reload.

From Plan 17-01 (already landed — Wave 1): `PATCH /api/v1/tasks/{id}` accepts `parent_task_id: number | null`, returns 422 on invalid nesting. `PATCH /api/v1/plan/blocks/{id}` accepts `parent_task_id: number | null` (independently of `completed`), returns 422 on invalid nesting.

Current `frontend/src/hooks/usePlan.ts` (full file, 74 lines) — `patchBlock(id: number, completed: boolean)` sends `{completed}` only; this plan adds a new sibling function, does not modify `patchBlock`.

Current `frontend/src/components/TodayTimeline.tsx` (full file, 34 lines) — flat `items.map()` over `markTimeline(items, nowHHMM)`, each row rendered as `<div className="timeline-row timeline-row--{phase}">`.

Current `frontend/src/components/AgendaItem.tsx` (full file, 102 lines) — single row: checkbox, title, priority badge, "Planned" badge, conflict badge, time. Inline `style` objects, not CSS classes, for most of the row (only `className="task-checkbox"` and `agenda-item--event` used today).

Current `frontend/src/pages/Today.tsx` (full file, 258 lines) — `TodayTimeline` is invoked at line 242 with only `items`, `nowHHMM`, `onToggle`; `handleToggle` (lines 151-159) is the existing pattern to mirror for the new `handleSetParent`.

CSS classes available from Plan 17-03 (already landed): `.timeline-children`, `.subtask-progress-badge`, `.drag-handle`, `.timeline-row--dragging`, `.timeline-row--nest-target`, `.timeline-row--insertion-before`/`--insertion-after`, `.subtask-collapse-toggle`, `.agenda-item-error`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePlan.patchBlockParent + TodayTimeline DndContext wiring</name>
  <files>frontend/src/hooks/usePlan.ts, frontend/src/components/TodayTimeline.tsx, frontend/src/pages/Today.tsx</files>
  <read_first>
    frontend/src/hooks/usePlan.ts (full file, 74 lines)
    frontend/src/components/TodayTimeline.tsx (full file, 34 lines)
    frontend/src/pages/Today.tsx (full file, 258 lines — especially handleToggle lines 151-159 and the TodayTimeline invocation at line 242)
    frontend/src/lib/taskHierarchy.ts (groupAgendaItemsByParent signature)
    frontend/src/lib/dragIntent.ts (resolveDropIntent signature)
  </read_first>
  <behavior>
    - Dropping a draggable task/block item onto the body of a task-type row whose `taskId` has no `parentTaskId` of its own calls `onSetParent(draggedItem, targetTaskId)`.
    - Dropping a nested item outside any row (empty timeline space, i.e. `over` is null in dnd-kit's `onDragEnd`) calls `onSetParent(draggedItem, null)`.
    - Calendar event items (`isEvent: true`) are never wrapped in a draggable, and are never valid drop targets for nesting.
    - `"before"`/`"after"` drop intents (per `resolveDropIntent`), when both the dragged item and the target are top-level items, call `setManualOrderIds(moveInOrder(...))` — the rendered top-level order updates immediately to reflect the reorder (D-03's locked "reorder in the flat list" behavior, implemented in-memory/session-level per Claude's discretion over persistence only — no backend call is made).
    - `"before"`/`"after"` drop intents where either side is a nested child are a no-op (D-03's reorder applies to "the flat list" — nested children are not part of the flat top-level list; reordering within a child group is out of scope).
  </behavior>
  <action>
    1. In `frontend/src/hooks/usePlan.ts`, add a new exported function alongside `patchBlock` (same file, same `API` constant, same error-throwing convention):
    ```typescript
    async function patchBlockParent(id: number, parentTaskId: number | null) {
      const res = await fetch(`${API}/blocks/${id}`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ parent_task_id: parentTaskId }),
      });
      if (!res.ok) throw new Error("Could not update plan block parent");
      await fetchBlocks();
    }
    ```
    Add `patchBlockParent` to the hook's returned object (alongside `patchBlock`).

    2. Rewrite `frontend/src/components/TodayTimeline.tsx` to:
       - Accept a new prop `onSetParent: (item: AgendaItemType, parentTaskId: number | null) => Promise<void>`.
       - Import `DndContext`, `PointerSensor`, `useSensor`, `useSensors`, `type DragEndEvent`, `type DragOverEvent` from `@dnd-kit/core`, and `moveInOrder`, `applyManualOrder` from `../lib/taskHierarchy` (alongside the already-planned `groupAgendaItemsByParent`).
       - **Final ownership design (children rendering lives in `AgendaItem`, not here):** `TodayTimeline` computes `{ topLevel, childrenByTaskId }` via `groupAgendaItemsByParent(items)`, applies the manual order via `applyManualOrder(topLevel, manualOrderIds)`, and renders one `<AgendaItem>` per resulting top-level item, passing `childItems={childrenByTaskId.get(item.taskId ?? -1)}` as a prop. `TodayTimeline` itself does NOT render a `.timeline-children` container — that rendering, and the collapse/expand toggle, are entirely `AgendaItem`'s responsibility (Task 2 of this plan implements it). `TodayTimeline` only supplies the grouped data and the drag/drop plumbing.
       - Track local drag-hover state (`overId: string | null`, `dropIntent: "nest" | "before" | "after" | null`) via `useState`, updated in `onDragOver` using `resolveDropIntent(event.activatorEvent... )` — compute `pointerY` from the drag event's collision rect (`event.over?.rect` gives `{top, height}`; dnd-kit's `DragOverEvent` exposes the active pointer via `event.delta`/`event.active.rect.current.translated` — use `event.over.rect.top` and `event.over.rect.height` together with `event.active.rect.current.translated?.top` as a proxy for `pointerY`, since dnd-kit does not expose a raw pointer-Y coordinate directly on the event; document this approximation inline with a comment).
       - Track local manual-order state: `const [manualOrderIds, setManualOrderIds] = useState<string[]>([]);` — an array of top-level `AgendaItem.id` strings representing the current session's manual reorder override (D-03's "reorder in the flat list", implemented in-memory per Claude's discretion over persistence only — this state is intentionally not sent to the backend and resets on page reload).
       - Only make a row draggable/droppable if `!item.isEvent` (D-04 — events are never draggable or nestable). Only make a row a valid *nest target* if it represents a Task (`item.taskId != null && !item.isBlock`) and it has no `parentTaskId` of its own (one-level rule mirrors backend validation, gives instant UI feedback before the round-trip).
       - In `onDragEnd`:
         - If `event.over == null`: call `onSetParent(draggedItem, null)` (un-nest, D-10) — but only if the dragged item currently has a non-null `parentTaskId` (otherwise it's already top-level, no-op).
         - Else if `event.over` is a valid nest target and `dropIntent === "nest"`: call `onSetParent(draggedItem, targetTaskId)`.
         - Else if `dropIntent` is `"before"` or `"after"` and both the dragged item and `event.over`'s item are top-level (neither has a `parentTaskId`): compute `const baseOrder = manualOrderIds.length > 0 ? manualOrderIds : topLevel.map((i) => i.id);` then `setManualOrderIds(moveInOrder(baseOrder, draggedItem.id, targetItem.id, dropIntent));` — this is a pure client-side reorder, no `onSetParent` call, no backend round-trip (D-03's reorder gesture, "stay implicit" per discretion).
         - Else (dropIntent is `"before"`/`"after"` but either side is a nested child): no-op.
       - Wrap error handling: if `onSetParent` throws (backend 422), surface a per-row error message (see Task 2 — `AgendaItem` needs an `errorMessage` prop for this) rather than letting it propagate unhandled.
       - Preserve the existing `if (items.length === 0) return <p className="timeline-empty">...` early return, applied to the pre-grouping `items` array.

    3. In `frontend/src/pages/Today.tsx`:
       - Add `patchBlockParent` to the `usePlan(todayKey)` destructuring (line 83).
       - Add a new handler after `handleToggle` (after line 159):
       ```typescript
       async function handleSetParent(item: AgendaItemType, parentTaskId: number | null) {
         if (item.isBlock && item.blockId != null) {
           await patchBlockParent(item.blockId, parentTaskId);
         } else if (item.taskId != null) {
           await patchTask(item.taskId, { parent_task_id: parentTaskId });
         }
       }
       ```
       - Pass `onSetParent={handleSetParent}` to the `<TodayTimeline .../>` invocation (line 242).
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "patchBlockParent" frontend/src/hooks/usePlan.ts` shows the new function defined and returned
    - `grep -n "groupAgendaItemsByParent\|resolveDropIntent\|DndContext" frontend/src/components/TodayTimeline.tsx` shows all three used
    - `grep -n "moveInOrder\|applyManualOrder\|manualOrderIds" frontend/src/components/TodayTimeline.tsx` shows all three used — confirms D-03's reorder gesture is wired, not a no-op
    - `grep -n "isEvent" frontend/src/components/TodayTimeline.tsx` shows event items excluded from drag/drop eligibility
    - `grep -n "handleSetParent" frontend/src/pages/Today.tsx` shows the new handler defined and passed to `TodayTimeline`
    - `grep -n "timeline-children" frontend/src/components/TodayTimeline.tsx` returns NO matches — confirms `TodayTimeline` does not render the children container itself (that is `AgendaItem`'s responsibility per Task 2)
    - `cd frontend && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>TodayTimeline groups items by parent, applies the manual reorder to top-level items, wires DndContext with nest/unnest/reorder resolution, excludes calendar events from drag entirely, and calls the correct patch function (task vs. block) based on item type. Reordering (before/after intent) updates local state immediately and is visible in the render order.</done>
</task>

<task type="auto">
  <name>Task 2: AgendaItem — drag handle, indent, progress badge, collapse, error state</name>
  <files>frontend/src/components/AgendaItem.tsx</files>
  <read_first>
    frontend/src/components/AgendaItem.tsx (full file, 102 lines)
    frontend/src/lib/taskHierarchy.ts (subtaskProgress signature)
  </read_first>
  <action>
    Extend `AgendaItem.tsx`'s props and rendering:
    1. Add new optional props: `childItems?: AgendaItemType[]` (this item's nested children, if any — undefined/empty means no badge/no children container), `dragHandleProps?: { listeners: any; attributes: any; setActivatorNodeRef: (el: HTMLElement | null) => void }` (from `useDraggable`, wired by the parent `TodayTimeline`), `isDragging?: boolean`, `isNestTarget?: boolean`, `errorMessage?: string | null`.
    2. Import `GripVertical`, `ChevronDown`, `ChevronRight` from `lucide-react` (matching the existing `lucide-react` import already present for other components in this codebase, e.g. `TaskDrawer.tsx`).
    3. Render a drag handle (`<span className="drag-handle" {...dragHandleProps?.listeners} {...dragHandleProps?.attributes} ref={dragHandleProps?.setActivatorNodeRef} aria-label={`Drag to reorder or nest "${item.title}"`}><GripVertical size={16} aria-hidden="true" /></span>`) at the start of the row, but ONLY when `!item.isEvent` (events never get a drag handle, per D-04).
    4. When `childItems && childItems.length > 0`, render a subtask progress badge after the title using `subtaskProgress(childItems)`:
    ```tsx
    {childItems && childItems.length > 0 && (() => {
      const { done, total } = subtaskProgress(childItems);
      return (
        <span className="subtask-progress-badge" aria-label={`${done} of ${total} subtasks complete`}>
          {done}/{total}
        </span>
      );
    })()}
    ```
    5. Apply `className` modifiers to the row's outer `<div>`: append `" timeline-row--dragging"` when `isDragging`, append `" timeline-row--nest-target"` when `isNestTarget`.
    6. When `errorMessage` is set, render `<div className="agenda-item-error">{errorMessage}</div>` immediately after the row.
    7. Render nested children directly inside `AgendaItem` (this IS the final design — `TodayTimeline`, per Task 1, only passes `childItems` down and never renders `.timeline-children` itself): add local `const [collapsed, setCollapsed] = useState(false);` (defaults expanded, per D-09). When `childItems && childItems.length > 0`, render a `<button className="subtask-collapse-toggle" aria-label={collapsed ? \`Expand subtasks of "${item.title}"\` : \`Collapse subtasks of "${item.title}"\`}>` next to the progress badge that toggles `collapsed`, and render `{!collapsed && (<div className="timeline-children">{childItems.map((child) => <AgendaItem key={child.id} item={child} onToggle={onToggle} onSetParent={onSetParent} dragHandleProps={/* wired by TodayTimeline per child, same as top-level rows */} isDragging={/* per child */} isNestTarget={false} errorMessage={/* per child */} />)}</div>)}`. Children ARE draggable too (a child can itself be dragged back out to unnest, per D-10) — `TodayTimeline` must wire `useDraggable`/`useDroppable` for child items exactly as it does for top-level items (same drag-handle wiring, same `onDragEnd` un-nest path when dropped outside any row), so pass the same `dragHandleProps`-shaped object down for each child. Children are never valid *nest targets* themselves (one-level rule) — always pass `isNestTarget={false}` for a child row's own `isNestTarget` prop, regardless of drag-hover state.
  </action>
  <verify>
    <automated>cd frontend && npx tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GripVertical\|subtask-progress-badge\|timeline-children\|agenda-item-error" frontend/src/components/AgendaItem.tsx` shows all four present
    - `grep -n "aria-label.*Drag to reorder or nest" frontend/src/components/AgendaItem.tsx` matches the UI-SPEC's exact accessibility copy
    - `grep -n "aria-label.*subtasks complete" frontend/src/components/AgendaItem.tsx` matches the UI-SPEC's exact badge aria-label pattern
    - `cd frontend && npx tsc -b --noEmit` exits 0
  </acceptance_criteria>
  <done>AgendaItem renders a drag handle (never for events), a neutral-styled subtask progress badge, expanded-by-default nested children with a manual collapse toggle, and inline error text on rejected nests — matching 17-UI-SPEC.md's copy and color contract exactly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Today timeline drag-nest/un-nest, progress badge, and event exclusion</name>
  <what-built>
    Drag-and-drop task/subtask nesting on the Today tab's "Your day" timeline: drag a task or planned block onto another task's row to nest it as a subtask; drag a nested item into empty space to un-nest it; parent rows show a live "done/total" progress badge; nested children render indented and expanded by default; calendar events are never draggable.
  </what-built>
  <how-to-verify>
    1. Open the app, go to Today. Create 2-3 quick tasks due today (e.g. "Go to Sam's Club", "Buy chicken", "Buy oatmeal") via the FAB if none exist yet.
    2. On the "Your day" timeline, press-and-hold the drag handle (grip icon) on "Buy chicken" and drag it onto the center of "Go to Sam's Club"'s row. Release — confirm "Buy chicken" now renders indented beneath "Go to Sam's Club", and "Go to Sam's Club" shows a "1/1" (or similar) progress badge.
    3. Repeat for "Buy oatmeal" — confirm the badge updates to "2/2" (or reflects however many are done).
    4. Check the "Buy chicken" checkbox complete — confirm "Go to Sam's Club" does NOT auto-complete, and the badge updates to reflect the new done count.
    5. Drag "Buy chicken" from its indented position into empty space on the timeline (not onto any row) — confirm it returns to a standalone top-level row and the badge count decreases.
    6. Confirm a calendar event row (if any exist today) has no drag handle and cannot be dragged.
    7. With 2+ standalone top-level rows visible, drag one row and drop it in the gap just above or below another row (not onto its center) — confirm the two rows visibly swap order immediately (D-03's reorder gesture). Reload the page — confirm nesting still persisted, but note the manual reorder itself is session-only and is expected to reset to the default due-date/priority order after reload (this is the documented "implicit, not persisted" scope decision — not a bug).
    8. Reload the page — confirm nesting persisted (children still appear indented under the parent, expanded by default).
  </how-to-verify>
  <action>Pause for the user to manually verify the Today timeline drag-and-drop nesting flow on a phone browser per the steps in how-to-verify. Do not proceed until approved.</action>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
`cd frontend && npx tsc -b --noEmit && npx vitest run` — confirms no type errors and no regressions across the full frontend test suite (including `agenda.test.ts`, which this plan does not modify but which reads types this plan's dependencies changed).
</verification>

<success_criteria>
1. Dragging a task/block onto a task row nests it (backend PATCH fires, UI reflects nesting after refresh).
2. Dragging a nested item into empty space un-nests it.
3. Dropping in the gap between two top-level rows reorders them immediately (in-memory/session-level, per D-03 — this is a locked decision, not optional; only its persistence mechanism was discretionary).
4. Calendar events are excluded from drag entirely.
5. Progress badge reflects live done/total count; no completion propagation in either direction.
6. Nested children render indented, expanded by default, with a manual collapse toggle available.
7. Human verification checkpoint passed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-task-subtask-hierarchy-drag-drop/17-04-SUMMARY.md`
</output>
