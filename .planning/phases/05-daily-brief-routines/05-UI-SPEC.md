---
phase: 5
slug: daily-brief-routines
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-13
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for Phase 5: Daily Brief & Routines.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Scope

Two UI surfaces added to the existing Settings page:

1. **Brief time picker** — user sets what hour the daily brief fires (default 08:00). A single `<input type="time">` field inside a Settings card, with a "Save Brief Time" button.
2. **Routines manager** — list of custom recurring routines (name + cron schedule + action label). User can create, edit, and delete routines. Uses the existing drawer pattern from Tasks.

Both surfaces live under `/settings` to keep the nav unchanged.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none — inline CSS + CSS custom properties |
| Icon library | lucide-react (already installed; use `Clock`, `Plus`, `Trash2`, `Pencil`) |
| Font | -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif (from body in styles.css) |

Source: `frontend/src/styles.css` and existing component files.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inline padding |
| sm | 8px | Field label-to-input gap, button padding vertical |
| md | 16px | Card padding, drawer body gap, section margin |
| lg | 24px | Between Settings sections (matches existing `marginBottom: 24`) |
| xl | 32px | Page top padding (matches `.page { padding: 32px 16px }`) |
| 2xl | 48px | Empty state vertical padding |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Touch targets: minimum 44px height on all tappable rows (matches existing `.task-row { min-height: 48px }` — use 48px)
- Bottom nav clearance: `paddingBottom: 56px` on page container — inherited from `<div style={{ paddingBottom: 56 }}>` in App.tsx; this is an existing codebase constant, NOT a spacing token. Do not use 56px elsewhere.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 600 | 1.4 |
| Section header | 13px | 600 | 1.4 |
| Heading / page title | 20px | 600 | 1.2 |

Notes (from styles.css):
- `body { font-size: 16px; line-height: 1.5 }` — confirmed.
- `.page-title { font-size: 20px; font-weight: 600; line-height: 1.2 }` — confirmed.
- Section label caps: `font-size: 13px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em` — use exact pattern from existing Settings.tsx for "Google Calendar" header.
- Drawer title: 20px weight 600 (matches `.drawer-title`).
- Do not introduce additional sizes.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0f172a` (`--bg`) | Page background |
| Secondary (30%) | `#1e293b` (`--surface`) | Cards, nav bar, drawer background |
| Accent (10%) | `#6366f1` (`--accent`) | Save button fill, active nav tab, focus ring |
| Destructive | `#ef4444` (`--destructive`) | Delete routine button text, confirm-delete button fill |

Source: `frontend/src/styles.css` `:root` block.

Accent reserved for:
- "Save Brief Time" button (filled)
- "Add Routine" FAB (filled, same `.fab` class)
- Active bottom nav tab indicator
- Focus-visible outline (`*:focus-visible { outline: 2px solid var(--accent) }`)

Accent is NOT used for: routine row borders, field borders, section headers, or informational text.

Secondary semantic colors (existing, do not add new ones):
- `#22c55e` — connected status dot (already in Settings.tsx; not used in this phase)
- `#f59e0b` — medium priority badge (not used in this phase)

---

## Component Inventory

Reuse existing patterns exactly. Do not introduce new component types.

| Component | Reuse Pattern | Source |
|-----------|--------------|--------|
| Settings card | `div` with `background: var(--surface); border: 1px solid var(--border); border-radius: 12px; padding: 16px` | Settings.tsx |
| Section label | `p` with `font-size: 13px; color: var(--text-secondary); margin: 0 0 12px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em` | Settings.tsx |
| Primary button | `.btn-save` class (full-width) or inline equivalent for brief save | styles.css |
| Ghost button | `border: 1px solid var(--border); background: transparent` — matches "Disconnect" button | Settings.tsx |
| Destructive row action | `.btn-delete` class — full-width, `color: var(--destructive)`, no border, `font-size: 16px` | styles.css |
| Confirm modal | `.confirm-modal-overlay` + `.confirm-modal` + `.confirm-modal-actions` | styles.css |
| Drawer | `.drawer` + `.backdrop` slide-in from right, 85vw max 400px | styles.css |
| Drawer header | `.drawer-header` with `.drawer-title` + `.drawer-close` | styles.css |
| Drawer body | `.drawer-body` — flex column, gap 16px | styles.css |
| Form field | `.drawer-field` with `label` + `input`/`select` | styles.css |
| Empty state | `.empty-state` — centered, padding 48px 16px | styles.css |
| Routine row | New — matches `.task-row` pattern: flex, `min-height: 48px`, `padding: 8px`, `border-bottom: 1px solid var(--border)` | Extend styles.css |

---

## Layout: Settings Page Extension

The Phase 5 Settings additions appear below the existing "Google Calendar" section, in this order:

1. **Daily Brief** section card
   - Section label: "Daily Brief"
   - Card content: time input + save button
2. **Routines** section
   - Section label: "Routines"
   - Routine list (or empty state)
   - Add button below the list (not FAB — use a text button `+ Add Routine` at 14px weight 600 accent color, to avoid overlap with the existing `.fab` usage on Tasks page)

Brief time input: use `<input type="time">` styled with `.drawer-field input` styles (background `var(--bg)`, border `1px solid var(--border)`, border-radius 6px, color `var(--text)`, padding `8px 10px`, font-size 16px). Display current saved value on load.

The Routines section is the primary visual anchor on the Settings page after Phase 5 additions; the Daily Brief card is secondary.

---

## Interaction States

### Brief Time Card

| State | Behavior |
|-------|----------|
| Loading | Show "Loading…" at 14px `var(--text-secondary)` — matches Settings.tsx null check |
| Idle | Time input shows current brief time; "Save Brief Time" button enabled |
| Saving | "Save Brief Time" button shows "Saving…" and `opacity: 0.6; cursor: not-allowed` — matches `busy` pattern in Settings.tsx |
| Saved | Button returns to "Save Brief Time"; no toast (keep it simple) |
| Error | Inline error text below input: `font-size: 12px; color: var(--destructive)` — matches `.task-row-error` |

### Routine Drawer

| State | Behavior |
|-------|----------|
| Create | Drawer opens empty; title "Add Routine" |
| Edit | Drawer opens pre-filled; title "Edit Routine" |
| Save (new) | POST, close drawer, refetch list |
| Save (edit) | PATCH, close drawer, refetch list |
| Delete | Show confirm modal; on confirm DELETE, close drawer, refetch list |

### Routine List

| State | Behavior |
|-------|----------|
| Loading | Show "Loading…" text in card area |
| Empty | Show empty state: heading "No routines yet" + body "Add a routine to automate recurring actions." |
| Populated | List of routine rows; each row tappable to open edit drawer |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Brief section label | DAILY BRIEF |
| Brief time field label | Brief time |
| Brief save CTA | Save Brief Time |
| Brief saving state | Saving… |
| Routines section label | ROUTINES |
| Add routine CTA | + Add Routine |
| Routine drawer title (create) | Add Routine |
| Routine drawer title (edit) | Edit Routine |
| Routine save CTA | Save Routine |
| Routine delete action | Delete Routine |
| Delete confirm heading | Delete routine? |
| Delete confirm body | This routine will stop firing and cannot be recovered. |
| Delete confirm button | Delete Routine |
| Delete cancel button | Cancel |
| Empty state heading | No routines yet |
| Empty state body | Add a routine to automate recurring actions. |
| Brief time load error | Could not load brief time. Try refreshing. |
| Save error (brief) | Failed to save. Check your connection and try again. |
| Save error (routine) | Failed to save routine. Check your connection and try again. |
| Delete error (routine) | Failed to delete routine. Check your connection and try again. |

---

## Form Fields: Routine Drawer

| Field | Input type | Validation | Placeholder |
|-------|-----------|------------|-------------|
| Name | `<input type="text">` | Required, max 80 chars | e.g. "Morning stretch reminder" |
| Cron schedule | `<input type="text">` | Required; validate as 5-part cron (`* * * * *`) client-side on submit | e.g. "0 8 * * *" |
| Action | `<select>` | Required | — |

Action options (select):
- `pushover_brief` — "Send daily brief notification"

(Additional action types are Phase 6 scope; keep select single-option for now with room to extend.)

Cron validation error copy: "Enter a valid cron expression (e.g. 0 8 * * *)."

---

## Accessibility

- All interactive elements have `aria-label` or visible label text.
- Drawer uses `role="dialog"` and `aria-modal="true"`.
- Confirm modal uses `role="alertdialog"`.
- Focus is trapped inside open drawer/modal (match existing TaskDrawer pattern).
- `*:focus-visible` outline already defined globally — do not suppress.
- Time input: associate label via `htmlFor`/`id`.
- Routine rows: use `role="button"` with `tabIndex={0}` and `onKeyDown` Enter handler if not using `<button>`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No third-party registries. All components are hand-rolled following existing project patterns.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
