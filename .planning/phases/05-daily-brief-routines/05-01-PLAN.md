---
phase: 05-daily-brief-routines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_brief.py
  - backend/tests/test_settings.py
  - backend/tests/test_routines.py
autonomous: true
requirements: [CAL-06, CAL-07, NOTIF-02]
must_haves:
  truths:
    - "Test files exist and fail (RED) because the implementation modules do not exist yet"
    - "Tests pin the locked decisions: title 'Good morning', priority 0, empty body 'Nothing scheduled today.'"
    - "Tests cover brief-time GET default 08:00, brief-time PUT, routine create/delete, invalid cron 422"
  artifacts:
    - path: "backend/tests/test_brief.py"
      provides: "Failing tests for build_brief_body() + send_daily_brief()"
      contains: "Nothing scheduled today."
    - path: "backend/tests/test_settings.py"
      provides: "Failing tests for GET/PUT /api/v1/settings/brief-time"
      contains: "brief-time"
    - path: "backend/tests/test_routines.py"
      provides: "Failing tests for routines CRUD + cron validation"
      contains: "routines"
  key_links:
    - from: "backend/tests/test_brief.py"
      to: "app.services.brief"
      via: "import build_brief_body, send_daily_brief"
      pattern: "from app.services.brief import"
---

<objective>
Wave 0 test scaffold for Phase 5. Create the failing pytest files that define expected behavior for the daily brief, brief-time settings endpoint, and routines CRUD BEFORE any implementation exists. These tests are the executable contract that Plans 02 and 03 must satisfy.

Purpose: Lock in the behavior (locked decisions D-01 through D-11) as tests so downstream implementation is verified, not assumed.
Output: Three test files that currently FAIL (RED) due to missing implementation modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-daily-brief-routines/05-CONTEXT.md
@.planning/phases/05-daily-brief-routines/05-RESEARCH.md
@.planning/phases/05-daily-brief-routines/05-VALIDATION.md

<interfaces>
<!-- Existing test patterns to mirror exactly. -->

backend/tests/conftest.py — session-scoped `create_test_db` fixture builds ALL tables from `Base.metadata.create_all` (no Alembic migration files exist; tables come from model metadata). New models added in Plans 02/03 are auto-created here. TEST_DB_URL = "sqlite+aiosqlite:///./test_secretary.db".

backend/tests/test_tasks.py — router test style:
```python
from fastapi.testclient import TestClient
from app.main import app
client = TestClient(app)
def test_create_task():
    r = client.post("/api/v1/tasks/", json={"title": "Buy milk", "priority": "high"})
    assert r.status_code == 201
```

backend/tests/test_scheduler.py — scheduler test style with MemoryJobStore fixture:
```python
from apscheduler.jobstores.memory import MemoryJobStore
from app.scheduler import scheduler
@pytest.fixture(autouse=True)
def memory_jobstore():
    scheduler.remove_jobstore("default")
    scheduler.add_jobstore(MemoryJobStore(), "default")
    yield
    for job in scheduler.get_jobs():
        job.remove()
```

backend/app/services/pushover.py — `PushoverClient().send(title=..., message=..., priority=...)` is the call to MOCK. Patch target: `app.services.brief.PushoverClient` (brief.py imports it locally inside send_daily_brief).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for brief service (test_brief.py)</name>
  <read_first>
    - backend/tests/conftest.py (fixture patterns, TEST_DB_URL)
    - backend/tests/test_scheduler.py (MemoryJobStore fixture pattern)
    - backend/app/services/pushover.py (PushoverClient.send signature to mock)
    - backend/app/models/calendar.py (CalendarEvent columns: start_dt, all_day, start_date, title, cancelled — NOTE: there is NO `date` column)
    - .planning/phases/05-daily-brief-routines/05-CONTEXT.md (D-01..D-04 brief content decisions)
  </read_first>
  <action>
Create `backend/tests/test_brief.py`. It imports from `app.services.brief` (module does not exist yet → import error / collection failure is the expected RED state). Write these tests:

1. `test_send_daily_brief_calls_pushover_good_morning` — patch `app.services.brief.PushoverClient` with a MagicMock; call `send_daily_brief()`; assert the mock instance's `.send` was called once with `title="Good morning"` and `priority=0` (D-01, D-04). Use `unittest.mock.patch("app.services.brief.PushoverClient")`.

2. `test_build_brief_body_empty_returns_placeholder` — patch the brief module's sync Session so it returns no tasks and no events; assert `build_brief_body()` == `"Nothing scheduled today."` (D-02 empty case from CONTEXT specifics).

3. `test_build_brief_body_timed_event_format` — arrange a single timed calendar event today at 08:30 titled "Team standup" in the test DB (use the sync engine pattern: `create_engine(TEST_DB_URL.replace("+aiosqlite",""))` + `sessionmaker`, insert a `CalendarEvent(google_id="e1", title="Team standup", start_dt=<today 08:30 UTC>, all_day=False, cancelled=False, start_date=None)`); patch `app.services.brief` to use that sync session/engine; assert the returned body contains the line `"08:30 Team standup"` (D-03 timed format).

4. `test_build_brief_body_untimed_task_bullet` — insert a pending Task due today at 00:00 titled "Call dentist"; assert body contains `"• Call dentist"` (D-03 untimed format).

To let the brief builder use the test DB, design the tests to patch a single module-level factory in brief.py (e.g. `app.services.brief._make_session` or `app.services.brief._Session`) — mirror how `test_calendar.py`/conftest patches `app.services.sync._Session`. Add a module docstring noting the patch target so Plan 02 implements a matching seam.

Mark all four tests with no skip — they MUST run and fail until Plan 02 lands.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_brief.py -x -q 2>&1 | grep -E "error|Error|failed|no tests ran|ModuleNotFoundError|ImportError"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_brief.py exists
    - test_brief.py contains `from app.services.brief import`
    - test_brief.py contains the string `"Good morning"`
    - test_brief.py contains the string `priority=0` or `"priority"` assertion equal to 0
    - test_brief.py contains `"Nothing scheduled today."`
    - test_brief.py contains `08:30 Team standup`
    - test_brief.py contains `• Call dentist`
    - `python -m pytest tests/test_brief.py` collects the tests and they FAIL or error (implementation not present) — non-zero exit
  </acceptance_criteria>
  <done>test_brief.py exists with 4 tests that fail/error because app.services.brief does not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Write failing tests for settings + routines routers</name>
  <read_first>
    - backend/tests/test_tasks.py (TestClient router test pattern)
    - backend/tests/test_scheduler.py (MemoryJobStore fixture)
    - backend/app/routers/tasks.py (router prefix + endpoint shape)
    - .planning/phases/05-daily-brief-routines/05-CONTEXT.md (D-05..D-11)
  </read_first>
  <action>
Create `backend/tests/test_settings.py`:
- `client = TestClient(app)` (import `from app.main import app`).
- `test_get_brief_time_default` — `GET /api/v1/settings/brief-time` returns 200 and JSON containing `{"hour": 8, "minute": 0}` when no settings row exists yet (D-05 default 08:00; Pitfall 2 — default when row absent).
- `test_set_brief_time` — `PUT /api/v1/settings/brief-time` with `{"hour": 7, "minute": 30}` returns 200; then `GET` returns `{"hour": 7, "minute": 30}`.
- `test_set_brief_time_reschedules_job` — use the MemoryJobStore autouse fixture (copy from test_scheduler.py); `PUT` brief-time, then assert `scheduler.get_job("daily_brief")` is not None (D-06 job id `daily_brief`).

Create `backend/tests/test_routines.py`:
- `client = TestClient(app)`. Include the MemoryJobStore autouse fixture so routine jobs do not persist to the real jobstore.
- `test_create_routine` — `POST /api/v1/routines/` with `{"name": "Evening review", "cron": "0 18 * * *", "action": "send_daily_brief"}` returns 201; response has an `id`; assert `scheduler.get_job(f"routine_{id}")` is not None (D-11 job id `routine_{id}`).
- `test_list_routines` — create one, then `GET /api/v1/routines/` returns 200 list containing the created name.
- `test_update_routine` — `PATCH /api/v1/routines/{id}` changing cron to `"30 18 * * *"` returns 200 and the updated cron; job still present.
- `test_delete_routine` — `DELETE /api/v1/routines/{id}` returns 204; `scheduler.get_job(f"routine_{id}")` is None (Pitfall 3 — job removed with row); second delete returns 404.
- `test_create_routine_invalid_cron` — `POST` with `{"name": "Bad", "cron": "every day 8am", "action": "send_daily_brief"}` returns 422 (Pitfall 4 — invalid cron rejected, not a 500).

All tests must run and FAIL/ERROR because the routers do not exist and are not mounted yet.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_settings.py tests/test_routines.py -q 2>&1 | grep -E "error|Error|failed|404|no tests ran"</automated>
  </verify>
  <acceptance_criteria>
    - File backend/tests/test_settings.py exists and contains `/api/v1/settings/brief-time`
    - test_settings.py asserts default `8` and `0` for hour/minute
    - test_settings.py references `scheduler.get_job("daily_brief")`
    - File backend/tests/test_routines.py exists and contains `/api/v1/routines/`
    - test_routines.py contains `"send_daily_brief"`
    - test_routines.py references `routine_` job id and asserts a 422 for invalid cron
    - Both files collect and FAIL/ERROR (routers not mounted) — non-zero pytest exit
  </acceptance_criteria>
  <done>test_settings.py and test_routines.py exist with failing tests pinning all router behavior including the 422 invalid-cron case.</done>
</task>

</tasks>

<verification>
Run `cd backend && python -m pytest tests/test_brief.py tests/test_settings.py tests/test_routines.py -q` — all three files collect and every new test FAILS or ERRORS (RED). No test passes (that would mean the contract is trivially satisfied or the implementation already exists). Pre-existing suite (`python -m pytest tests/test_tasks.py tests/test_scheduler.py -q`) still passes.
</verification>

<success_criteria>
- Three new test files exist with the grep-verifiable strings listed in acceptance criteria.
- All new tests are RED (fail/error) because implementation modules/routers are absent.
- Existing tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/05-daily-brief-routines/05-01-SUMMARY.md`
</output>
