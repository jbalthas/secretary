---
phase: 04-calendar-sync
plan: 04
type: execute
wave: 2
depends_on: [04-02, 04-03]
files_modified:
  - frontend/src/types/task.ts
  - frontend/src/hooks/useCalendarEvents.ts
  - frontend/src/lib/agenda.ts
  - frontend/src/pages/Today.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/components/BottomNav.tsx
  - frontend/src/App.tsx
  - frontend/src/styles.css
autonomous: false
requirements: [CAL-01, CAL-02]

must_haves:
  truths:
    - "Bottom nav has three tabs: Today | Tasks | Settings"
    - "Settings page shows connection status and a Connect / Disconnect control"
    - "Today agenda shows real calendar events from /api/v1/events/today instead of PLACEHOLDER_EVENTS"
    - "Calendar events are visually distinguished from tasks (left indigo border, no checkbox)"
  artifacts:
    - path: "frontend/src/pages/Settings.tsx"
      provides: "OAuth connect/disconnect + status card"
      contains: "Connect Google Calendar"
    - path: "frontend/src/hooks/useCalendarEvents.ts"
      provides: "fetch hook for today's events"
      contains: "events/today"
    - path: "frontend/src/lib/agenda.ts"
      provides: "buildAgenda accepting real events; PLACEHOLDER_EVENTS removed"
      contains: "buildAgenda"
  key_links:
    - from: "frontend/src/pages/Today.tsx"
      to: "/api/v1/events/today"
      via: "useCalendarEvents hook → buildAgenda"
      pattern: "useCalendarEvents"
    - from: "frontend/src/pages/Settings.tsx"
      to: "/auth/google"
      via: "Connect button navigates to server OAuth redirect"
      pattern: "/auth/google"
    - from: "frontend/src/components/BottomNav.tsx"
      to: "/settings"
      via: "third NavLink tab"
      pattern: "/settings"
---

<objective>
Surface calendar sync in the UI (CAL-01 connect UX, CAL-02 events in agenda): add a Settings page with the OAuth connect/disconnect card, add a third bottom-nav tab, and replace the PLACEHOLDER_EVENTS stub in the Today agenda with real events from GET /api/v1/events/today. Build per 04-UI-SPEC.md exactly.

Purpose: Closes the loop — the user connects their calendar and sees real events. Wave 2 (depends on auth + events endpoints from Plans 02/03). Has a human-verify checkpoint (visual/OAuth flow), so autonomous=false.
Output: Settings.tsx, useCalendarEvents.ts, updated agenda.ts/Today.tsx/BottomNav.tsx/App.tsx/styles.css, plus a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-calendar-sync/04-UI-SPEC.md
@.planning/phases/04-calendar-sync/04-CONTEXT.md

<interfaces>
Backend endpoints (Plans 02/03) the frontend consumes:
- GET  /auth/google                  → 307 redirect to Google consent (server-side; navigate browser here)
- GET  /api/v1/calendar/status       → { connected: boolean, last_synced_at: string|null }
- POST /api/v1/calendar/disconnect   → { connected: false }
- GET  /api/v1/events/today          → CalendarEventOut[]:
    { google_id: string, title: string, start_dt: string|null, end_dt: string|null, all_day: boolean, start_date: string|null }

Existing frontend patterns (match exactly):
frontend/src/types/task.ts:
```ts
export interface AgendaItem { id: string; title: string; time: string | null; priority: Priority | null; isEvent: boolean; }
```
frontend/src/lib/agenda.ts — CURRENTLY: `buildAgenda(tasks, now)` merges tasks with PLACEHOLDER_EVENTS. You will change the signature to accept events and DELETE PLACEHOLDER_EVENTS (STATE.md "[02-05]: PLACEHOLDER_EVENTS named export for Phase 4 swap").
frontend/src/hooks/useTasks.ts — fetch-then-setState hook pattern to mirror for useCalendarEvents.
frontend/src/components/BottomNav.tsx — NavLink tab pattern; uses lucide-react icons (Calendar, ListTodo). Add Settings (gear) icon.
frontend/src/App.tsx — Routes; add `<Route path="/settings" element={<Settings />} />`.
frontend/src/pages/Today.tsx — renders agenda; allDay split by time===null.

04-UI-SPEC.md is the binding visual contract: tokens, copy strings, .settings-card / .connection-status-row / .status-dot / .agenda-item--event CSS, three connection states, disconnect confirm-modal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalendarEvent type + useCalendarEvents hook + agenda.ts swap</name>
  <read_first>
    - frontend/src/types/task.ts (AgendaItem interface)
    - frontend/src/hooks/useTasks.ts (hook pattern to mirror)
    - frontend/src/lib/agenda.ts (current buildAgenda + PLACEHOLDER_EVENTS to remove)
  </read_first>
  <action>
    1. In `frontend/src/types/task.ts`, add:
       ```ts
       export interface CalendarEvent {
         google_id: string;
         title: string;
         start_dt: string | null;
         end_dt: string | null;
         all_day: boolean;
         start_date: string | null;
       }
       ```
    2. Create `frontend/src/hooks/useCalendarEvents.ts` mirroring useTasks: fetch `GET /api/v1/events/today`, store `CalendarEvent[]`, refresh on mount via useEffect. Return `{ events, refresh }`. On fetch error, set events to `[]` (do not throw — agenda must still render tasks).
    3. Rewrite `frontend/src/lib/agenda.ts`:
       - DELETE the `PLACEHOLDER_EVENTS` export entirely.
       - Change signature to `buildAgenda(tasks: Task[], events: CalendarEvent[], now: Date = new Date()): AgendaItem[]`.
       - Map each CalendarEvent to an AgendaItem: `id: \`event-${e.google_id}\``, `title: e.title || "(No title)"`, `time: e.all_day ? null : (e.start_dt ? e.start_dt.slice(11,16) : null)`, `priority: null`, `isEvent: true`. Filter events to today using the same `today` string logic already in the file (all-day matches `e.start_date === today`; timed matches `e.start_dt?.slice(0,10) === today`).
       - Merge: all-day items (tasks + events) first, then timed items sorted by time (keep existing localeCompare sort). Preserve existing task mapping logic unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/task.ts` contains `interface CalendarEvent`
    - `frontend/src/hooks/useCalendarEvents.ts` contains `events/today`
    - `frontend/src/lib/agenda.ts` does NOT contain `PLACEHOLDER_EVENTS`
    - `frontend/src/lib/agenda.ts` contains `events: CalendarEvent[]`
    - `frontend/src/lib/agenda.ts` contains `isEvent: true`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Real events flow through buildAgenda; placeholder removed; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Settings page + BottomNav 3rd tab + App route + Today wiring + CSS</name>
  <read_first>
    - .planning/phases/04-calendar-sync/04-UI-SPEC.md (binding: tokens, copy table, CSS classes, three states, disconnect modal)
    - frontend/src/components/BottomNav.tsx (NavLink pattern + lucide icons)
    - frontend/src/App.tsx (Routes)
    - frontend/src/pages/Today.tsx (agenda render; events split)
    - frontend/src/styles.css (existing tokens + .confirm-modal + .btn-save classes)
    - frontend/src/pages/Tasks.tsx (page structure + confirm-modal usage pattern, if present)
  </read_first>
  <action>
    1. `frontend/src/components/BottomNav.tsx`: add a third NavLink `to="/settings"` with lucide `Settings` (gear) icon (size 22) and label "Settings", same active/inactive color logic as existing tabs.
    2. `frontend/src/App.tsx`: import `Settings` from `./pages/Settings` and add `<Route path="/settings" element={<Settings />} />`.
    3. Create `frontend/src/pages/Settings.tsx` per UI-SPEC layout:
       - `<h1 className="page-title">Settings</h1>`.
       - A `.settings-card` titled "Google Calendar" (`.settings-card-title`) containing `.connection-status-row` (`.status-dot` + label) and the CTA.
       - On mount fetch `GET /api/v1/calendar/status`. Three states per UI-SPEC:
         - not connected: grey dot (`--text-disabled`), "Not connected", `.btn-save` button "Connect Google Calendar" → `window.location.href = "/auth/google"`.
         - connected: green dot (`#22c55e`), "Connected — syncs every 5 min", "Disconnect" text link (styling: `font-size:14px; color:var(--destructive); background:none; border:none; cursor:pointer`).
         - On `?connected=true` query param, treat as connected (refetch status).
       - Sync meta line `.settings-meta`: "Never synced" / "Last synced just now" / "Last synced [N] minutes ago" computed from last_synced_at (relative time).
       - Disconnect flow: use existing `.confirm-modal` pattern. Title "Disconnect Google Calendar?", body "Synced events will be removed from the agenda.", confirm "Disconnect" → `POST /api/v1/calendar/disconnect` then refetch status, cancel "Cancel".
       - `aria-label` on status dot: `aria-label={\`Connection status: ${connected ? "connected" : "not connected"}\`}`.
       Use exact copy strings from the UI-SPEC Copywriting Contract.
    4. `frontend/src/pages/Today.tsx`: import `useCalendarEvents`; call it; pass events into `buildAgenda(tasks, events)`. Render event AgendaItems with the `--event` variant. If AgendaItem component needs a flag, pass `item.isEvent` through to apply `.agenda-item--event` (left indigo border, no checkbox).
    5. `frontend/src/styles.css`: append the new classes from UI-SPEC verbatim: `.settings-card`, `.settings-card-title`, `.connection-status-row`, `.status-dot`, `.agenda-item--event`, `.settings-meta` (14px, var(--text-secondary)). Do NOT redefine existing tokens.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/BottomNav.tsx` contains `/settings`
    - `frontend/src/App.tsx` contains `path="/settings"`
    - `frontend/src/pages/Settings.tsx` contains `Connect Google Calendar`
    - `frontend/src/pages/Settings.tsx` contains `/auth/google`
    - `frontend/src/pages/Settings.tsx` contains `Connected — syncs every 5 min`
    - `frontend/src/pages/Settings.tsx` contains `Disconnect Google Calendar?`
    - `frontend/src/pages/Today.tsx` contains `useCalendarEvents`
    - `frontend/src/styles.css` contains `.agenda-item--event`
    - `frontend/src/styles.css` contains `.settings-card`
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>Settings page, third nav tab, route, Today wiring, and CSS all per UI-SPEC; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — OAuth round-trip, event display, disconnect</name>
  <what-built>
    Full calendar sync UI: a Settings page with Google Calendar connect/disconnect, a third "Settings" bottom-nav tab, and the Today agenda now sourcing real events from the backend (PLACEHOLDER_EVENTS removed).
  </what-built>
  <action>Pause for human verification. The user performs the steps in how-to-verify against the running app and OAuth flow; no code changes in this task.</action>
  <how-to-verify>
    1. Ensure the backend is running and GOOGLE_CLIENT_SECRETS_JSON / redirect URI / session secret are set (from 04-01 user_setup) and the OAuth consent screen is published to "In production".
    2. Open the app, tap the new "Settings" tab (gear icon). Confirm three tabs: Today | Tasks | Settings.
    3. Status shows "Not connected" with a grey dot. Tap "Connect Google Calendar".
    4. Complete the Google OAuth consent in the browser. You should be redirected back to /settings?connected=true and see a green dot + "Connected — syncs every 5 min".
    5. Create an event in Google Calendar for today. Wait up to 5 minutes (or restart backend to trigger startup sync). Open Today — the event appears with a left indigo border and NO checkbox, sorted by time among tasks.
    6. Back in Settings, tap "Disconnect" → confirm modal "Disconnect Google Calendar?" → Disconnect. Status returns to "Not connected" and the event disappears from Today.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (status state, event styling, OAuth redirect, disconnect behavior).</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0
- `cd frontend && npm run build` exits 0
- PLACEHOLDER_EVENTS no longer present anywhere in frontend/src
- Human-verify checkpoint approved (OAuth round-trip + event display + disconnect)
</verification>

<success_criteria>
CAL-01 connect UX (Settings-only per D-01/D-02/D-03) and CAL-02 events-in-agenda are visible and working in the UI per 04-UI-SPEC.md. PLACEHOLDER_EVENTS removed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-calendar-sync/04-04-SUMMARY.md`
</output>
