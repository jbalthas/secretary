---
phase: 14-progression-substrate
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [backend/app/scheduler.py, backend/app/routers/export.py, backend/app/schemas/export.py, backend/app/main.py, backend/tests/test_snapshots.py]
autonomous: true
requirements: [PROG-01, PROG-02]
---

<objective>Register reboot-safe jobs and expose the on-demand backend trigger for the future Sync page.</objective>

<tasks>
<task type="auto"><name>Scheduler wiring</name><action>Add Sunday 23:50 snapshot_progress and monthly snapshot_cleanup jobs with stable IDs, replacement, timezone, and startup registration.</action></task>
<task type="auto"><name>Trigger endpoint</name><action>Add synchronous POST /api/v1/export/snapshot with a typed created/skipped response and register the router.</action></task>
<task type="auto"><name>Verification</name><action>Test scheduler and endpoint integration, confirm one Alembic head, then run the full backend suite.</action><verify>cd backend && python -m alembic heads && python -m pytest -q</verify></task>
</tasks>

<output>Create 14-02-SUMMARY.md and update roadmap/state.</output>