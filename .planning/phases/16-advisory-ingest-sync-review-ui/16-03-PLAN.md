---
phase: 16-advisory-ingest-sync-review-ui
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - backend/app/routers/advisory.py
  - backend/app/main.py
  - backend/tests/test_advisory_routes.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [ADVISE-01, ADVISE-02, ADVISE-05]
must_haves:
  truths:
    - "GET /api/v1/advisory/schema returns AdvisoryPayload.model_json_schema() with status 200"
    - "POST /api/v1/advisory/preview validates the body as AdvisoryPayload (422 on a missing rationale or unknown key) and returns AdvisoryPreviewResult with no DB writes"
    - "POST /api/v1/advisory/confirm validates the body as AdvisoryConfirmRequest and returns AdvisoryResult; an unknown goal external_key returns 422"
    - "advisory.router is registered in main.py alongside ingest.router"
    - "CI runs `grep -r 'anthropic|openai|litellm' backend/app/` and fails the build if it matches"
  artifacts:
    - path: "backend/app/routers/advisory.py"
      provides: "GET /schema, POST /preview, POST /confirm async routes mirroring routers/ingest.py"
      contains: "router = APIRouter"
      min_lines: 25
    - path: "backend/app/main.py"
      provides: "advisory.router registration"
      contains: "advisory.router"
    - path: "backend/tests/test_advisory_routes.py"
      provides: "Route-level 200/422 tests via async test client"
      contains: "def test_"
    - path: ".github/workflows/ci.yml"
      provides: "LLM-import grep guard CI step"
      contains: "anthropic"
  key_links:
    - from: "backend/app/routers/advisory.py"
      to: "app.services.advisory_service"
      via: "async route -> dry_run_advisory / apply_advisory"
      pattern: "advisory_service"
    - from: "backend/app/main.py"
      to: "app.routers.advisory.router"
      via: "include_router"
      pattern: "advisory.router"
---

<objective>
Expose the advisory service over HTTP and wire the CI guard. Add an async advisory router
(`GET /schema`, `POST /preview`, `POST /confirm`) mirroring `routers/ingest.py`, register it
in `main.py`, add route-level tests, and add the LLM-import grep guard to CI.

Purpose: Deliver the HTTP surface for ADVISE-01 (schema), ADVISE-02 (preview), ADVISE-05
(confirm), and the CI/grep correctness gate.
Output: `routers/advisory.py` (new), extended `main.py`, `tests/test_advisory_routes.py` (new), `.github/workflows/ci.yml` (new or extended).

HARD CONSTRAINTS (locked):
- Routes are `async def` on `Depends(get_session)` (the async ingest pattern), NOT the sync
  export path. FastAPI's built-in RequestValidationError handler surfaces 422s — no manual
  try/except for validation.
- Unknown-goal-key / unknown-milestone ValueErrors from the service MUST become HTTP 422 (catch
  ValueError in the route and raise HTTPException(422, detail=...) OR register a handler).
- NO LLM imports anywhere in backend/app/. The CI grep guard enforces this.
- Prefix mirrors ingest: `f"{settings.api_prefix}/advisory"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advisory-ingest-sync-review-ui/16-RESEARCH.md
@backend/app/routers/ingest.py
@backend/app/main.py
@backend/app/schemas/advisory.py
@backend/app/services/advisory_service.py
@backend/tests/test_ingest.py
@backend/tests/conftest.py

<interfaces>
backend/app/routers/advisory.py (mirror routers/ingest.py exactly):
  router = APIRouter(prefix=f"{settings.api_prefix}/advisory", tags=["advisory"])

  @router.get("/schema")
  async def get_schema():
      return AdvisoryPayload.model_json_schema()

  @router.post("/preview", response_model=AdvisoryPreviewResult)
  async def preview(payload: AdvisoryPayload, session: AsyncSession = Depends(get_session)):
      try:
          return await advisory_service.dry_run_advisory(payload, session)
      except ValueError as e:
          raise HTTPException(status_code=422, detail=str(e))

  @router.post("/confirm", response_model=AdvisoryResult)
  async def confirm(req: AdvisoryConfirmRequest, session: AsyncSession = Depends(get_session)):
      try:
          return await advisory_service.apply_advisory(req, session)
      except ValueError as e:
          raise HTTPException(status_code=422, detail=str(e))

main.py: app.include_router(advisory.router)   # next to ingest.router (line ~85)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: advisory router + main.py registration</name>
  <files>backend/app/routers/advisory.py, backend/app/main.py</files>
  <read_first>
    - backend/app/routers/ingest.py (full — exact import set, prefix, response_model usage)
    - backend/app/main.py (the include_router block lines 76-89; add advisory after ingest at line 85)
    - backend/app/schemas/advisory.py (AdvisoryPayload, AdvisoryPreviewResult, AdvisoryResult, AdvisoryConfirmRequest)
  </read_first>
  <action>
    Create backend/app/routers/advisory.py with the three routes from <interfaces>. Imports:
    `from fastapi import APIRouter, Depends, HTTPException`; `from sqlalchemy.ext.asyncio import AsyncSession`;
    `from app.db import get_session`; `from app.config import settings`;
    `from app.schemas.advisory import AdvisoryPayload, AdvisoryPreviewResult, AdvisoryResult, AdvisoryConfirmRequest`;
    `from app.services import advisory_service`.
    In backend/app/main.py, add `from app.routers import advisory` to the routers import group and
    `app.include_router(advisory.router)` immediately after the `ingest.router` line (~85). Do not
    reorder existing registrations.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.main import app; paths=[r.path for r in app.routes]; assert any('/advisory/schema' in p for p in paths), paths; assert any('/advisory/preview' in p for p in paths); assert any('/advisory/confirm' in p for p in paths); print('routes ok')"</automated>
  </verify>
  <acceptance_criteria>
    - routers/advisory.py contains `APIRouter(prefix=f"{settings.api_prefix}/advisory"`
    - routers/advisory.py contains `@router.get("/schema")`, `@router.post("/preview"`, `@router.post("/confirm"`
    - routers/advisory.py contains `async def` for all three routes and `Depends(get_session)`
    - routers/advisory.py contains `HTTPException(status_code=422`
    - main.py contains `advisory.router`
    - `cd backend && python -c "from app.main import app"` exits 0 and the three advisory paths exist
  </acceptance_criteria>
  <done>Three advisory routes registered and importable; 422 mapping for ValueError in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: route-level tests (200/422, no-writes, idempotent)</name>
  <files>backend/tests/test_advisory_routes.py</files>
  <read_first>
    - backend/tests/test_ingest.py (how the async test client posts to /ingest/preview and /confirm; status assertions)
    - backend/tests/conftest.py (async client + session fixtures + seed helpers)
    - backend/app/schemas/advisory.py (request/response shapes)
  </read_first>
  <action>
    Create backend/tests/test_advisory_routes.py (mirror test_ingest.py client usage):
      - test_schema_endpoint: GET /api/v1/advisory/schema -> 200; JSON has "properties" with
        keys goal_adjustments/milestone_adjustments/new_tasks/notes/session_id/generated_at.
      - test_preview_ok: seed a goal; POST /api/v1/advisory/preview with a valid AdvisoryPayload
        -> 200; body.goals[0].fields non-empty; DB row counts unchanged after.
      - test_preview_missing_rationale_422: POST a payload whose goal_adjustment omits rationale
        -> 422 (FastAPI validation).
      - test_preview_unknown_goal_422: POST referencing an absent external_key -> 422.
      - test_confirm_ok: POST /api/v1/advisory/confirm AdvisoryConfirmRequest{advisory_id,payload}
        -> 200; body.created/updated reflect the change; goal target_date updated in DB.
      - test_confirm_idempotent: POST the same confirm twice -> both 200; second body.replayed True;
        row counts unchanged.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_advisory_routes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_advisory_routes.py contains `test_schema_endpoint`, `test_preview_unknown_goal_422`, `test_confirm_idempotent`
    - `cd backend && python -m pytest tests/test_advisory_routes.py -q` -> all pass
    - `cd backend && python -m pytest -x -q` -> full suite green
  </acceptance_criteria>
  <done>All advisory route tests green; full backend suite green.</done>
</task>

<task type="auto">
  <name>Task 3: CI LLM-import grep guard</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ (list existing workflows; if one exists, ADD a step instead of a new file)
    - .planning/phases/16-advisory-ingest-sync-review-ui/16-RESEARCH.md ("CI grep guard" row)
  </read_first>
  <action>
    First check for an existing workflow: `ls .github/workflows/`. If a CI workflow exists, ADD a
    step to its backend job; otherwise create .github/workflows/ci.yml with a minimal job. The step:
        - name: Guard against LLM client imports in backend
          run: |
            if grep -rE "anthropic|openai|litellm" backend/app/; then
              echo "FORBIDDEN LLM client import found in backend/app/"; exit 1
            fi
    Note: grep returns exit 0 when it MATCHES, so a match triggers `exit 1` (fail). When nothing
    matches grep exits 1 and the `if` is false -> step passes.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -rE "anthropic|openai|litellm" backend/app/; test $? -ne 0 && echo "GUARD_PASSES_LOCALLY"</automated>
  </verify>
  <acceptance_criteria>
    - a workflow under .github/workflows/ contains the strings `anthropic`, `openai`, `litellm` in a grep guard step
    - the guard step uses `exit 1` on match
    - locally `grep -rE "anthropic|openai|litellm" backend/app/` returns no matches (exit 1) -> guard would pass
  </acceptance_criteria>
  <done>CI fails on any future LLM client import in backend/app/; currently clean.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_advisory_routes.py -q` -> green
- `cd backend && python -m pytest -x -q` -> full suite green
- The three advisory routes resolve on app import
- `grep -rE "anthropic|openai|litellm" backend/app/` -> no matches; CI guard step present
</verification>

<success_criteria>
- ADVISE-01/02/05 HTTP surface live: schema/preview/confirm routes with 422 validation and
  ValueError->422 mapping; advisory.router registered; CI grep guard enforces the no-LLM rule.
</success_criteria>

<output>
After completion, create `.planning/phases/16-advisory-ingest-sync-review-ui/16-03-SUMMARY.md`
</output>
