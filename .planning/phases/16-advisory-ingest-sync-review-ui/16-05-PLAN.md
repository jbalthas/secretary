---
phase: 16-advisory-ingest-sync-review-ui
plan: 05
type: execute
wave: 5
depends_on: ["16-01", "16-04"]
files_modified:
  - backend/scripts/regen_advisor_schema.py
  - frontend/src/lib/advisorPrompt.ts
autonomous: true
requirements: [PROMPT-01]
must_haves:
  truths:
    - "frontend/src/lib/advisorPrompt.ts no longer contains the literal placeholder '[SCHEMA BLOCK]'"
    - "advisorPrompt.ts contains the JSON Schema produced by AdvisoryPayload.model_json_schema(), embedded in the ADVISOR_PROMPT template literal"
    - "The prompt instructs the LLM to echo BOTH session_id AND generated_at verbatim from the brief header (so Phase 16 staleness is self-contained)"
    - "A repeatable script backend/scripts/regen_advisor_schema.py prints the schema so the embed can be regenerated if the model changes"
  artifacts:
    - path: "backend/scripts/regen_advisor_schema.py"
      provides: "Prints AdvisoryPayload.model_json_schema() as pretty JSON for embedding"
      contains: "model_json_schema"
    - path: "frontend/src/lib/advisorPrompt.ts"
      provides: "ADVISOR_PROMPT with the real schema block + session_id/generated_at echo instruction"
      contains: "ADVISOR_PROMPT"
  key_links:
    - from: "frontend/src/lib/advisorPrompt.ts"
      to: "backend/app/schemas/advisory.AdvisoryPayload.model_json_schema()"
      via: "regen script output embedded as the schema block"
      pattern: "model_json_schema"
---

<objective>
Finalize PROMPT-01: regenerate the `[SCHEMA BLOCK]` placeholder in `advisorPrompt.ts` from the
now-final `AdvisoryPayload.model_json_schema()`, via a small repeatable script, and ensure the
prompt instructs the LLM to echo both `session_id` and `generated_at` (required by 16-04's
staleness check). This is the LAST plan of the phase (per ROADMAP), run after the schema (16-01)
and the page (16-04) are stable.

Purpose: Deliver PROMPT-01 (schema-block regen).
Output: `backend/scripts/regen_advisor_schema.py` (new), updated `frontend/src/lib/advisorPrompt.ts`.

HARD CONSTRAINTS:
- Do NOT hand-write the schema — it MUST come from `AdvisoryPayload.model_json_schema()`.
- Embed as static text in the TS template literal (advisorPrompt.ts is a static const, not a
  runtime fetch) — mirrors the existing ingestPrompt.ts pattern.
- The embedded JSON must not contain a backtick or `${` (Pydantic JSON Schema never does) so the
  template literal stays valid.
- NO LLM imports added anywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advisory-ingest-sync-review-ui/16-RESEARCH.md
@frontend/src/lib/advisorPrompt.ts
@backend/app/schemas/advisory.py
@.planning/phases/15-context-export-advisor-prompt/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: regen script that prints the advisory JSON schema</name>
  <files>backend/scripts/regen_advisor_schema.py</files>
  <read_first>
    - backend/app/schemas/advisory.py (AdvisoryPayload — the model to introspect)
    - backend/scripts/ (list — match any existing script style/shebang; create dir if absent)
  </read_first>
  <action>
    Create backend/scripts/regen_advisor_schema.py:
        import json
        from app.schemas.advisory import AdvisoryPayload
        if __name__ == "__main__":
            print(json.dumps(AdvisoryPayload.model_json_schema(), indent=2))
    It must run from backend/ with the app importable (same as alembic/pytest invocation).
  </action>
  <verify>
    <automated>cd backend && python scripts/regen_advisor_schema.py | python -c "import sys,json; d=json.load(sys.stdin); assert 'properties' in d and 'goal_adjustments' in d['properties']; print('schema ok')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/regen_advisor_schema.py contains `AdvisoryPayload` and `model_json_schema`
    - `cd backend && python scripts/regen_advisor_schema.py` prints valid JSON with a `properties.goal_adjustments` key
  </acceptance_criteria>
  <done>Script prints the canonical advisory JSON schema.</done>
</task>

<task type="auto">
  <name>Task 2: embed schema + echo instruction into advisorPrompt.ts</name>
  <files>frontend/src/lib/advisorPrompt.ts</files>
  <read_first>
    - frontend/src/lib/advisorPrompt.ts (full — header comment lines 1-2, the [SCHEMA BLOCK] placeholder ~line 37, the EXAMPLE PAYLOAD 40-71, the echo/session_id wording)
    - .planning/phases/15-context-export-advisor-prompt/15-CONTEXT.md (D-12: prompt instructs LLM to echo session_id; D-11 staleness needs generated_at too)
  </read_first>
  <action>
    1. Run `cd backend && python scripts/regen_advisor_schema.py` and capture the JSON output.
    2. In frontend/src/lib/advisorPrompt.ts, replace the literal `[SCHEMA BLOCK]` placeholder with
       that JSON (pretty-printed, embedded inside the existing ADVISOR_PROMPT backtick template).
       Remove the now-stale "[SCHEMA BLOCK] is a placeholder" header comment (lines 1-2) or update
       it to note the schema is generated by backend/scripts/regen_advisor_schema.py.
    3. Ensure the prompt's echo instruction names BOTH fields: update the wording so the LLM is
       told to "echo `session_id` AND `generated_at` verbatim from the brief header" and add
       `generated_at` to the EXAMPLE PAYLOAD (alongside the existing `session_id`). This is what
       16-04's staleness banner reads back.
    4. Ensure the milestone IN-SCOPE prose + EXAMPLE PAYLOAD reflect the finalized schema: a
       milestone_adjustment matches by `title` and renames via optional `new_title` (the generated
       schema block now includes `new_title`); adjustable fields are target_date/done/new_title.
    5. Confirm the embedded JSON has no backtick or `${`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c "\[SCHEMA BLOCK\]" src/lib/advisorPrompt.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[SCHEMA BLOCK\]" frontend/src/lib/advisorPrompt.ts` == 0 (placeholder gone)
    - advisorPrompt.ts contains `"goal_adjustments"` and `"properties"` (the embedded schema)
    - advisorPrompt.ts contains both `session_id` and `generated_at` in the echo instruction/example
    - advisorPrompt.ts has no backtick or `${` inside the embedded schema block
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>advisorPrompt.ts carries the real generated schema + dual echo instruction; placeholder gone; builds clean.</done>
</task>

</tasks>

<verification>
- `cd backend && python scripts/regen_advisor_schema.py` -> valid advisory JSON schema
- `grep -c "\[SCHEMA BLOCK\]" frontend/src/lib/advisorPrompt.ts` -> 0
- `cd frontend && npx tsc --noEmit && npm run build` -> succeeds
- `grep -rc "anthropic\|openai\|litellm" backend/app/` -> 0
</verification>

<success_criteria>
- PROMPT-01 complete: the advisor prompt's schema block is generated from
  AdvisoryPayload.model_json_schema(); the prompt echoes session_id + generated_at for staleness.
</success_criteria>

<output>
After completion, create `.planning/phases/16-advisory-ingest-sync-review-ui/16-05-SUMMARY.md`
</output>
