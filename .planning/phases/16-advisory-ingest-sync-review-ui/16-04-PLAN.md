---
phase: 16-advisory-ingest-sync-review-ui
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - frontend/src/types/goal.ts
  - frontend/src/hooks/useAdvisory.ts
  - frontend/src/pages/Advisor.tsx
  - frontend/src/lib/advisoryId.ts
  - frontend/src/hooks/useAdvisory.test.ts
autonomous: true
requirements: [ADVISE-04, ADVISE-07, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "On the existing /advisor Sync page, a paste-only textarea + 'Run preview' button posts to /api/v1/advisory/preview and renders a per-item diff grouped Goals / Milestones / New tasks, each row showing entity·field·old→new and rationale ALWAYS visible as sub-text"
    - "Each diff row defaults to accepted (checked); the user unchecks to reject; Confirm posts ONLY the accepted rows to /api/v1/advisory/confirm"
    - "advisory_id is computed client-side as a stable hash of the FULL pasted reply (before reject-filtering) so re-confirming the same advisory is a no-op replay regardless of which subset is accepted"
    - "The advisor's notes render as a distinct accent callout ABOVE the diff; notes is display-only"
    - "After Confirm the page STAYS on /advisor and shows an in-page success summary (N goals · M milestones · K tasks) with a Link to /goals — it does NOT auto-navigate"
    - "The page header shows 'Last advisor sync: N days ago' from AppSettings.last_advisory_at; a non-blocking staleness banner shows when the pasted reply's generated_at is older than 7 days, and Confirm stays enabled"
  artifacts:
    - path: "frontend/src/hooks/useAdvisory.ts"
      provides: "preview/confirm fetch hook modeled on useIngest (parse422, errors, notes, sessionId, generatedAt)"
      contains: "export function useAdvisory"
    - path: "frontend/src/pages/Advisor.tsx"
      provides: "Paste/notes/staleness/diff/accept-reject/confirm/success UI appended below Phase-15 sections"
      contains: "Run preview"
    - path: "frontend/src/lib/advisoryId.ts"
      provides: "computeAdvisoryId(fullPayload) -> sha256 of canonical JSON"
      contains: "computeAdvisoryId"
    - path: "frontend/src/types/goal.ts"
      provides: "Advisory diff types + Goal.priority_rank"
      contains: "AdvisoryEntityDiff"
  key_links:
    - from: "frontend/src/pages/Advisor.tsx"
      to: "frontend/src/hooks/useAdvisory.ts"
      via: "preview/confirm calls + state"
      pattern: "useAdvisory"
    - from: "frontend/src/pages/Advisor.tsx"
      to: "frontend/src/lib/advisoryId.ts"
      via: "client-computed advisory_id over the full pasted reply"
      pattern: "computeAdvisoryId"
---

<objective>
Extend the existing `/advisor` Sync page (Phase 15 shell) with the inbound advisory UI:
a paste box, a notes callout, a non-blocking staleness banner, a richer per-item diff grouped
Goals / Milestones / New tasks with always-visible rationale and per-row accept/reject, a
Confirm that posts only the accepted subset, and an in-page success summary. Add the
`useAdvisory` hook, the advisory diff types (+ `Goal.priority_rank`), and the client-side
`advisory_id` hash.

Purpose: Deliver ADVISE-04 (diff review UI), ADVISE-07 (per-row accept/reject), SYNC-01
(single-page loop), SYNC-02 (last-sync + staleness).
Output: extended `types/goal.ts`, `hooks/useAdvisory.ts` (new), extended `pages/Advisor.tsx`, `lib/advisoryId.ts` (new), `useAdvisory.test.ts` (new).

HARD CONSTRAINTS (locked):
- D-06 single linear page: APPEND new sections BELOW the existing Phase-15 export sections in
  Advisor.tsx — no tabs, no stepper.
- D-07 paste-only: a `<textarea>` + "Run preview" button. NO file upload.
- D-01 rationale ALWAYS visible as sub-text under each row (never hover/tooltip).
- D-02 richer diff (entity·field·old→new·rationale; new tasks as add-rows) grouped into
  Goals / Milestones / New tasks — a NEW component, not the shallow Ingest DiffGroup. Reuse the
  `diff-badge*` / `var(--surface)` / `var(--border)` CSS conventions.
- D-03 rows default accepted; user unchecks to reject.
- D-04 Confirm posts a client-filtered payload of accepted rows only. PLUS a stable
  `advisory_id` computed over the FULL pasted reply (D-05 one-shot idempotency).
- D-08 STAY on /advisor after confirm; in-page success summary + `<Link to="/goals">`; do NOT
  `navigate("/goals")`.
- D-09 notes = accent callout above the diff; display-only.
- D-10 "Last advisor sync: N days ago" in the page header from last_advisory_at.
- D-11 staleness banner non-blocking, computed from the pasted reply's `generated_at`
  (now - generated_at > 7 days); Confirm stays enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-advisory-ingest-sync-review-ui/16-CONTEXT.md
@.planning/phases/16-advisory-ingest-sync-review-ui/16-RESEARCH.md
@frontend/src/pages/Advisor.tsx
@frontend/src/pages/Ingest.tsx
@frontend/src/hooks/useIngest.ts
@frontend/src/hooks/useExport.ts
@frontend/src/types/goal.ts

<interfaces>
frontend/src/types/goal.ts (ADD):
  export interface AdvisoryFieldChange { field: string; old: unknown; new: unknown; }
  export interface AdvisoryEntityDiff {
    external_key: string; title: string; action: "update" | "create";
    rationale: string; fields: AdvisoryFieldChange[];
  }
  export interface AdvisoryPreviewResult {
    goals: AdvisoryEntityDiff[]; milestones: AdvisoryEntityDiff[]; new_tasks: AdvisoryEntityDiff[];
    notes: string | null; session_id: string; generated_at: string;
  }
  export interface AdvisoryResult { created: Record<string,number>; updated: Record<string,number>; advisory_id: string; replayed: boolean; }
  // also add priority_rank?: number | null to the existing Goal interface

frontend/src/lib/advisoryId.ts:
  export async function computeAdvisoryId(fullPayload: unknown): Promise<string>
    // canonical JSON (recursively sorted keys) -> SHA-256 hex via crypto.subtle.digest

frontend/src/hooks/useAdvisory.ts (model on useIngest.ts):
  preview(payload) -> POST /api/v1/advisory/preview ; sets previewResult/notes/sessionId/generatedAt/errors
  confirm(req: {advisory_id, payload}) -> POST /api/v1/advisory/confirm ; resolves AdvisoryResult | null
  parse422 reused; state: previewResult, errors, previewing, confirming, reset()

Advisor.tsx new sections (append after the Snapshot section):
  - header line near <h1>: "Last advisor sync: {N} days ago" (fetch last_advisory_at — reuse an
    existing settings fetch or add a tiny GET; if no endpoint exists, read it from the confirm
    response/AppSettings via the existing settings hook — Claude's discretion, must show the line)
  - Paste advisory response: <textarea value={raw}> + "Run preview" button (JSON.parse in
    try/catch -> parseError state; then call advisory.preview(parsed))
  - after preview: notes callout (if previewResult.notes) ABOVE the diff
  - staleness banner: if (Date.now() - Date.parse(previewResult.generated_at)) > 7*864e5 ->
    non-blocking banner; Confirm still enabled
  - AdvisoryDiffGroup x3 (Goals / Milestones / New tasks): per row a checkbox (default checked),
    entity title + badge, per-field old→new lines, rationale sub-text always visible
  - Confirm button: build acceptedPayload = previewResult filtered to checked rows mapped back to
    the original pasted payload items; advisory_id = await computeAdvisoryId(fullPastedPayload);
    advisory.confirm({advisory_id, payload: acceptedPayload})
  - on success: setSummary from AdvisoryResult; render "N goals · M milestones · K tasks" + <Link to="/goals">; do NOT navigate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: advisory types + Goal.priority_rank + computeAdvisoryId</name>
  <files>frontend/src/types/goal.ts, frontend/src/lib/advisoryId.ts</files>
  <read_first>
    - frontend/src/types/goal.ts (existing Goal interface 12-26; IngestEntityDiff/IngestPreviewResult 52-63 to mirror)
  </read_first>
  <action>
    In frontend/src/types/goal.ts add the four advisory interfaces from <interfaces> and add
    `priority_rank?: number | null;` to the existing Goal interface.
    Create frontend/src/lib/advisoryId.ts:
        function canonical(v: unknown): unknown { /* recursively sort object keys; arrays preserved */ }
        export async function computeAdvisoryId(fullPayload: unknown): Promise<string> {
          const json = JSON.stringify(canonical(fullPayload));
          const buf = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(json));
          return [...new Uint8Array(buf)].map(b => b.toString(16).padStart(2,"0")).join("");
        }
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types/goal.ts contains `AdvisoryEntityDiff`, `AdvisoryPreviewResult`, `AdvisoryResult`, `AdvisoryFieldChange`
    - types/goal.ts Goal interface contains `priority_rank`
    - lib/advisoryId.ts contains `export async function computeAdvisoryId`
    - lib/advisoryId.ts contains `crypto.subtle.digest("SHA-256"`
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Types + priority_rank + computeAdvisoryId compile clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useAdvisory hook + unit test</name>
  <files>frontend/src/hooks/useAdvisory.ts, frontend/src/hooks/useAdvisory.test.ts</files>
  <read_first>
    - frontend/src/hooks/useIngest.ts (full — parse422, preview/confirm fetch, 200/422/network handling, reset)
    - frontend/src/hooks/useExport.ts (how sessionId/generatedAt are held in state)
    - frontend/src/types/goal.ts (AdvisoryPreviewResult / AdvisoryResult)
  </read_first>
  <action>
    Create frontend/src/hooks/useAdvisory.ts as a near-copy of useIngest.ts with:
      - URLs /api/v1/advisory/preview and /api/v1/advisory/confirm
      - previewResult: AdvisoryPreviewResult | null; errors: string[]; previewing/confirming bools
      - preview(payload) parses 422 via the same parse422 helper (copy it)
      - confirm(req: { advisory_id: string; payload: unknown }) returns Promise<AdvisoryResult | null>
        (200 -> parsed AdvisoryResult; 422 -> set errors, return null; network -> catch)
      - reset()
    Create frontend/src/hooks/useAdvisory.test.ts (Vitest): mock fetch; assert preview sets
      previewResult on 200 and errors on 422; assert confirm returns the AdvisoryResult on 200 and
      null on 422.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/hooks/useAdvisory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - hooks/useAdvisory.ts contains `export function useAdvisory`
    - hooks/useAdvisory.ts contains `/api/v1/advisory/preview` and `/api/v1/advisory/confirm`
    - hooks/useAdvisory.ts contains `parse422`
    - `cd frontend && npx vitest run src/hooks/useAdvisory.test.ts` -> passes
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>useAdvisory preview/confirm behave like useIngest with advisory URLs; hook test green.</done>
</task>

<task type="auto">
  <name>Task 3: Advisor.tsx — paste/notes/staleness/diff/accept-reject/confirm/success</name>
  <files>frontend/src/pages/Advisor.tsx</files>
  <read_first>
    - frontend/src/pages/Advisor.tsx (full — the Phase-15 sections; append below the Snapshot section, keep them intact)
    - frontend/src/pages/Ingest.tsx (DiffGroup 7-35 for the visual starting point; paste→preview→confirm→error-list flow; note it navigate()s — Advisor must NOT)
    - frontend/src/hooks/useAdvisory.ts (preview/confirm + state)
    - frontend/src/lib/advisoryId.ts (computeAdvisoryId)
    - frontend/src/types/goal.ts (AdvisoryPreviewResult/AdvisoryEntityDiff)
  </read_first>
  <action>
    Extend Advisor.tsx (do NOT remove the Phase-15 prompt/brief/preview/snapshot sections):
    - State: rawAdvisory (textarea), parseError, accepted (Set/record of row identifiers keyed by
      group+external_key, default-checked when previewResult arrives), summary (AdvisoryResult|null),
      lastAdvisoryAt (from settings).
    - Header: near the page title render "Last advisor sync: {daysAgo(lastAdvisoryAt)} days ago"
      (or "never" if null). Source last_advisory_at via the existing settings hook/fetch; if none
      exists, add a minimal GET to AppSettings — discretion, but the line MUST render.
    - Paste section (below Snapshot): <textarea value={rawAdvisory} ...> + "Run preview" button.
      onClick: try JSON.parse(rawAdvisory) -> parsed; catch -> setParseError; then
      advisory.preview(parsed) and stash parsed as the FULL pasted payload for hashing.
    - On previewResult: initialize `accepted` to all rows checked (D-03 default-accept).
    - Notes callout: if previewResult.notes -> a div with accent border (var(--border)/accent),
      quote style, rendered ABOVE the diff (D-09).
    - Staleness banner: if Date.now() - Date.parse(previewResult.generated_at) > 7*864e5 ->
      non-blocking banner ("This advisory is N days old"); Confirm stays enabled (D-11).
    - AdvisoryDiffGroup(label, items): for each AdvisoryEntityDiff render a row with: a checkbox
      bound to accepted[label+external_key] (default checked), the title + a diff-badge
      (create/update), the per-field lines `field: old → new` (new tasks: `field: → new`, add-row,
      no old), and the rationale ALWAYS as sub-text beneath (D-01). Reuse diff-badge* + var(--surface)/
      var(--border). Render three groups: Goals, Milestones, New tasks.
    - Validation errors: render advisory.errors as <li className="error-list-item"> (reuse pattern).
    - Confirm button (enabled when previewResult exists and >=1 row accepted):
        const acceptedPayload = rebuild an AdvisoryPayload from the ORIGINAL pasted payload,
          keeping only rows whose accepted[...] is true (filter goal_adjustments by external_key,
          milestone_adjustments by goal_external_key+title, new_tasks by index/external_key);
        const advisory_id = await computeAdvisoryId(fullPastedPayload);   // FULL, unfiltered
        const res = await advisory.confirm({ advisory_id, payload: acceptedPayload });
        if (res) setSummary(res);   // DO NOT navigate
    - Success summary: when summary -> render "{summary.created.goals+updated.goals} goals · ...
      milestones · ... tasks adjusted" + <Link to="/goals">View in Goals</Link> (D-08). If
      summary.replayed, note "already applied".
    Use inline styles + CSS vars consistent with the existing sections.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Advisor.tsx still contains the Phase-15 markers (e.g. the copy-prompt / brief / snapshot sections — grep for `ADVISOR_PROMPT` and `snapshot`)
    - Advisor.tsx contains `Run preview` and a `<textarea`
    - Advisor.tsx contains `computeAdvisoryId(` and posts via `useAdvisory`
    - Advisor.tsx contains `Last advisor sync`
    - Advisor.tsx contains a `<Link to="/goals"` and does NOT call `navigate("/goals")`
    - Advisor.tsx renders rationale per row (grep `rationale`)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Sync page shows paste→preview→accept/reject→confirm→in-page summary; notes callout +
    staleness banner present; advisory_id hashed over the full reply; no auto-navigation; builds clean.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` -> 0 errors
- `cd frontend && npx vitest run src/hooks/useAdvisory.test.ts` -> green
- `cd frontend && npm run build` -> succeeds
- Manual golden path (per CLAUDE.md "test the golden path"): paste a sample advisory reply ->
  preview shows grouped diff with rationale -> uncheck one row -> Confirm -> stay on page,
  success summary + Goals link; re-paste same reply -> replayed (no dupes)
</verification>

<success_criteria>
- ADVISE-04/07 + SYNC-01/02 delivered: grouped field-level diff with always-visible rationale,
  default-accept per-row reject, single-page loop, last-sync line + non-blocking staleness banner,
  stay-on-page success summary; advisory_id stable across accepted subset (one-shot idempotency).
</success_criteria>

<output>
After completion, create `.planning/phases/16-advisory-ingest-sync-review-ui/16-04-SUMMARY.md`
</output>
