---
phase: 15-context-export-advisor-prompt
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - frontend/src/hooks/useExport.ts
  - frontend/src/pages/Advisor.tsx
  - frontend/src/App.tsx
  - frontend/src/components/BottomNav.tsx
autonomous: false
requirements: [EXPORT-01, PROMPT-01]
must_haves:
  truths:
    - "Visiting /advisor renders the Sync page with: Copy advisor prompt button, Copy advisory brief button, on-demand Snapshot button, and a read-only rendered-brief preview"
    - "Clicking Copy advisory brief fetches GET /api/v1/export/bundle and copies the markdown (header has generated_at + session_id) to the clipboard"
    - "Clicking Copy advisor prompt copies ADVISOR_PROMPT (incl. [SCHEMA BLOCK] + >=1 new-task example) to the clipboard"
    - "Clicking Snapshot calls POST /api/v1/export/snapshot"
    - "A Sync entry appears in the bottom navigation and the /advisor route resolves"
  artifacts:
    - path: "frontend/src/hooks/useExport.ts"
      provides: "fetchBundle + triggerSnapshot fetch/loading/error hook modeled on useIngest.ts"
      contains: "export function useExport"
    - path: "frontend/src/pages/Advisor.tsx"
      provides: "Sync page shell (copy-prompt, copy-bundle, snapshot, preview)"
      contains: "ADVISOR_PROMPT"
    - path: "frontend/src/App.tsx"
      provides: "/advisor route"
      contains: "/advisor"
    - path: "frontend/src/components/BottomNav.tsx"
      provides: "Sync nav entry"
      contains: "/advisor"
  key_links:
    - from: "frontend/src/hooks/useExport.ts"
      to: "/api/v1/export/bundle"
      via: "fetch GET"
      pattern: "/api/v1/export/bundle"
    - from: "frontend/src/hooks/useExport.ts"
      to: "/api/v1/export/snapshot"
      via: "fetch POST (on-demand snapshot deferred from Phase 14)"
      pattern: "/api/v1/export/snapshot"
    - from: "frontend/src/pages/Advisor.tsx"
      to: "frontend/src/lib/advisorPrompt.ts"
      via: "import ADVISOR_PROMPT"
      pattern: "from \"../lib/advisorPrompt\""
---

<objective>
Build the `/advisor` Sync page -- the full shell Phase 16 extends. It exposes one-click copy
of the advisor system prompt (PROMPT-01) and the export bundle (EXPORT-01), an on-demand
snapshot trigger (the Phase 14 button that belongs here), and a read-only preview of the
rendered brief so the user can eyeball it before pasting.

Purpose: Deliver the UI half of EXPORT-01 + PROMPT-01 and create the page Phase 16 builds on.
Output: `useExport.ts` (new), `Advisor.tsx` (new), `/advisor` route in App.tsx, Sync nav entry
in BottomNav.tsx.

This plan is NON-AUTONOMOUS: it ends in a human-verify checkpoint because clipboard copy and
the full backend->clipboard round-trip are browser-runtime behaviors (per 15-VALIDATION.md
Manual-Only Verifications). It depends on 15-02 so the `/bundle` endpoint exists for the
golden-path test, and on 15-01 for advisorPrompt.ts.

OUT OF SCOPE (Phase 16): pasting an advisory reply, diff preview, confirm/apply, the
"last sync: N days ago" stamp. This page only produces and copies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-context-export-advisor-prompt/15-CONTEXT.md
@.planning/phases/15-context-export-advisor-prompt/15-RESEARCH.md
@frontend/src/hooks/useIngest.ts
@frontend/src/pages/Ingest.tsx
@frontend/src/App.tsx
@frontend/src/components/BottomNav.tsx
@frontend/src/lib/advisorPrompt.ts

<interfaces>
Backend contract (from 15-02):
- GET /api/v1/export/bundle -> 200 JSON {markdown: string, session_id: string, generated_at: string}
- POST /api/v1/export/snapshot -> 200 JSON {created: number, skipped: number}

Frontend patterns to mirror (confirmed by inspection):
- useIngest.ts: useState loading/error pattern, fetch with try/catch/finally, parse on res.ok
- Ingest.tsx clipboard copy (lines 42-48):
      const [copied, setCopied] = useState(false);
      async function handleCopy() {
        await navigator.clipboard.writeText(TEXT);
        setCopied(true);
        setTimeout(() => setCopied(false), 2000);
      }
- Ingest.tsx markup classes already in styles.css: .page, .page-title, .section-label,
  .btn-text-accent, .btn-save, .prompt-block
- ADVISOR_PROMPT exported from ../lib/advisorPrompt (created in 15-01)
- BottomNav.tsx: 5 NavLink tabs (Today/Tasks/Goals/Organize/Settings), lucide-react icons,
  NavLink style callback `({ isActive }) => ({ ...tabStyle, color: isActive ? "var(--accent)" : "var(--text-secondary)" })`
  NOTE: the existing /ingest route is NOT in BottomNav -- adding a 6th tab here is the first
  nav addition; verify the flex:1 layout still fits (Pitfall 5: check 320px width).
- App.tsx: route list inside <Routes>; add <Route path="/advisor" element={<Advisor />} />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useExport.ts hook (fetchBundle + triggerSnapshot)</name>
  <files>frontend/src/hooks/useExport.ts</files>
  <read_first>
    - frontend/src/hooks/useIngest.ts (the exact loading/error/fetch pattern to mirror)
  </read_first>
  <action>
    Create frontend/src/hooks/useExport.ts mirroring useIngest.ts's state shape. Export:
        export function useExport() {
          const [bundle, setBundle] = useState<string | null>(null);
          const [sessionId, setSessionId] = useState<string | null>(null);
          const [generatedAt, setGeneratedAt] = useState<string | null>(null);
          const [loading, setLoading] = useState(false);
          const [snapshotting, setSnapshotting] = useState(false);
          const [error, setError] = useState<string | null>(null);

          async function fetchBundle(): Promise<string | null> {
            setLoading(true); setError(null);
            try {
              const res = await fetch("/api/v1/export/bundle");
              if (res.ok) {
                const data = await res.json();
                setBundle(data.markdown); setSessionId(data.session_id); setGeneratedAt(data.generated_at);
                return data.markdown as string;
              }
              setError(`Failed to load bundle (HTTP ${res.status}).`);
              return null;
            } catch {
              setError("Failed to load bundle — check your connection.");
              return null;
            } finally { setLoading(false); }
          }

          async function triggerSnapshot(): Promise<{created:number;skipped:number} | null> {
            setSnapshotting(true); setError(null);
            try {
              const res = await fetch("/api/v1/export/snapshot", { method: "POST" });
              if (res.ok) return await res.json();
              setError(`Snapshot failed (HTTP ${res.status}).`);
              return null;
            } catch {
              setError("Snapshot failed — check your connection.");
              return null;
            } finally { setSnapshotting(false); }
          }

          return { bundle, sessionId, generatedAt, loading, snapshotting, error, fetchBundle, triggerSnapshot };
        }
    `fetchBundle` returns the markdown so the page can fetch-then-copy in one click.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - useExport.ts contains `export function useExport`
    - useExport.ts contains the literal `/api/v1/export/bundle`
    - useExport.ts contains the literal `/api/v1/export/snapshot`
    - useExport.ts contains `method: "POST"` for the snapshot call
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>useExport hook compiles and exposes fetchBundle + triggerSnapshot with loading/error state.</done>
</task>

<task type="auto">
  <name>Task 2: Advisor.tsx Sync page + /advisor route + BottomNav Sync entry</name>
  <files>frontend/src/pages/Advisor.tsx, frontend/src/App.tsx, frontend/src/components/BottomNav.tsx</files>
  <read_first>
    - frontend/src/pages/Ingest.tsx (page layout, .page/.page-title/.section-label classes, copy-button pattern, .prompt-block)
    - frontend/src/lib/advisorPrompt.ts (ADVISOR_PROMPT export to import)
    - frontend/src/hooks/useExport.ts (the hook from Task 1)
    - frontend/src/App.tsx (route list + import ordering)
    - frontend/src/components/BottomNav.tsx (NavLink pattern, tabStyle, lucide-react import line)
  </read_first>
  <action>
    Create frontend/src/pages/Advisor.tsx (default export `Advisor`). Use the Ingest.tsx page
    structure (className "page", h1 "page-title"). Import:
        import { useState } from "react";
        import { useExport } from "../hooks/useExport";
        import { ADVISOR_PROMPT } from "../lib/advisorPrompt";

    Sections (D-09 -- all four required):
    1. ADVISOR PROMPT section: a `.section-label` "Advisor prompt", a `.btn-text-accent` button
       "Copy advisor prompt" that does `navigator.clipboard.writeText(ADVISOR_PROMPT)` then shows
       "Copied!" for 2s (separate `promptCopied` boolean). Render ADVISOR_PROMPT in a
       `<pre className="prompt-block">`.
    2. ADVISORY BRIEF section: a `.btn-save` button "Copy advisory brief" that calls
       `const md = await ex.fetchBundle(); if (md) { await navigator.clipboard.writeText(md);
       setBriefCopied(true); setTimeout(...) }` (separate `briefCopied` boolean). Disable while
       `ex.loading`; show "Loading…" label. Show `ex.error` in `var(--destructive)` if set.
    3. PREVIEW: when `ex.bundle` is set, render it read-only in a `<pre className="prompt-block">`
       so the user can eyeball before pasting (D-09). Show generatedAt + sessionId above it.
    4. SNAPSHOT section: a button "Take snapshot now" calling `ex.triggerSnapshot()`; disable
       while `ex.snapshotting`; on success show a small confirmation like
       "Snapshot saved (N created)". This is the Phase 14 on-demand snapshot button relocated here.

    Use `const ex = useExport();` and two/three local `useState` booleans for the copied flags.
    Match Ingest.tsx inline-style conventions; exact styling/collapsibility is Claude's discretion.

    In frontend/src/App.tsx: add `import Advisor from "./pages/Advisor";` and inside <Routes>
    add `<Route path="/advisor" element={<Advisor />} />` (keep all existing routes).

    In frontend/src/components/BottomNav.tsx: import an icon (e.g. `Bot`) from lucide-react by
    adding it to the existing import line. Add a 6th NavLink BEFORE Settings:
        <NavLink to="/advisor" style={({ isActive }) => ({ ...tabStyle,
          color: isActive ? "var(--accent)" : "var(--text-secondary)" })}>
          <Bot size={22} />
          Sync
        </NavLink>
    The nav uses flex:1 per tab; a 6th tab narrows each evenly (Pitfall 5 -- acceptable, verify
    at 320px in the checkpoint).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Advisor.tsx contains `import { ADVISOR_PROMPT } from "../lib/advisorPrompt"`
    - Advisor.tsx contains `useExport`
    - Advisor.tsx contains `navigator.clipboard.writeText`
    - Advisor.tsx references `triggerSnapshot` (the snapshot button)
    - App.tsx contains `path="/advisor"` and `import Advisor`
    - BottomNav.tsx contains `to="/advisor"` and the text `Sync`
    - BottomNav.tsx imports the chosen icon from lucide-react (e.g. `Bot`)
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    /advisor renders the four-section Sync page; route + nav entry wired; project type-checks and
    builds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the /advisor Sync page round-trip</name>
  <action>Pause for the user to exercise the full /advisor page in a running stack (see how-to-verify). No code changes — this gate confirms clipboard copy, the bundle round-trip, calendar privacy (counts only), the on-demand snapshot, and the Sync nav entry all work end-to-end before the phase is marked done.</action>
  <what-built>
    The /advisor Sync page (copy-prompt, copy-bundle, snapshot, preview), wired to the
    GET /api/v1/export/bundle and POST /api/v1/export/snapshot backend endpoints from 15-02.
  </what-built>
  <how-to-verify>
    1. Start the stack (backend + `cd frontend && npm run dev`) and open the app.
    2. Confirm a "Sync" tab appears in the bottom nav; tap it -> the /advisor page loads. Check
       the 6-tab nav is not visually broken at a narrow (~320px) width.
    3. Click "Copy advisor brief". Paste into a text editor. Verify the markdown starts with
       "# Advisor Brief" and the header line contains both `generated_at:` and `session_id:`.
       Verify it lists your active goals (career/learning first), a 14-day block summary, a
       7-day calendar load showing per-day COUNTS ONLY (no event titles -- privacy check D-05),
       a trend/velocity label per goal, and a Stalled Goals section.
    4. Click "Copy advisor prompt". Paste into a text editor. Verify it contains: role framing
       (advisor for Jack, 4-week horizon), an in-scope and an out-of-scope list, the literal
       `[SCHEMA BLOCK]` placeholder, an example payload that includes AT LEAST ONE new-task item,
       and notes-field guidance.
    5. Click "Take snapshot now". Verify it succeeds (confirmation shows; no error). The
       read-only brief preview should display the rendered markdown after a brief fetch.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what was wrong (e.g. event titles leaked, missing section, nav broken).</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` -> exit 0
- `cd frontend && npm run build` -> exit 0
- Human checkpoint: copy-brief and copy-prompt produce correct clipboard content; snapshot fires;
  calendar load shows counts only (no titles); Sync nav entry present and route resolves
</verification>

<success_criteria>
- EXPORT-01 (UI half): one click copies the complete brief (header carries generated_at + session_id)
- PROMPT-01 (UI half): one click copies the advisor prompt (role framing, scope lists,
  [SCHEMA BLOCK], example payload with a new-task item, notes guidance)
- On-demand snapshot button (Phase 14 deferral) lives on this page and fires
- /advisor route + Sync nav entry wired; project builds
</success_criteria>

<output>
After completion, create `.planning/phases/15-context-export-advisor-prompt/15-03-SUMMARY.md`
</output>
