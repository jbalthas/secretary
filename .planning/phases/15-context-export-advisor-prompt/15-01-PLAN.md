---
phase: 15-context-export-advisor-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_export.py
  - frontend/src/lib/advisorPrompt.ts
autonomous: true
requirements: [EXPORT-01, EXPORT-02, EXPORT-03, EXPORT-04, EXPORT-05, EXPORT-06, PROMPT-01]
must_haves:
  truths:
    - "Running pytest tests/test_export.py collects 8 test functions and the export_service-dependent ones FAIL/ERROR (RED) because export_service does not yet exist"
    - "test_no_llm_imports passes against the current backend/app/ (locked CI guard)"
    - "advisorPrompt.ts exists and exports ADVISOR_PROMPT containing the literal [SCHEMA BLOCK] placeholder"
  artifacts:
    - path: "backend/tests/test_export.py"
      provides: "Wave 0 RED test scaffold for all EXPORT-01..06 + test_no_llm_imports CI guard"
      contains: "def test_bundle_endpoint"
    - path: "frontend/src/lib/advisorPrompt.ts"
      provides: "Advisor system prompt with [SCHEMA BLOCK] placeholder (PROMPT-01)"
      contains: "[SCHEMA BLOCK]"
  key_links:
    - from: "backend/tests/test_export.py"
      to: "app.services.export_service"
      via: "deferred import inside test body"
      pattern: "from app.services.export_service import|from app.services import export_service"
---

<objective>
Wave 0 scaffolding for Phase 15. Create the failing test file that defines the
behavioral contract for the export bundle (so Wave 1 backend work has a RED->GREEN
target), and create the static advisor system prompt file with the `[SCHEMA BLOCK]`
placeholder (so the Wave 1 frontend Advisor page can import it without a missing-module error).

This plan writes NO production backend logic. The export_service-dependent tests are
expected to be RED. The advisorPrompt.ts content is the finished deliverable for
PROMPT-01's prompt body EXCEPT the JSON schema, which is intentionally the literal placeholder.

Purpose: Establish the Nyquist verification target (per 15-VALIDATION.md) and unblock both
Wave 1 plans (15-02 backend needs the tests; 15-03 frontend needs advisorPrompt.ts).
Output: `backend/tests/test_export.py`, `frontend/src/lib/advisorPrompt.ts`.

PHASE-16 DEFERRAL FLAG (PROMPT-01): The `[SCHEMA BLOCK]` literal in advisorPrompt.ts is a
deliberate placeholder. Phase 16's final plan does a one-line find-replace of `[SCHEMA BLOCK]`
with the output of `AdvisoryPayload.model_json_schema()` once the advisory Pydantic models
are finalized. Per research, `AdvisoryPayload` does NOT yet exist -- do NOT hand-write the
schema now and do NOT create AdvisoryPayload in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-context-export-advisor-prompt/15-CONTEXT.md
@.planning/phases/15-context-export-advisor-prompt/15-RESEARCH.md
@.planning/phases/15-context-export-advisor-prompt/15-VALIDATION.md
@backend/tests/test_snapshots.py
@frontend/src/lib/ingestPrompt.ts

<interfaces>
Contracts Wave 1 backend (15-02) implements against. Tests must assert these:
- export_service.build_export_bundle() returns dict: {markdown: str, session_id: str, generated_at: str}
- export_service._velocity_label(snapshots) -> str  (snapshots ordered oldest->newest, each has .progress_pct)
- GET /api/v1/export/bundle returns 200 with JSON {markdown, session_id, generated_at}

Session-patching: export_service calls _compute_progress_sync (from brief.py) and
_find_stalled_goals (from guidance_service.py); each module has its own module-level
_Session. The test fixture must patch ALL THREE to the same test Session:
    with patch("app.services.export_service._Session", Session), \
         patch("app.services.brief._Session", Session), \
         patch("app.services.guidance_service._Session", Session):
        yield Session

Confirmed model fields (do not guess):
- Goal: id, external_key, title, type (GoalType: career/life/health/learning/financial),
  target_date (date|None), status (GoalStatus: active/archived/completed), milestones, tasks
- Milestone: id, goal_id, title, target_date, done (bool)
- GoalProgressSnapshot: goal_id, snapshotted_on (date), progress_pct (int)
- Task: id, title, priority (Priority: high/medium/low -- use .value), due_date (datetime|None),
  completed (bool), completed_at, goal_id, is_habit, external_key
- ScheduledBlock: id, title, date_key (str "YYYY-MM-DD"), completed (bool), task_id
- CalendarEvent: start_dt (UtcDateTime tz-aware), start_date (str|None all-day),
  all_day (bool), cancelled (bool), title (NEVER export per D-05)

Test imports:
    from app.db import Base
    from app.main import app
    from app.models import Task, Priority
    from app.models.goal import Goal, GoalProgressSnapshot, GoalStatus, GoalType, Milestone
    from app.models.plan import ScheduledBlock
    from app.models.calendar import CalendarEvent
    from fastapi.testclient import TestClient
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test scaffold -- backend/tests/test_export.py</name>
  <files>backend/tests/test_export.py</files>
  <read_first>
    - backend/tests/test_snapshots.py (the tmp_path session fixture + TestClient + patch("..._Session", Session) pattern to mirror exactly)
    - backend/app/services/brief.py (lines 1-27: _compute_progress_sync signature + module-level _Session)
    - backend/app/services/guidance_service.py (lines 26-52: _find_stalled_goals(session, threshold_days) + get_stalled_goals + its own _Session)
    - backend/app/models/goal.py (GoalType, GoalStatus, GoalProgressSnapshot, Goal, Milestone fields)
    - backend/app/models/plan.py (ScheduledBlock.date_key, .completed)
    - backend/app/models/calendar.py (CalendarEvent.start_dt, start_date, all_day, cancelled, title)
    - backend/app/models/__init__.py (Task.priority is Priority enum, .due_date, .completed, .is_habit, .completed_at)
  </read_first>
  <action>
    Create backend/tests/test_export.py. All export_service-dependent tests use DEFERRED imports
    of `app.services.export_service` INSIDE the test body (mirrors Phase 08 decision: defer the
    service import so the file collects before export_service.py exists).

    Build an `export_session` fixture like test_snapshots.py's tmp_path fixture, but patch all
    three module _Session refs to one test Session (see <interfaces> patch block). Seed rows via
    the fixture session and commit before calling the service.

    Write EXACTLY these 8 test functions (names are contractual -- 15-VALIDATION.md references them):

    1. def test_no_llm_imports():  -- CI GUARD (locked hard constraint). Does NOT import
       export_service; passes immediately. Implementation:
           import pathlib
           root = pathlib.Path(__file__).resolve().parent.parent / "app"
           offenders = [str(p) for p in root.rglob("*.py")
                        if any(s in p.read_text(encoding="utf-8") for s in ("anthropic","openai","litellm"))]
           assert offenders == [], f"LLM library import found in: {offenders}"

    2. def test_velocity_label():  -- EXPORT-04 pure logic. Deferred-import
       `from app.services.export_service import _velocity_label`. Use
       types.SimpleNamespace(progress_pct=N) stand-ins. Assert (snapshots ordered oldest->newest):
           _velocity_label([]) == "no_data"
           _velocity_label([SN(50)]) == "no_data"
           _velocity_label([SN(40), SN(55)]) == "accelerating"   # delta +15 >= 10
           _velocity_label([SN(60), SN(50)]) == "stalling"        # delta -10 <= -5
           _velocity_label([SN(50), SN(52)]) == "steady"          # delta +2

    3. def test_bundle_endpoint(export_session):  -- EXPORT-01. Seed one active career Goal.
       TestClient(app).get("/api/v1/export/bundle"). Assert status_code == 200; JSON body has
       keys "markdown","session_id","generated_at"; body["markdown"].startswith("# Advisor Brief");
       body["session_id"] is a non-empty str.

    4. def test_bundle_contains_goal_section(export_session):  -- EXPORT-02. Seed active career
       Goal "Ship v2" with one Milestone "Beta cut", a high-priority Task with a due_date, a low
       Task, and one overdue Task (completed=False, due_date in the past). Call
       export_service.build_export_bundle(); assert markdown contains "Ship v2", "career", "%",
       "Beta cut", both top task titles, and "overdue" (case-insensitive) reflecting the count.

    5. def test_block_summary(export_session):  -- EXPORT-03. Seed 3 ScheduledBlock rows in the
       last 14 days: 1 completed=True; 1 completed=False with date_key < today (slipped); 1
       completed=False with date_key == today (upcoming, NOT slipped). Call build_export_bundle();
       assert markdown shows Planned=3, Completed=1, Slipped=1.

    6. def test_goal_ordering(export_session):  -- EXPORT-06. Seed three active goals:
       type=health "Run", type=career "Promo", type=learning "Rust". Call build_export_bundle();
       assert markdown.index("Promo") < markdown.index("Run") AND
       markdown.index("Rust") < markdown.index("Run") (career & learning before health).

    7. def test_calendar_section_privacy(export_session):  -- EXPORT-05. Seed two CalendarEvent
       rows in the next 7 days with title "DENTIST APPOINTMENT" (one all_day with start_date set,
       one timed with start_dt set), cancelled=False. Also seed a stalled goal (active goal +
       linked non-habit task with completed_at None). Call build_export_bundle(); assert
       "DENTIST APPOINTMENT" NOT in markdown (D-05 privacy); assert a "Calendar" header and a
       per-day digit count are present; assert the stalled goal title appears under a "Stalled" section.

    8. def test_trend_no_data(export_session):  -- EXPORT-04 integration. Seed an active goal with
       ZERO GoalProgressSnapshot rows; assert build_export_bundle() markdown contains "no_data"
       for that goal's trend (graceful degradation).

    Add a local seed helper and `from app.models import Priority` for task priorities. Do NOT
    create export_service.py or the /bundle route here -- those are 15-02. Tests 3-8 are EXPECTED RED.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_export.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && python -m pytest tests/test_export.py --collect-only -q` lists exactly 8 tests and exits 0
    - test_export.py contains `def test_no_llm_imports`
    - test_export.py contains `def test_bundle_endpoint`
    - test_export.py contains `def test_velocity_label`
    - test_export.py contains `def test_block_summary`
    - test_export.py contains `def test_goal_ordering`
    - test_export.py contains `def test_calendar_section_privacy`
    - test_export.py contains `def test_trend_no_data`
    - test_export.py contains the literal `patch("app.services.export_service._Session"`
    - `cd backend && python -m pytest tests/test_export.py::test_no_llm_imports -x -q` exits 0
    - `cd backend && python -m pytest tests/test_export.py::test_bundle_endpoint -x -q` FAILS or ERRORS (RED)
  </acceptance_criteria>
  <done>
    test_export.py collects 8 tests; test_no_llm_imports passes; export_service-dependent tests
    are RED. The contract for 15-02 is locked.
  </done>
</task>

<task type="auto">
  <name>Task 2: advisorPrompt.ts with [SCHEMA BLOCK] placeholder (PROMPT-01)</name>
  <files>frontend/src/lib/advisorPrompt.ts</files>
  <read_first>
    - frontend/src/lib/ingestPrompt.ts (the TypeScript string-constant export shape to mirror)
    - .planning/phases/15-context-export-advisor-prompt/15-RESEARCH.md (section "Advisor System Prompt Structure (PROMPT-01)" -- the 8 required sections)
    - .planning/REQUIREMENTS.md (ADVISE-08 and PROMPT-01 -- exact scope wording)
  </read_first>
  <action>
    Create frontend/src/lib/advisorPrompt.ts exporting one string constant:
        export const ADVISOR_PROMPT = `...`;
    Mirror the backtick-template-literal shape of ingestPrompt.ts. The prompt body MUST contain
    these 8 sections, in order (PROMPT-01 + D-14/D-15/D-16):

    1. ROLE FRAMING: "You are a career and engineering advisor for Jack." State a 4-week planning
       horizon and that career- and learning-type goals are prioritized.
    2. IN-SCOPE -- advisor MAY: adjust a goal's `target_date` and `priority_rank` (matched by
       `external_key`); adjust a milestone's `target_date`, `done` (forward-only), and `title`
       (matched by goal + title); propose NEW tasks (see section 4). Each item carries a required
       `rationale`.
    3. OUT-OF-SCOPE -- advisor MUST NOT: create goals, or change a goal's status, title, or type;
       edit, complete, or delete any EXISTING task.
    4. NEW-TASK SCOPE (ADVISE-08, D-15): advisor MAY propose new tasks with `title` (REQUIRED);
       optional `description`, `due_date`, `priority`, `estimated_minutes`; each linked to a goal
       by `external_key`; each carrying a REQUIRED `rationale`.
    5. JSON SCHEMA: a line containing exactly `[SCHEMA BLOCK]` on its own, preceded by a comment
       line "The advisory response JSON schema:". Do NOT hand-write any schema (D-16).
    6. EXAMPLE PAYLOAD: a JSON example with at least one goal adjustment (with rationale), at
       least one milestone adjustment (with rationale), and AT LEAST ONE new-task item (title +
       external_key + rationale -- D-15 hard requirement). Include a top-level `session_id` and a
       top-level `notes` string in the example.
    7. NOTES GUIDANCE: the top-level free-text `notes` field is surfaced to the user before
       confirm and is NEVER written to goal/milestone/task entities.
    8. SESSION_ID ECHO (D-12): instruct the LLM to copy the `session_id` from the brief header
       verbatim into the top-level `session_id` of its advisory JSON reply.

    Add a top-of-file comment:
        // PROMPT-01: [SCHEMA BLOCK] is a placeholder. Phase 16 replaces it (one-line find-replace)
        // with AdvisoryPayload.model_json_schema() output. Do not hand-write the schema.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - advisorPrompt.ts contains `export const ADVISOR_PROMPT`
    - advisorPrompt.ts contains the literal `[SCHEMA BLOCK]`
    - advisorPrompt.ts contains `external_key`
    - advisorPrompt.ts contains `rationale`
    - advisorPrompt.ts contains `session_id`
    - advisorPrompt.ts contains `notes`
    - advisorPrompt.ts contains a top-of-file comment naming Phase 16 and `model_json_schema`
    - `grep -ci "out-of-scope\|out of scope" frontend/src/lib/advisorPrompt.ts` >= 1
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    advisorPrompt.ts exists, type-checks, exports ADVISOR_PROMPT with all 8 sections and the
    literal [SCHEMA BLOCK] placeholder flagged for Phase 16 replacement.
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_export.py --collect-only -q` -> 8 tests collected, exit 0
- `cd backend && python -m pytest tests/test_export.py::test_no_llm_imports -x -q` -> pass
- `cd frontend && npx tsc --noEmit` -> exit 0; advisorPrompt.ts imports cleanly
- export_service-dependent tests are RED (expected until 15-02)
</verification>

<success_criteria>
- 8 named test functions exist and collect
- CI guard test passes
- advisorPrompt.ts ships with the literal [SCHEMA BLOCK] and all PROMPT-01 sections
- No AdvisoryPayload created; no schema hand-written; Phase 16 deferral flagged in-file
</success_criteria>

<output>
After completion, create `.planning/phases/15-context-export-advisor-prompt/15-01-SUMMARY.md`
</output>
